# GPT Pro SDPX Performance Optimization Plan — 2026-08-28

- Conversation: https://chatgpt.com/c/6a912f20-707c-83ec-946b-7d4a6ed680cc
- Frozen evidence source: `c7a912dd91e12a25bd4b90b3eca2c2283ee41dd5`
- Follow-up scope: performance only; correctness invariants unchanged
- Generation time reported by GPT Pro: 29m 52s

> Later Phase 6/provider fixes were supplied as in-flight facts, not treated as
> merged production capabilities. Revalidate every recommendation against the
> integration SHA before implementation.

---

Worked for 29m 52s结论与状态基线 本轮只讨论性能，不重复上一轮完整 bug 审阅。 我将状态分成两层： 冻结源码事实：仍以证据包中的 c7a912dd… 为基线；上一轮已按 MANIFEST.tsv 完整校验 476/476 个条目。 冻结后在途事实：采用你本轮提供的 Phase 6、MFLA/BFLA、PureKLU/QDLDL 和 benchmark 状态，但不把尚未集成的 prototype 当成 production capability。 因此，本轮不再把“Phase 6 sparse path 的全局 m×m operator/inverse”列为待办；重点转到冻结树仍能看到、且你给出的在途修复没有明确覆盖的四类热点： sparse HSD setup 中的 Matrix(A)、dense RRQR 和可能的 dense row-space basis，见 src/hsd/hsd.jl:200–321, 345–377； Schur/expanded assembly 按列重复遍历全部锥块、重复 scatter/full-vector apply，见 src/hsd/product_cone_hsd.jl:635–693、src/hsd/predictor_corrector.jl:91–180； 同一 factor epoch 内重复执行 factor reconstruction、transform proof 和多层 residual replay，见 src/hsd/product_cone_hsd.jl:880–980, 1906–2107, 2395–2565； expanded session 同时长期持有多套 d×d 矩阵、临时 RHS 和方向对象，见 src/kkt/expanded_quasidefinite.jl:307–433。 所有建议保持： 一个 HSD+一个 NewtonSystem+原坐标 certificate 权威​ 不同 route 只能是同一 NewtonSystem 的不同装配、消元和 factorization 实现。 下面的收益范围均是目标阶段耗时降幅，不是未经测量的端到端加速承诺。若某阶段占端到端时间比例为 si​，该工作的端到端贡献约为： ΔTE2E​/TE2E​≈si​×ri​, 其中 ri​ 是该阶段实际测得的降幅。 A. 前 10 个性能工作包 排序表 排名工作包目标阶段收益风险净开发成本实现归属1block-aware KKT/Schur assemblyassembly 阶段目标下降 30%–75%；PSD/混合锥收益最高中高8–15 人日SDPX 负责 block plan、布局、调度；MFLA/BFLA 仅提供 GEMM/SYRK/TRSM2factor-epoch proof receipt，删除重复 O(d3) factor replayfactor proof/revalidation 阶段目标下降 60%–95%中4–7 人日SDPX；provider 只返回原始 factor diagnostics3sparse-preserving equality/HSD setupsparse cold setup 时间目标下降 40%–90%；峰值内存目标下降 60%–99%高8–15 人日SDPX 控制流；稀疏 rank/symbolic 能力由 SuiteSparse/PureKLU 等 provider 提供4真实 symbolic/numeric reuse 与 factorization_reuse receipt重复迭代的 analyze/setup 时间目标下降 20%–70%；避免无效 numeric refactor中高SDPX 5–8 人日；provider 5–10 人日SDPX 管 epoch/pattern；PureKLU/QDLDL/provider 管 symbolic/numeric factor5PSD congruence panelization 与 BLAS-3 assemblyPSD scaling/assembly 阶段目标下降 30%–80%中高8–14 人日SDPX 负责 panel/prepack；MFLA/BFLA 负责高性能矩阵核6expanded workspace 压缩、预分配和 in-place recoveryroute 峰值 RSS 目标下降 25%–60%；setup/copy 时间下降 10%–35%中5–9 人日SDPX；provider 拥有其破坏性 factor scratch7multi-RHS factor reuse 与 refinement panelsolve/refine 阶段目标下降 10%–35%；迭代分配下降 50%–95%中4–8 人日SDPX 负责 RHS 生命周期；MFLA/BFLA/PureKLU 实现 solve_multi!8经验校准的 route planner 与 RSS preflight不以单一加速率衡量；目标是避免错误 route、OOM 和高精度无效 densify中6–10 人日SDPX；provider 必须暴露 capability、workspace/fill 估计9fixed-dimension local kernels：fixq3、Exp/Power 3×3对目标模型的局部 assembly/scaling 阶段下降 15%–60%中5–9 人日SDPX；不得生成独立 solver/HSD10融合原始 residual reduction 与确定性线程预算residual/certificate 阶段目标下降 20%–60%；并行收益取决于 block 数量中高6–11 人日SDPX；provider 只暴露线程模型和原始 diagnostics 明确的仓库边界 必须在 SDPX 完成 一个 NewtonSystem 的 block incidence 和 assembly plan； factor epoch、matrix epoch、pattern signature、attempt receipt； predictor/corrector/refinement 的 RHS 生命周期； route planner、memory preflight、fallback provenance； fixq3、Exp/Power、PSD 的数学布局与数据编排； 原始五方程 residual 和原坐标 certificate； deterministic reduction 和全局线程预算。 只能由 provider 完成 MFLA/BFLA 的 LU/LDL、GEMM、SYRK、TRSM、multi-RHS、transpose solve； PureKLU 的 CSC validation、symbolic/numeric factor、zero-update pattern 语义、alias-safe multi-RHS； QDLDL 的 symbolic/numeric state、stale-state 清理和 companion inertia； provider 内部的 factor cache 所有权和破坏性 scratch。 SDPX 不应复制 MFLA/BFLA 的矩阵内核，也不应为高精度 sparse LU 重新写第二套隐藏实现。 B. 四类数值精度的推荐 route 1. Float64 dense 项目推荐候选 route由校准模型比较 dense reduced/bordered 与 dense expanded；不能仅按 n 或 m 固定切换factorization精确 operator 若确实对称且合同条件成立，可用 pivoted LDLᵀ；否则使用 pivoted LU。不能为了使用 LDLᵀ 改写数学 operatorproviderStandardLA/LAPACK；具体 kernel 由 provider 层负责cache每个 numeric matrix epoch 一次 factor；predictor、corrector、同 epoch refinement 共用refinement在原始未扰动 NewtonSystem 上计算 backward error；达到现有门限即停止；若 residual 不再收缩则显式判定本 route 失败fallbackplanner 预先列出且内存可行的 attempt chain；每次 attempt 均记录，不能 catch 后静默换 route 推荐策略不是“永远 dense Schur”。当 reduced condensation 引入明显条件放大，expanded LU 可能比更小但更坏条件的 Schur 更稳、更快。 2. Float64 sparse 项目推荐首选 exact solveUMFPACK 或经过合同测试的 sparse nonsymmetric LUsymbolicpattern 确定后只做一次；ordering、elimination tree、factor capacity 与 pattern signature 绑定numeric每个 matrix epoch refill + numeric factor；不得因数值零变化重建 symbolicQDLDL当前仅用于 symmetric companion inertia/诊断；在没有严格合同等价证明前，不应用其结果替代 exact Newton directioncachesymbolic_epoch、numeric_epoch、factor_epoch 分开；必须报告真实 reuse hit/missrefinementsparse exact operator 上 solve，随后在原始五方程上验收；refinement RHS 可使用同一 numeric factorattempt chainsparse exact → dense expanded → bordered 只能在各 route 已通过 memory preflight 时显式执行fail closedpattern drift、rank reduction 要求 dense basis、provider 不支持类型、峰值内存不可证明时直接拒绝对应 route 特别需要处理冻结源码中的： juliaAf = Matrix{T}(A) qr(Matrix(transpose(Af)), ColumnNorm()) 对于 sparse full-rank 常见情况，应保留原变量坐标或使用稀疏/隐式 rank 处理；不能先 densify，再宣称后续 factor 是 sparse。 对 rank-deficient sparse A，推荐顺序是： provider 能提供 implicit sparse QR/Q-operator：使用隐式表示； 否则改走原坐标 sparse expanded NewtonSystem； 两者都不可行且 dense route 超内存：fail closed。 不应物化 dense null-space basis。 3. Float64x2 / Float64x4 项目推荐当前 production routedense MFLAsparse route仅在 PureKLU 的 CSC、stale state、zero-update、multi-RHS alias 问题全部关闭并通过 SDPX 原始 residual gate 后启用factorizationexact nonsymmetric operator 用 MFLA LU；对称 operator 只有在严格成立时才用 LDLᵀcacheMFLA 已验证的 factor cache 由 provider 拥有；SDPX 只管理 matrix/factor epochRHSsolve_multi! 处理同一 factor 下的独立 RHS；不得把有数据依赖的 predictor 和 corrector 强行同时求解refinement全程 Float64x2 或 Float64x4；residual、correction 和 norm accumulation 均不得退回 Float64escalationx2→x4 或 x4→BigFloat 必须作为显式新 attempt，从源数据重新构造，不可复用低精度 factor，也不可隐藏发生fail closedPureKLU 不可用且 dense MFLA 的 memory UCB 超过可用 RSS 时，拒绝求解，而不是 downcast 4. BigFloat256 项目推荐当前 production route中等规模优先 dense BFLAsparse routePureKLU 完成合同修复和集成后，作为 BigFloat exact nonsymmetric sparse LUQDLDLcompanion inertia/诊断，不单独承担物理解precisionsetup 前冻结 256-bit；并行区域内禁止修改默认 precision/roundingcacheBFLA/PureKLU 拥有 factor storage；SDPX epoch 变更时显式 invalidaterefinementBigFloat256 原类型 residual 和 correction；记录每步 residual contraction、MPFR allocation 与 RSSmemorydense route 的 peak memory 必须使用 provider 实测 upper bound，而不能只按 sizeof(BigFloat) 粗估fail closedBFLA 缺失、PureKLU 尚未 production、dense peak RSS 超限时诚实退出；不得用 Float64 临时代替 BigFloat 的瓶颈通常首先是对象数量、matrix copy、factor replay 和无效 workspace，而不是单个 MPFR 运算。因此应先删除架构性物化，再考虑增加线程。 C. 具体优化顺序 C0. 先建立不掺 JIT 的分阶段基线 在任何 kernel 修改前，必须能单独记录： Tsetup​,Tassembly​,Tfactor​,Tpred​,Tcorr​,Trefine​,Tcertificate​. 否则很容易把“assembly 变快但 factor 次数增加”误判为优化。 C1. factor epoch 级 proof receipt 当前 bordered 路径存在同一 factor 被多次重建/验证的风险。优化方式不是删除证明，而是把证明提升到 factor epoch： juliaFactorReceipt( matrix_epoch, factor_epoch, pattern_signature, provider_id, precision, regularization, factor_backward_bound, transform_proof, factor_status, ) 每次 factor 只执行一次昂贵证明。每个 RHS 只做： finite/epoch/signature 检查； provider solve； RHS 对应的 factor-coordinate residual； 原 bordered/expanded operator residual； 原始五方程 residual。 任何 matrix、scale、regularization、factor 或 pattern epoch 变化都使 receipt 失效。 目标 gate： factor reconstruction count = numeric factorization count； 不能随 predictor/corrector/refinement RHS 数量增长； 原始 residual 与现有实现等价或更小。 C2. 冻结 BlockIncidencePlan setup 时一次性生成： block row range active variable columns J_b A block CSC positions Schur output tile positions PSD panel descriptors fixed-size cone descriptors pattern signature 对锥块 b，令 Ab​=A[Rb​,Jb​]，局部 scaling/operator 为 Gb​。Schur contribution 应直接写为： S[Jb​,Jb​]+=Ab⊤​Gb​Ab​, border contribution 类似为： g[Jb​]+=Ab⊤​Gb​qb​. 具体正负号只由统一 NewtonSystem 提供，不能在每个 route/kernel 中重新编码。 这一步消除： 每个 Schur 列重新扫描全部锥块； full m-vector 的 zero/scatter/apply； 结构信息在每次迭代重新发现； pattern drift 被误当成 ordinary numeric update。 C3. 先做 fixed-size block direct assembly orthant / SOC 直接形成局部 Ab⊤​Gb​Ab​，小块采用 unrolled 或小 panel，不先构造全局 cone operator。 Exp/Power 3×3 每个 scaling epoch 冻结： 3×3 scaling/factor； determinant、pivot 和局部证明信息； block-local scratch。 随后以 SoA 或连续 block array 批量处理： ub​=Gb​ab​,Sb​=Ab⊤​ub​. 不得为每个 3×3 block 调用 generic dense factor API，也不得把所有 3×3 block 拼成全局 dense inverse。 当前 Exp 2、Power 1 个 reproducible FINDING 在 correctness 关闭前，只能用于 falsification，不能进入性能排行榜。 C4. PSD congruence panelization 冻结源码中 PSD scaling workspace 持有多块 dense 矩阵，且 _psd_congruence_svec! 倾向于逐 svec unpack、GEMM、pack。推荐改为： setup 时预打包当前 PSD block 的 active coefficient matrices； 每次 scaling epoch 只更新 congruence factor； 将一组 active columns 组成 panel； 用 MFLA/BFLA/BLAS 的 GEMM/SYRK/TRSM 批量变换； 直接写 Schur tile，不生成全局 cone operator。 对 PSD block Xj​，不要反复执行： svec−1→WXj​W⊤→svec. 应以 panel 形式处理多个 Xj​。SDPX 负责 panel layout，矩阵核必须留在 MFLA/BFLA。 优先 profile： unpack/pack 时间； congruence GEMM 时间； Schur SYRK 时间； panel 临时字节； 每 scaling epoch 的 panel rebuild 次数。 C5. symbolic once，numeric refill Phase 6 已有 pattern-signature 和 pattern drift 校验，下一步应把它落实为明确生命周期： structure epoch: build BlockIncidencePlan build CSC slots analyze ordering allocate factor capacity numeric epoch: refill existing nzval numeric factor solve all RHS 必须区分： symbolic analysis reuse； numeric matrix reuse； factor reuse； solve reuse。 factorization_reuse=true 只有在本次 solve 确实使用了同一 numeric factor 时才能写入，不能由“缓存对象存在”推断。 C6. multi-RHS 的正确边界 predictor 和 corrector 不能作为同一个同时 RHS panel，因为 corrector RHS 依赖 predictor 方向。 可以共享的是： 同一 scaling/matrix epoch 的 factor； bordered formulation 中彼此独立的固定 RHS； 同一 refinement stage 下的多个独立 RHS； batch benchmark 或多方向诊断； provider 内部 triangular solve 的多列布局。 正确次序是： factor once solve predictor RHS construct corrector RHS from predictor result solve corrector RHS with same factor compute residual solve refinement correction with same factor RHS、solution、direction、cone action 和 residual buffers 均应预分配；recover_expanded_direction 不应在每次 RHS 创建新向量。 C7. fixq3 只作为 NewtonSystem 的局部贡献 fixq3 应迁移为类似： juliaassemble_fixed_trace_q3_contribution!( newton_system, block_plan, scaling_epoch, ) 要求： 使用已有 Lorentz-3/2×2 局部公式； 直接贡献到统一 KKT/Schur； 不生成另一套 solver； 不拥有独立 termination/certificate； 不改变 HSD 状态机； 与通用 SOC 参考实现做逐元素和方向残差对照。 C8. 融合原始 residual reduction 当前 residual、route validation、refinement 和 certificate 可能重复计算： AΔx,A⊤Δy, 以及相同的 block cone action。 推荐每个 candidate direction 只形成一次： A_dx At_dy cone_linear_action HSD scalar residual terms block norm partials 然后同时供： Newton 五方程 residual； refinement correction； route acceptance； terminal original-coordinate certificate。 权威 residual 必须使用固定 reduction tree 和原数值类型。不得用 provider 的内部低精度 norm 替代。 C9. 最后再加线程 线程化应建立在串行 block plan、reuse 和 allocation-free kernel 已稳定之后。否则线程只会并行化重复工作或放大内存峰值。 D. Route planner 的量化设计 1. planner 输入特征 不能只使用 n,m。至少需要： x=(n,m,r,dexpanded​,dreduced​,nnz(A),ρA​,{Jb​},qmaxPSD​,nnz(K),nnz(L+U),p,κ^,RSS0​,Mlimit​). 其中： r：实际 equality rank； Jb​：每个 cone block 的 active variable set； p：precision bits； κ^：route-specific condition/condensation estimate； nnz(L+U)：symbolic provider 的 factor fill estimate。 定义 fill ratio： ϕ=nnz(K)nnz(L+U)​. 但 ϕ 不应有统一固定阈值；它进入时间和内存模型。 2. 内存可行性门 对每个 route r，provider 或 benchmark 模型给出： MrU​=Mmatrix​+Msymbolic​+Mnumeric factorU​+Msolve scratchU​+Mcertificate​+Mthread scratch​. 可用 headroom： Hmem​=Mjob/cgroup limit​−RSScurrent​−Rcalibrated​. 其中 Rcalibrated​ 不是拍脑袋常数，而是从历史运行中拟合： Rcalibrated​=Qhigh​[peakRSS−RSS0​−Mroute​], 并按： provider； 数值类型； thread mode； factorization； 机器/BLAS 版本 分别保存。 route 只有在： MrU​≤Hmem​ 时才有资格尝试。使用 peak RSS upper bound，而不是最终 factor storage。 3. 数值可达性门 令 uT​ 为 provider 报告的 unit roundoff。对 route 的 condensation/scaling/factorization，拟合： η​rU​=fr​(uT​,κ^map​,pivot growth,scaling spread,regularization,ϕ), 其中 η 是原始 NewtonSystem 的 backward error。 要求： η​rU​≤ηrequired​, 这里的 ηrequired​ 直接取现有未放宽的验收门限。 需要注意：高条件数本身不应成为机械拒绝理由，因为稳定 LU 仍可能有小 backward error。planner 使用的是“该 route 经 condensation、缩放和 refinement 后达到原始 residual 的经验上界”，最终接受仍由实际原始 residual 决定。 可以从下式导出 route-specific condition envelope，而不是写死 cond < 1e12： κ^max,r​=fr−1​(ηrequired​;uT​,…). 4. 时间模型 分开预测： Tr​=Tsetup​+Niter​(Tassembly​+Tfactor​+Tsolve/refine​)+Tcertificate​. 主要特征包括： dense factor flop estimate； sparse symbolic nnz(L+U) 和 frontal size； PSD panel dimensions； RHS 数量； precision bits； MPFR limb 数； thread mode； 预期 numeric factor 次数。 不能把一次小矩阵 microbenchmark 的 GFLOP/s 外推到所有规模。 5. route 选择规则 sparse route 有资格的条件 exact provider 支持当前数值类型和 transpose/multi-RHS； pattern signature 稳定； rank/equality 处理不会 densify； sparse memory upper bound 可行； predicted original residual 可达； symbolic analysis 没有发现灾难性 fill； 当前实例位于该 provider 的校准包络内。 dense MFLA/BFLA 有资格的条件 provider 原生支持该数值类型； dense peak bytes upper bound 可行； route-specific residual 模型可达； 不需要 downcast； 当前维度和 block layout 位于校准包络内。 sparse 与 dense 都可行 选择预测总时间最小者，但同时考虑预测区间。若两个 route 的时间置信区间重叠，应优先选择： 历史 certificate failure rate 更低； memory margin 更大； refinement/fallback 次数更少； provenance 更完整的 route。 必须 fail closed 的条件 没有支持当前类型的 exact provider； dense memory upper bound 超限，sparse provider 尚未认证； rank-deficient sparse reduction 只能生成 dense basis； pattern drift 后无法安全重新 analyze； provider state/epoch 不可证明； 所有可行 route 的 residual upper bound 均不能达到现有门限； route 位于校准范围之外且没有保守的可证明内存界； 可选 BFLA/PureKLU 缺失。 6. 阈值如何校准 不要在源码中写： n > 1000 => sparse density < 0.05 => sparse bits > 128 => BFLA 真正阈值是模型交点： Tsparse​(x)=Tdense​(x), 并同时满足 memory 和 certificate eligibility。 校准数据集必须覆盖： n/m 比； block 数与 block size 分布； 最大 PSD block； nnz(A) 与每个 block 的 active-column overlap； fill 从低到高； well-conditioned 到 ill-conditioned； Float64、x2、x4、BigFloat256； 1 线程与候选线程模式； 不同 provider。 按 CPU、BLAS、provider 版本、precision type 建立版本化模型，并对未参与训练的问题族做 holdout 验证。 planner 的核心验收指标是 route regret： R=Tbest feasible​Tchosen​−Tbest feasible​​, 以及： OOM misroute 数必须为零； silent downcast 数必须为零； hidden fallback 数必须为零； certificate regression 数必须为零； 所选 route 与最优 route 在统计区间内是否可区分。 在数据不足的硬件或 provider 上，只做 capability + memory + certificate 可行性选择，不宣称已有可靠性能阈值。 E. 线程模型 1. 单一 ThreadBudget 建议 planner 输出： juliaThreadBudget( julia_outer_threads, blas_threads, provider_threads, reduction_bins, mode, ) 基本约束： outer Julia block parallel 开启时，BLAS/provider threads 设为 1； 大 PSD panel 交给多线程 GEMM/SYRK 时，outer assembly 串行； sparse factor 由 provider 控制时，不在 factor 外再开 Julia tasks； 同一时刻只允许一个主要并行层。 2. 可并行部分 阶段推荐并行方式独立 cone scaling按 block 分配，workspace 独占orthant/SOC/Exp/Power assemblyouter Julia block parallel，provider threads=1多个中小 PSD blockblock parallel，每个 block 的 BLAS 单线程单个大 PSD blockouter 串行，MFLA/BFLA/BLAS 负责 panel 内线程Schur output按输出 tile 唯一 owner，避免多个 task 同时累加line-search block boundsblock parallel + 固定顺序 reductionresidual partials固定 virtual bins，最后按固定树归并certificate cone membershipblock-local 并行，权威 norm 固定顺序归并 不能使用原子加法把多个 cone block 直接累加到同一 Schur tile；这既影响性能，也破坏确定性。应使用： 输出 tile 唯一所有者；或 私有 partial tile + 固定顺序归并。 3. 不应并行的部分 HSD 状态更新； factor/matrix epoch 变更； route/fallback 决策； predictor→corrector 依赖链； factor cache invalidate； terminal status promotion； 原始 certificate 的最终判决逻辑。 4. BigFloat/MPFR BigFloat 并行必须遵守： setup 前冻结 precision； 并行区内不调用 setprecision 或改变 rounding； 每个 task 使用独占 BigFloat 对象和 scratch； factor object 不跨 task 并发写； provider 未声明 thread-safe 前，BigFloat factorization 保持串行； 单独记录 MPFR allocations、limb bytes 和 allocator contention； 只有在 coarse-grained block workload 足够大时才启用 outer threading。 BigFloat 小块大量 task 化通常会被 task 调度、MPFR allocation 和 cache miss 抵消。 5. 确定性 reduction 权威 residual/certificate 使用固定 virtual bins： setup 时按工作量确定固定 block→bin 映射； task 完成顺序不影响 bin 内顺序； bin 按固定二叉树或固定线性顺序归并； 不使用 @fastmath； 记录 requested/effective Julia、BLAS 和 provider thread 数。 理想状态下，1/2/4/8 线程得到相同 status、route、iteration count 和 certificate；浮点 residual 至少应满足固定的可解释重现合同，而不能随 task 调度随机漂移。 F. Benchmark 方法 1. 分离 JIT、cold setup 和 warm iteration 每个 benchmark 至少拆成： 进程/JIT Julia 启动； package load； method compilation。 这部分单独记录，不混入 solver kernel。 cold numeric setup canonicalization； equality/rank analysis； BlockIncidencePlan； sparse symbolic analysis； provider workspace； factor capacity； PSD panel prepack； route planner。 warm iteration scaling update； KKT/Schur assembly； numeric factor； predictor solve； corrector RHS construction； corrector solve； 每次 refinement； line search； state update。 termination/certificate original-coordinate primal residual； dual residual； gap； cone membership； infeasibility/unbounded certificate； route/factor receipt closure。 2. 每次运行必须记录 结构指标 n,m,r； nnz(A)； cone block histogram； 最大 PSD block； 每块 active-column 数； nnz(K) / nnz(S)； predicted 和 actual nnz(L+U)； fill ratio； symbolic frontal/column count； expanded/reduced dimension。 时间指标 setup； rank/equality reduction； symbolic； assembly； numeric factor； predictor solve； corrector solve； refinement 每一步； line search； certificate； route fallback/attempt time。 计数指标 symbolic analysis count； numeric factorization count； factor receipt build count； factor reuse hit/miss； RHS 数量与 panel width； refinement 次数； fallback attempt chain； pattern drift/reanalysis 数； PSD panel rebuild 数； factor reconstruction/replay 数。 内存指标 每阶段 allocations count/bytes； provider factor bytes； symbolic bytes； thread scratch； MPFR allocations/estimated limb bytes； current RSS； peak RSS； planner predicted peak RSS； prediction error。 数值指标 五个原始 Newton residual 分量； normalized backward error； factor-coordinate residual； regularization 大小； pivot growth/rcond/inertia diagnostics； terminal primal/dual/gap/cone residual； status； iteration count； planned/executed route。 3. small / medium / large 分层 不建议按统一固定 n 划分，而按资源转折点划分。 Small working set 仍处于 LLC 或接近 LLC； 所有 provider/precision 可在标准 CI 中运行； 用于 allocation、alias、epoch、exact parity 和 certificate gate； 当前 10 个 certificate PASS 可作为起点。 Medium working set 已跨越 LLC； 覆盖 sparse/dense 实际 crossover； assembly、factor、solve 中至少一个成为明确主导阶段； 用于 planner 校准和主要性能回归。 Large 接近调度器/集群允许的 memory envelope，但保留校准 headroom； 必须包含 sparse fill 压力、多个大 PSD block 和高精度 factor； fresh process； 主要验证 peak RSS、fill、factor reuse、capacity 与端到端时间； 不放进普通 PR quick CI。 分层边界由实际 cache break、route crossover 和 memory envelope测得，而不是源码常数。 4. 统计方法 同一 commit/provider/type 使用 fresh process 重复； 至少满足现有计划中的多进程重复，校准阶段继续运行到 route 排名的置信区间稳定； 报告 median、MAD/置信区间，而不是最小值； 固定 CPU affinity、BLAS/provider thread mode； 记录 CPU、Julia、BLAS、MFLA/BFLA/PureKLU/QDLDL 版本； 优化前后必须具有相同 status、route policy、certificate 门限和原始 residual 语义。 当前 6 个 FINDING： EXP 2； SDP 3； Power 1； 应保留为 correctness/falsification case，但在修复为 certificate PASS 前，不进入“哪个版本更快”的性能 aggregate。否则可能奖励更快地得到错误结果。 G. 12 个原子提交计划 以下 owned files 是建议的独占修改边界；Phase 6 分支文件名若已调整，应保持相同职责边界。凡涉及 src/hsd/product_cone_hsd.jl 的提交必须串行合并，符合一个 HSD writer 的约束。 ID原子提交Owned files预期收益范围必须新增的 falsifying benchmark/test回滚边界并行与依赖P0perf: phase-separated benchmark and receipt schemasrc/performance_trace.jl、src/pipeline/timing.jl、benchmark/result_schema.jl、fresh-process runner直接性能收益 0；为后续提供可信归因phase 总和与 wall time 一致性；缺失数据不得写成 0；fallback 每个 attempt 都有 timing全部为 additive instrumentation，可整提交回滚首先执行；其他提交依赖其测量P1kkt: factor-epoch proof receipt and truthful reuse metadata新建 src/kkt/factor_receipt.jl、src/hsd/product_cone_hsd.jl、相关 testsproof/revalidation 阶段 60%–95%；factor reconstruction 次数降为每 factor epoch 一次修改 matrix/factor/scale epoch 后 receipt 必须失效；predictor/corrector/refine 后原 residual 不变；故障注入验证 reuse=falsereceipt 逻辑可独立回滚；不改变 Newton 方程可与 P2、provider 修复并行；HSD 文件与 P4/P7 串行P2hsd: sparse-preserving equality setupsrc/hsd/hsd.jl、新建 src/hsd/equality_reduction.jl、src/la/sparse.jlsparse setup 时间 40%–90%；RSS 60%–99%，视原 densify 比例大 sparse full-rank case 中禁止 Matrix(A)；rank-deficient 且无 implicit provider 时必须显式 expanded/fail-closed；证书与 dense reference 一致新 equality reduction mode 可整提交回滚；旧 dense RRQR 仅保留显式 dense route可与 P1、P3、provider 修复并行P3kkt: immutable BlockIncidencePlan and numeric slots新建 src/kkt/block_incidence.jl、src/kkt/system.jl、Phase6 reduced-Schur pattern filesassembly 10%–35%；symbolic/setup 20%–60%overlapping/gapped block patterns；structural zero→nonzero drift；numeric zero update不得重做 symbolic；逐元素与参考 assembly 对照plan 未启用时可回滚到参考 kernel；不得作为 hidden runtime fallback依赖 P0；P4/P5/P8/P11 依赖P4kkt: direct fixed-dimension contributions for fixq3 and Exp/Power新建 src/kkt/specializations/fixed_trace_q3.jl、src/soc_native.jl、src/cones/nonsymmetric/scaling3.jl、有限范围 product_cone_hsd.jlQ3/Exp/Power 局部 scaling/assembly 15%–60%通用 SOC 与 fixq3 逐元素、方向和五方程 parity；3×3 alias、epoch、near-boundary case；现有 3 个 FINDING 不得被隐藏specialization 可整体删除，通用 reference path保留为显式测试路径依赖 P3；与 P5 可并行；HSD merge 与 P1/P7 串行P5psd: panelized congruence and Schur tilessrc/cones/symmetric/psd.jl、src/cones/symmetric/types.jl、新建 src/kkt/psd_panels.jlPSD scaling/assembly 30%–80%scalar-column reference 对照；随机 PSD block；x2/x4/BigFloat 无 downcast；1/多线程一致；panel alias testpanel kernel 可回滚，保留显式 reference implementation 供 tests依赖 P3；可与 P4、provider 工作并行P6kkt: allocation-free RHS panels and compact expanded workspacesrc/hsd/predictor_corrector.jl、src/kkt/expanded_quasidefinite.jl、src/kkt/system.jlsolve/refine 10%–35%；迭代 allocation 50%–95%；expanded RSS 15%–45%warm iteration allocation gate；predictor/corrector 依赖测试；multi-RHS alias；provider destructive factor 行为；original residual parityworkspace 布局和 in-place recovery 可整提交回滚依赖 P1、P3；与 P5 可部分并行，最终 merge 串行P7perf: fused original residual reductions and deterministic ThreadBudgetsrc/kkt/system.jl、src/hsd/product_cone_hsd.jl、src/kernels/threaded.jlresidual/certificate 20%–60%；并行收益由 block 分布决定1/2/4/8 线程 status、iterations、attempt chain 一致；固定 reduction；无 oversubscription；A·dx/Aᵀ·dy 计数各一次可关闭 ThreadBudget 并回滚融合 reduction；旧权威 residual 不删除直到 parity 完成依赖 P4、P5、P6；HSD writer 串行P8planner: calibrated route, fill, condition and RSS modelsrc/midend/formulation_planner.jl、src/pipeline/workspace_estimate.jl、src/performance_trace.jl、benchmark/calibration/*目标为 route regret 降低、OOM misroute=0；不预设固定加速率holdout family；模拟 cgroup memory cap；未知 provider/type fail-closed；预测/实际 RSS、fill、time 对照；不能出现 downcastplanner model/version 可独立回滚到 capability-only conservative policy依赖 P0、P2、P3；provider capability 可并行开发P9PureKLU: harden CSC, pattern epoch and alias-safe multi-RHSPureKLU 仓库：CSC input、symbolic/numeric state、solve_multi tests对合格高精度 sparse case，factor/setup 阶段目标下降 20%–70%；主要收益可能是容量malformed CSC；stale factor；zero-update pattern；pattern drift；multi-RHS self-alias；transpose；BigFloat/x2/x4 residual；无 Float64 conversion完整 provider 版本可独立回滚；不影响 SDPX dense route与 P0–P8 并行；P11 依赖P10QDLDL: harden symbolic state and companion inertia receiptQDLDL 仓库：pattern state、numeric update、inertia receipt、testscompanion analysis 阶段目标下降 20%–60%；端到端通常次于 exact solvestale state；zero update；indefinite/pivot edge；x2/x4/BigFloat；inertia 与 dense reference；不得把 companion 解当物理解provider 版本独立回滚与 PureKLU/SDPX 并行；P11 依赖P11providers: integrate exact sparse attempt chain and capability receiptsSDPX ext/*、src/la_backend.jl、Phase6 attempt/factor session files合格高精度 sparse workload 可避免 dense route；目标收益 10%–50% 或解决 dense 不可容纳问题sparse→expanded→bordered 强制故障链；planned/executed metadata；actual reuse；provider 缺失 honest-skip；原始五方程 gate；无 hidden fallbackoptional extensions 可整提交移除；dense MFLA/BFLA 保持不变依赖 P1、P3、P8、P9、P10；最后集成 推荐并行波次： W0W1W2W3W4W5​:P0:{P1, P2, P3, P9, P10}:{P4, P5, P8}:P6:P7:P11​ 其中 provider 仓库可独立并行；SDPX 中触及 HSD 主文件的 P1、P4、P7 必须串行落地，避免多 writer 同时改核心状态机。 最先做的三个优化 第一：factor-epoch proof receipt 这是最可能以较低风险获得明显端到端收益的工作。 冻结源码中 factor reconstruction、transform validation、factor-coordinate residual 和 original residual 在同一 factor 的多个 RHS/ refinement 中可能重复。首先应验证： factor_proof_seconds / total_seconds； factor reconstruction count； numeric factor count； predictor/corrector/refinement RHS 数； 每 factor epoch 的 proof build 数； 删除重复 replay 后原始五方程 backward error； status、iteration、attempt chain 是否完全一致。 判定成功的核心不是某个倍数，而是： Nproof−build​=Nnumeric−factor​, 且 proof 时间不再随 RHS 数近似线性增长。 第二：block-aware KKT/Schur assembly，随后立即做 PSD panelization 这是最可能成为大 SDP 和混合锥模型的主收益来源。 重点 profile： assembly_seconds / iteration_seconds； 每 Schur 列扫描的 cone block 数； full-vector zero/scatter 次数； cone operator apply 次数； PSD svec unpack/pack 次数； GEMM/SYRK/TRSM 时间； panel width； assembly allocations； nnz(S)、active-column overlap； assembly 后原始五方程 residual； iteration count 和 factor count。 需要证明的是：减少了重复 block traversal 和数据搬运，而不是通过改动 Newton 方程或跳过 scaling/certificate 获得表面提速。 第三：sparse-preserving HSD setup + 真实 symbolic/numeric reuse 这是最可能扩大可求解规模、并改善高精度 sparse 端到端表现的工作。 重点 profile： cold equality/rank setup 时间； Matrix(A) 或 dense null-basis 分配次数，目标为 sparse route 上零； current/peak RSS； RSS 对 nnz(A)、nnz(K)、nnz(L+U) 的增长关系； symbolic analysis count，目标通常为每 pattern epoch 一次； numeric factor count； actual factorization reuse hit/miss； fill ratio； sparse/dense route prediction与实际时间； sparse→expanded→bordered attempt chain； PureKLU 原始 backward error； BigFloat/x2/x4 是否出现任何低精度对象或转换。 这三项完成后，才适合判断后续最高收益究竟来自： expanded workspace 压缩； multi-RHS refinement； fixq3； Exp/Power 3×3； residual threading。 在 profile 尚未给出各阶段占比之前，不应承诺固定加速倍数；但从冻结源码的数据流和你提供的 Phase 6 状态看，这三项最有可能分别消除重复立方工作、重复全块装配和 sparse setup densification/reanalysis，因此应优先进入下一轮原子提交。
