# GPT Pro SDPX Bug and Kernel Review — 2026-08-28

- Conversation: https://chatgpt.com/c/6a912f20-707c-83ec-946b-7d4a6ed680cc
- Frozen source reviewed: `c7a912dd91e12a25bd4b90b3eca2c2283ee41dd5`
- Evidence package: `SDPX_GPTPRO_BUG_KERNEL_REVIEW_20260828_c7a912dd.zip`
- Manifest: 476 entries, all size/SHA-256 verified by GPT Pro
- Verdict: **NEEDS_REWORK**

> Scope caveat: the review intentionally treated unmerged Phase 6 candidate
> `24c3289` only as an attachment. Later branch `62bd56e` already addresses
> several I1/B2 findings and is undergoing an updated review gate.

---

Worked for 83m 46s审阅结论 Verdict：NEEDS_REWORK。 证据包已完整解压并按 MANIFEST.tsv 校验：476 个清单条目的文件大小与 SHA-256 全部匹配，没有缺失或被替换的源码证据。我依次读取了 README_FIRST.md、USER_PROMPT.md、MANIFEST.tsv、docs/PLAN.md，并审阅了包内状态、既有 review 和候选修复附件。 审阅时严格区分了两棵代码： 当前冻结源码：c7a912dd…，这是本次结论的唯一源码事实来源。 附件候选：24c3289…，尚未合并，只作为修复思路和历史审阅证据，不能用来证明当前缺陷已经解决。 当前最严重的问题不是容差，而是以下三者没有形成统一、可审计的事实链： 实际求解路径=结果中报告的路径=证书所依赖的路径. 因此，当前冻结版本不应被标记为 production-certified，也不应通过放宽残差、可行性或锥成员容差来“修复”。 一、阻塞级问题 B1. 证书与锥成员验证对 NaN/Inf fail-open 这是一个可以直接触发的真实 bug。 部分正交锥、自由锥、零锥及证书残差检查采用“只有违反容差时才拒绝”的写法，例如逻辑上类似： juliaif x < -tol return false end 或： juliaif residual > tol return false end IEEE 浮点语义下： juliaNaN < -tol == false NaN > tol == false 因此，若没有先执行全局有限性检查，NaN 会绕过这些拒绝分支。自由锥虽然不限制符号，但数值证书仍必须是有限向量；正无穷也不能作为有限维锥证书中的合法数值。 影响 非有限 primal/dual 向量可能进入内部“valid/certified”路径。 残差范数、目标值、缩放量或证书射线一旦被污染，后续比较不能再代表数学判断。 即使当前默认未开放最终 certification，这也是启用任何 production certificate 前必须解决的阻塞问题。 必须修复 先统一执行： juliaall(isfinite, x) all(isfinite, s) all(isfinite, y) isfinite(τ) isfinite(κ) 以及所有目标值、残差、归一化尺度和证书分母的有限性检查，然后才允许进入锥成员和容差判断。 每一种锥块均应加入 NaN、+Inf、-Inf 注入测试。不需要也不允许修改现有容差。 B2. expanded → bordered fallback 的执行事实与公开元数据不一致 当前调用链允许 expanded KKT 路径失败后切换到 bordered 路径，但结果对象中的部分诊断信息仍来自预先选定的 execution plan，而不是最终成功产生 Newton 方向的实际 attempt。 因此可能出现： 实际执行：expanded 失败，bordered 成功； 对外报告：expanded/dense； 证书或状态判断：仍根据预选路径或 provider 属性进行。 这不是日志显示问题，而是 provenance bug。 影响 用户看到的 KKT formulation/provider 可能不是实际执行路径。 无法审计正则化、fallback、refinement 与最终方向之间的关系。 benchmark 和故障统计会把 bordered 成本、失败原因归到 expanded。 证书可能引用错误的 provider、矩阵形式或 regularization 状态。 隐藏 fallback 会掩盖系统性 KKT 失败，并导致后续性能优化针对错误瓶颈。 必须修复 每次 Newton 求解必须返回不可变的 solve receipt，至少记录： requested formulation； attempt 顺序； 每个 attempt 的 operator/formulation； 实际 provider 与数值精度； regularization 参数； factorization 状态； refinement 次数； 原始算子上的初始与最终 backward error； fallback 原因； 最终被接受的 attempt。 公开 diagnostics、状态判定和证书 provenance 必须全部从最终 receipt 派生，不能再从 planner 的预期值填充。 Fallback 可以保留，但必须： 显式启用； 记录触发原因； 对外可见； 在原始未扰动方程上重新验证； 不能通过 catch-all 异常静默吞掉。 B3. expanded KKT 的固定 companion inertia 判据缺乏数学成立条件 重新对照 expanded 与 reduced 方程后，没有发现需要整体翻转符号的 Newton/KKT 推导错误。两套冻结方程在其变量和残差约定下是相互一致的。 真正的问题是 acceptance logic。 当前 expanded 精确 Newton 算子本质上是非对称的。代码另外构造或检查一个对称 companion，并假设该 companion 应具有固定惯性；若惯性不符合预期，则可能拒绝当前 factorization、施加 regularization 或触发 fallback。 但要将 companion inertia 作为原始 Newton 系统的合法性判据，至少需要证明： companion 与原算子之间是明确的非奇异合同变换，而不仅是行符号翻转、左乘或非合同的缩放； 所需正定/负定块条件在当前迭代点成立； regularization、equilibration 和锥 scaling 没有改变预期惯性； 预期正负特征值数量是根据当前块结构推导出来的，而不是硬编码。 这些成立条件在当前实现契约中没有得到保证。原始非对称算子可逆、且能够得到很小 backward error，并不自动意味着另一个对称 companion 必须具有某个固定惯性。 影响 合法的未正则化 Newton 方向可能被错误拒绝。 触发不必要的 regularization、重复 factorization 或 bordered fallback。 改变 predictor/corrector 方向及步长，造成迭代次数波动甚至停滞。 高精度情况下会浪费尤其昂贵的矩阵构造和分解。 inertia failure 被错误解释成 Newton 方程失败。 正确修复方向 expanded 路径的主 acceptance gate 应是原始算子上的 backward error： η=∥Koriginal​∥∥Δ∥+∥r∥∥Koriginal​Δ−r∥​, 并结合有限性检查与 iterative refinement。 Inertia 只能在理论前提明确时作为诊断或附加保护： 前提成立：根据实际块符号和 regularization 计算 expected inertia； 前提不成立：记录 inertia_unavailable 或 not_applicable； 不能仅因 companion 惯性不符合硬编码值，就否决一个原始残差已经通过的 Newton 解。 二、重要架构与性能问题 I1. reduced-Schur 虽标为 sparse，实际仍保留全局稠密工作区和显式逆 当前 reduced-Schur 路径的 sparse 命名与其真实内存合同不一致： 保留全局 m×m 稠密 workspace； 物化 inverse 或 inverse-like 稠密矩阵； 在 provider 能利用稀疏性之前已经完成 densification； factorization 成功或 nominal certified 状态没有被原始方程残差独立验证。 影响 对于 bootstrap 规模问题： memory=O(m2),dense work=O(m3), 即使输入 A 高度稀疏，也无法获得预期的稀疏扩展性。 对 BigFloat 更严重：大量矩阵元素会引入高精度 limb 初始化、复制和释放成本，GC 与内存带宽可能早于算术吞吐成为主要瓶颈。 显式求逆也不应作为核心 Newton 操作： H−1v 应通过 factor-and-solve 实现，而不是先形成 H−1。 修复原则 先做一个行为保持的原子提交： juliaapply_Hinv!(out, factor, rhs) 替代所有显式逆物化。随后再独立实现： sparse symbolic pattern； numeric refill； symbolic/numeric factorization 分离； 稀疏 Schur 只写入结构上需要的条目； 或矩阵自由的 Schur operator； factor cache 的版本与失效契约。 不能把“成功调用了 sparse provider”当成已经保持稀疏。 I2. ProductConeHSDState 对纯 LP/SOC/SDP 也无条件构造 nonsymmetric coupled workspace 即使模型中完全没有 exponential cone 或 power cone，ProductConeHSDState 仍会构造 nonsymmetric coupled workspace。 该 workspace 的维度与 nr + 2 关联，并包含多块稠密方阵及 LU/factor 缓存。因此，对称锥模型也承担了本不应存在的： O((nr​+2)2) 内存、初始化和高精度对象成本。 影响 LP/SOC/PSD 构造时间与内存异常增大。 BigFloat 初始化成本显著。 workspace 生命周期过长，导致峰值常驻内存上升。 即使 nonsymmetric 路径从未执行，也已经支付完整成本。 benchmark 会把无关 workspace 构造误认为 PSD/KKT 内核成本。 修复原则 在 execution-plan 构造阶段根据 cone trait 分流： juliahas_nonsymmetric_cones(cones) 纯对称锥时： coupled workspace 为 nothing，或使用独立的 symmetric-only state 类型； 不分配其矩阵和 LU 缓存； hot loop 不应在每次 Newton step 中进行动态判断或首次分配。 应加入三种精度下的构造分配回归： Float64 MultiFloat BigFloat 纯 LP/SOC/PSD 模型必须证明没有 nonsymmetric coupled matrix allocation。 I3. provider 的 certified 状态不等于数学证书 当前 provider/factorization 层存在把“factorization 成功”“solve 返回”“refinement 未报错”或 nominal certified 标志，与 Newton 方程数学验收混在一起的倾向。 Provider 无法单独证明： 使用的是不是最终对外宣称的 operator； 是否施加过 regularization； 是否发生过 fallback； residual 是在缩放坐标还是原始坐标计算； residual 对应扰动后的矩阵还是未扰动矩阵； 高精度 accumulation 是否与工作精度一致。 正确分层 Provider 只能报告原始事实： factorization 类型和状态； pivot/regularization 信息； solve/refinement 原始诊断； capabilities。 真正的 certified 必须在 provider 上层统一计算，并使用： 最终 solve receipt； 原始未扰动算子； 原坐标 residual； 全量有限性检查； 明确的精度和 norm contract。 我没有把这一点表述成“已经构造出错误的最终用户证书”的数值反例；但当前契约确实允许求解成功事实与证书事实分离，因此在启用 production certification 前属于必须封闭的风险。 I4. 高精度瓶颈首先是架构性物化和缓存，而不是算术本身 冻结源码中已经确认的高精度缺陷，不是简单的“BigFloat 太慢”，而是： 不需要的稠密 workspace 被提前构造； inverse-like 对象被物化； fallback/inertia failure 可能引发重复高精度 factorization； provider 与 certificate 之间没有统一 residual receipt； 稀疏路径在进入 provider 前已经稠密化。 因此，仅替换 BLAS、增加线程或接入另一个 linear solver provider，不会解决主要问题。 当前证据不足以让我断言核心 Newton 路径存在一个统一的强制 Float64 下转换 bug；已经确认的是，现有 workspace 与 reduced-Schur 结构会系统性破坏任意精度扩展性。优化应优先删除无效物化，再讨论并行核。 三、Newton/KKT 数学审阅的明确结论 项目结论expanded 与 reduced 方程符号在当前变量/残差约定下相互一致，未发现应整体翻转的符号错误原始 expanded operator本质非对称；应直接验证其 backward errorsymmetric companion inertia缺乏作为固定 acceptance gate 的完整成立条件regularization可以使用，但必须记录扰动，并在原始未扰动方程上重新验收bordered fallback可以存在，但必须显式、可追踪、进入证书 provenance高精度不能替代原坐标 residual 和 finite-data 验证修复容差不应放宽任何 primal、dual、gap、cone 或 refinement 容差 尤其不建议因为 inertia 检查失败，就直接修改 KKT 符号。这样会把 acceptance bug 误修成方程 bug。 四、必须新增的回归测试 非有限值污染矩阵 对每个证书字段、每种锥块分别注入 NaN、+Inf、-Inf，必须在任何容差判断前拒绝。 companion inertia 反例 构造原 expanded operator 可逆且 backward error 很小，但 companion 不满足硬编码惯性的实例；未正则化方向必须被接受，inertia 只记录为诊断。 确定性 fallback provenance 用 mock/fault-injection provider 强制 expanded factorization 失败、bordered 成功。结果必须报告完整 attempt chain，而不是依赖随机病态矩阵触发。 regularization 原方程验收 求解扰动系统后，必须保存扰动量，并对未扰动原始算子重新计算 residual。 真正的 sparse contract 大型稀疏测试中禁止生成全局 m×m dense Schur workspace，Newton 路径禁止调用 inv。 对称锥零 coupled allocation LP/SOC/PSD 模型在 Float64、MultiFloat、BigFloat 下都不得构造 nonsymmetric coupled workspace。 这些测试均不需要放宽容差。 五、可并行原子提交计划 波次原子提交内容边界验收条件依赖W1-Acert: fail closed on nonfinite data全局 finite gate 与逐锥 finite membership全部 NaN/Inf 注入测试拒绝；容差不变无W1-Bkkt: add immutable solve-attempt receipt只新增 receipt 数据结构和采集，不改变求解策略单元测试覆盖所有字段；现有数值结果不变无W1-Ccones: lazy nonsymmetric coupled workspace纯对称锥不构造 coupled workspaceLP/SOC/PSD 零 coupled allocation；结果一致无W1-Dschur: replace explicit inverse with factor apply引入 apply_Hinv!，暂不同时重写 sparse backendNewton residual 一致；核心路径不再调用 inv无W2-Akkt-expanded: make inertia diagnostic-only unless provenacceptance 改用原始 operator backward errorinertia 反例通过；现有 expanded 回归不退化W1-BW2-Bsolver: make expanded-to-bordered fallback explicitfallback 返回 attempt receipt；移除计划值填报强制 fallback 测试报告真实最终路径W1-BW2-Csparse-schur: remove global dense m-by-m workspacesymbolic pattern、numeric refill 或 matrix-free operator内存随 nnz/结构增长，不再按 m2 常驻W1-DW3-Acert: derive certification only from final solve receiptprovider 不再拥有最终 certified 语义fallback、regularization、provider 组合测试W1-A、W1-B、W2-BW3-Bproviders: precision-stable owned factor cache缓存所有权、矩阵版本、失效和精度契约Float64/MultiFloat/BigFloat 复用与失效测试W1-B、W1-DW4bench: kernel allocation and backward-error gatesconstructor、Schur build、factor/solve/refine CI gate各精度分别设内存、时间、原残差基线W1-C、W2-C、W3-B 推荐并行关系： {W1-A,W1-B,W1-C,W1-D}→{W2-A,W2-B,W2-C}→{W3-A,W3-B}→W4. 不要整体合并 24c3289… 候选；应把其中有效修改映射到上述原子提交，逐项在 c7a912dd… 基线上验证。数值政策、证书语义和性能重构不应混在同一提交中。 六、本次实际使用的证据 强制入口文件均已使用： 证据用途README_FIRST.md冻结树、审阅顺序与证据边界USER_PROMPT.md审阅目标、禁止项和输出要求MANIFEST.tsv476 个文件的大小与 SHA-256 完整性验证docs/PLAN.md当前算法、provider、certificate 与性能计划状态/审阅附件已知状态、候选 24c3289…、历史 finding 与修复意图expanded/reduced KKT 源码及测试Newton 方程、companion inertia、regularizationbordered/fallback/result diagnostics 源码实际路径与公开元数据来源certificate/cone membership 源码及测试NaN/Inf fail-openreduced-Schur/provider 源码dense workspace、显式逆与 nominal certificationProductConeHSDState 相关源码无条件 nonsymmetric workspaceBigFloat/MultiFloat/provider 相关源码高精度 workspace 和 factor contract 完整逐文件清单已经按 MANIFEST.tsv 写出，包括： 证据相对路径； Manifest 中的 SHA-256； 独立重算的 SHA-256； 是否一致； 该文件在本次审阅中的用途。 下载完整严格审阅报告（含源码路径与行号摘录） 下载 MANIFEST-backed 证据使用清单 下载完整解压文件目录清单SDPX_c7a912dd_strict_review.mdDocumentOpen fileSDPX_c7a912dd_evidence_used.tsvFileOpen fileSDPX_c7a912dd_archive_inventory.txtDocumentOpen file
