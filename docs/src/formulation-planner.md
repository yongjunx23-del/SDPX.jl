```@raw html
<!-- The source of this page is kept at docs/formulation_planner.md so it is
     also readable directly from the repository. -->
```

```@eval
using Markdown
Markdown.parse(
    read(joinpath(@__DIR__, "..", "formulation_planner.md"), String),
)
```
