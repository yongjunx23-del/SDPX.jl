# Reviewed provenance dossier

- External generator: `pole_ansatz_scan/julia/generate_pole_pmp.jl`, SHA-256
  `72e0e5f16c1b0f5f3e671a9f0599d06425977d672437a265fbab1b5f07827fcd`.
- External auditor: `pole_ansatz_scan/julia/audit_pole_case.jl`, SHA-256
  `407c4ff154c69989e24a28ec764bcb17dbf7c71d17c3f35858588c318231fbba`.
- Frozen external receipt JSON SHA-256
  `03399fed09bcf535fce61084536ff8c18d9aa260243cd657e89f76f7b69525ba`.
- `1541ab4fa666eedebbdc16706b668040c963f04d` is the SDPX import base only;
  it is not an external identity for the coefficient matrix.

The machine-readable manifest is authoritative.  It records only the
implemented source variable `tau`, the source guard precision and operation
order, and the algebraic equivalence of the source PSD block, Q3 vector, and
unitarity disk.  The physical factor-four normalization is unresolved.

Rows remain `sampled_build_only`; the exact N14 receipt g0 strings are bounded
provenance metadata and are not attached to this artifact's coefficient
matrix.  Production is absent from ordinary suites and remains behind the
explicit guarded production driver.  No strict witness or solver certificate
is claimed.
