feat(ir): heterogeneous ConeProductLayout + mixed-family route (Subagent A)

- Add ConeProductLayout{Blocks} and ConeBlockDescriptor (offset, dimension,
  storage, cone parameter, dual orientation, reconstruction metadata).
- Add cone_product_layout(program), global_to_block/block_to_global exact
  coordinate mapping, per-cone barrier_degree.
- Add PowerCone domain + :power native cone kind.
- classify_native_cone_program now returns :mixed_family instead of throwing
  UnsupportedNativeConeRoute; single-family lowering still fails closed.
- Tests: cone_product_layout.jl + updated route_classifier.jl.
