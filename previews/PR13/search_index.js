var documenterSearchIndex = {"docs":
[{"category":"section","location":"precision/#Precision","page":"Precision","text":"SDPX solves in the arithmetic owned by its typed Model{T}. Construct the model with Model(Float64), Model(BigFloat; precision_bits=...), or a loaded fixed-width type such as Model(Float64x4). There is no process-global arithmetic selector and no implicit conversion from an untyped model.","title":"Precision"},{"category":"section","location":"precision/#Choosing-a-type","page":"Precision","text":"Type Significand ≈ decimal digits Notes\nFloat64 53 bits ~16 fastest; fine when you do not need more than double precision\nFloat64x2 (MultiFloats.jl) 105 bits ~31 bitstype, zero GC pressure, threads normally\nFloat64x4 (MultiFloats.jl) 209 bits ~62 sweet spot for many EFT/modular-bootstrap runs\nBigFloat model precision_bits arbitrary arbitrary precision; Model(BigFloat) defaults to 256 bits\n\nMultiFloats.jl types are enabled automatically once you using MultiFloats in your session, with no other change needed.","title":"Choosing a type"},{"category":"section","location":"precision/#BigFloat-precision-plumbing","page":"Precision","text":"Model(BigFloat; precision_bits=bits) owns the model and solve precision. Public modeling operations copy coefficients, right-hand sides, starts, and objective data inside that precision scope. Existing BigFloat values still carry the precision at which they were originally created: copying a rounded value into a wider model cannot recover missing digits. Build source data inside setprecision(BigFloat, bits) do ... end whenever those digits matter.","title":"BigFloat precision plumbing"},{"category":"section","location":"precision/#Staged-working-precision","page":"Precision","text":"BigFloat callers select the staged working-precision policy through typed public settings:\n\nmodel = Model(BigFloat; precision_bits=256)\nsettings = Settings{BigFloat}(working_precision_policy=:auto)\nresult = optimize!(model; settings=settings)\n\nThe selector combines the smallest requested tolerance, a 96-bit numerical guard, and a dimension term, rounds upward to 32 bits, and clamps the result to the predeclared ladder. A lower-precision result is accepted only if normal original-coordinate certification succeeds. Precision exhaustion, stagnation, an almost-optimal status, or a numerical failure advances to the requested model precision when the predeclared policy and remaining wall time allow it.\n\nThe policy defaults to :auto; :fixed remains the expert override. See the native BigFloat report for measured staged-precision behavior.","title":"Staged working precision"},{"category":"section","location":"precision/#Guarded-mixed-precision-KKT-solves","page":"Precision","text":"mixed_precision_kkt defaults to :auto for BigFloat and fixed-width types wider than Float64, and to :off for Float64. When enabled, SDPX may attempt a lower-precision factor only if crossover and conditioning guards permit it, then computes residuals and accumulates corrections in the requested arithmetic.\n\nThis is a guarded accelerator rather than a change in the numerical contract: loss of positive definiteness or rank during conversion, a conservative condition estimate above the configured limit, an excessive predicted correction count, non-finite conversion, or failed target-precision refinement switches back to the native factorization and recomputes the direction. Explicit :on for fixed-width arithmetic is a measured expert mode. BigFloat and :auto retain static cutoffs. Repeated rejection is cooled down and eventually disabled for that solve. Exact singleton-local 2×2 BigFloat arrows have a separate guarded path when MultiFloats is loaded. See bench/mixed_precision_kkt/RESULTS.md for the current promotion evidence and exact thresholds.","title":"Guarded mixed-precision KKT solves"},{"category":"section","location":"precision/#Tolerance-vs.-precision","page":"Precision","text":"ϵ_gap below roughly 100·eps(T) is usually unreachable: solve! warns up front when this looks like the case. If progress reaches the arithmetic floor, the solver returns a non-optimal status with structured precision-floor or stagnation diagnostics instead of silently spinning. As a rule of thumb, reaching a duality gap of 10^-d needs roughly d·log2(10) ≈ 3.32·d significand bits of headroom beyond the noise floor of the linear algebra itself.","title":"Tolerance vs. precision"},{"category":"section","location":"precision/#Dynamic-range","page":"Precision","text":"MultiFloats.jl types inherit Float64's exponent range (~10±308). Raw high-degree-polynomial bootstrap sample data can exceed this. solve! detects a non-finite iterate and reports NumericalBreakdown with a message pointing at scaling=:equilibrate (which routinely cuts several orders of magnitude off the dynamic range of badly scaled data) or falling back to BigFloat.\n\nBigFloat values are mutable, so general native BigFloat kernels are serial; ownership-safe independent blocks, exact local arrow phases, and disjoint Schur tiles may use requested workers. Fixed-width types can use Julia threads more broadly.","title":"Dynamic range"},{"category":"section","location":"threading/#Threading-Guide","page":"Threading","text":"","title":"Threading Guide"},{"category":"section","location":"threading/#Starting-and-limiting-threads","page":"Threading","text":"Start Julia with -t N or JULIA_NUM_THREADS=N. The process thread count is an upper bound; SolverOptions(threads=...) or the public threads=... keyword may select fewer threads for one solve:\n\nJULIA_NUM_THREADS=8 OPENBLAS_NUM_THREADS=8 julia --project=. solve_problem.jl\n\nresult = solve(c, A, C, B, b; threads=4)\n\nThe execution plan can reduce that request when the arithmetic or estimated work does not justify parallel scheduling.\n\nArithmetic Solver scheduling policy\nFloat64 Threaded for sufficiently large block and Schur work; small latency-bound cases remain serial.\nFloat64xN Threaded when the type is an immutable fixed-width arithmetic; extended arithmetic crosses over earlier than Float64.\nBigFloat General native MPFR phases use one solver thread. Exact singleton-local 2x2 arrows and all-local 2x2 cell systems with explicit equalities may use ownership-safe block tasks and disjoint Schur/Gram-tile workers.\nUnknown scalar type Serial unless the kernel layer explicitly marks it safe.","title":"Starting and limiting threads"},{"category":"section","location":"threading/#Scheduling-and-synchronization","page":"Threading","text":"PSD blocks are normally assigned to workers by longest-processing-time (LPT) greedy partitioning. The weights estimate factorization and Schur work, so heterogeneous bootstrap blocks are balanced by cost rather than by block count. One measured exception applies to uniform Float64x4 reduced-arrow systems: with at least 256 identical 2x2 blocks and at most 32 active block tasks, contiguous ownership improves cache and pointer locality without creating load imbalance. Wider teams and every heterogeneous system retain LPT. Executed diagnostics report both fine_grained_block_tasks and fine_grained_block_partition.\n\nThe principal threaded paths avoid locks and atomics:\n\ndense blocks accumulate into one private Schur matrix per active LPT bin;\nsparse blocks transform only active variables and own their compact pair values;\nexact-arrow problems write disjoint local and coupling blocks, with a small private global-global accumulator per bin;\nresiduals, predictor/corrector right-hand sides, direction recovery, local arrow factorization, and local arrow solves reuse the same cached schedule.\n\nReductions happen in a fixed bin order. At a fixed thread count the result is deterministic. Changing the thread count may change the floating-point reduction order and therefore the last few bits.","title":"Scheduling and synchronization"},{"category":"section","location":"threading/#Block-local-crossovers","page":"Threading","text":"Residual construction, block Cholesky, predictor/corrector right-hand sides, and direction recovery use the cached LPT schedule when either the historical 256-block threshold is met or estimated cubic block work is large enough:\n\nFloat64-family sum(k[l]^3):          at least 1,000,000\nfixed-width extended sum(k[l]^3):   at least   100,000\n\nThis dimension-aware route matters for dense lattice SDPs that have a small number of moderately large blocks. Task_Low08 has 32 blocks of dimensions 23–74 and sum(k[l]^3) = 3,977,757; the former count-only policy therefore left all block-local phases serial. On one node125 run with 16 OpenBLAS threads, the retained scheduler measured median solve times of 36.708 / 32.158 / 29.780 / 27.424 seconds at 4 / 8 / 16 / 32 Julia workers. All runs used 28 iterations and 119 backtracking contractions and passed the same original-coordinate certificate. A direct old/new same-node A/B at 32 workers improved median solve time from 32.062 to 28.438 seconds (11.3%).\n\nTask-local dense Schur accumulators remain memory-capped. The default share is 15% of scheduler-aware available memory. A deliberately narrow Float64 policy raises it to 25% only when all of these conditions hold:\n\nSchur dimension:       at least 4,096\nPSD block count:       at least 16\nrequested workers:     at least 16\navailable memory:      at least 16 GiB\n\nOn Task_Low08 with a 28 GiB explicit ceiling this selects 25 of the 32 possible bins. A same-node controlled run reduced median Schur assembly from 8.140 to 6.701 seconds and the stable solve from 27.449 to 25.965 seconds. Increasing the share to 35% produced only a noisy 1.7% median change, made the stable second repetition 2.1% slower, and raised peak RSS by 0.44%; it was rejected. Fixed-width extended and BigFloat arithmetic retain the 15% rule.","title":"Block-local crossovers"},{"category":"section","location":"threading/#Schur-crossovers","page":"Threading","text":"Thread launch and reduction are measurable costs. Dense Schur assembly uses a conservative arithmetic-work crossover:\n\nFloat64 estimated Gram work:          at least 4,000,000\nfixed-width extended estimated work:  at least   100,000\n\nThe automatic plan also keeps Float64 SDP models with only 1x1/2x2 blocks and fewer than 1,000 variables serial. These are policy thresholds, not universal hardware constants; benchmark changes on the target cluster before recalibrating them.\n\nWhen a block kernel produces only the lower triangle, the reducer also reads, initializes, and accumulates only that triangle. Work is split by triangular area across column-major ranges. For other fixed-width paths, the established full-matrix reducer remains in use where it benchmarks faster.\n\nThe Float64x4 reduced-arrow path also has phase-specific worker and tile crossovers. A narrow shared triangle may expose fewer independent tiles than requested workers, while its thousands of local 2x2 blocks can still use a wider team. SDPX therefore selects Schur/SYRK workers separately from short block-local tasks and reports the executed value as schur_threads. On the measured 1,700-block, 144-shared-variable CSDR model, 48–95 Schur workers use an eight-column tile only when the default twelve-column triangle has fewer than two jobs per worker. Requests of 96 or more use at most 64 Schur workers for this narrow geometry and at most 32 tasks for synchronization-sensitive block phases. Wider shared systems retain the full request.\n\nThe same path uses a separate factor_threads crossover for the reduced shared Cholesky. Orders 128–256 use a sixteen-column lower-only factor with one cached reciprocal per pivot and four-lane SIMD across independent panel rows. Trailing panel solves and triangular updates use at most eight workers; other dimensions and arithmetic types keep their established factor. On the order-144 EPYC benchmark, the previous factor took 15.891 ms, the new serial kernel took 10.253 ms, and the eight-worker kernel took 6.890 ms. In the full 41-iteration medium solve, cumulative factor time fell from 0.671 to 0.140 seconds. Executed diagnostics report the selected factor width.\n\nDense non-arrow Float64x4 refinement repeatedly applies the symmetric Schur matrix after a lower-only build. For dimensions of at least 1,024 and more than one requested worker, SDPX partitions complete output-row ranges among at most ceil(dimension / 256) workers. Each task reads the stored triangle and owns every scalar it writes; no task-local matrix or reduction is needed. The scalar hot loop allocates zero bytes. Aliased input/output vectors fall back to the established multiplication path.\n\nThe opt-in dense Float64x4 mixed KKT fallback uses Float64x2 only after the Float64 residual guard fails. Conversion and first-touch initialization own disjoint linear ranges; blocked Cholesky panel solves own disjoint rows; trailing and equality SYRK tasks own complete lower-triangular tiles; and L^-1 B plus recovery products own disjoint right-hand-side columns or output ranges. This path respects SolverOptions.threads, leaves BLAS at one thread, and allocates its 0.596 GiB Task_Low08 workspace lazily. Native Float64x4 remains the final serial fallback.","title":"Schur crossovers"},{"category":"section","location":"threading/#BigFloat-policy","page":"Threading","text":"Native BigFloat uses the serial owned-storage path for general models even if Julia was started with multiple threads. Exact singleton-local 2x2 arrow models and block-diagonal 2x2 cell models whose Schur variables are all local and whose only global coupling is through explicit equalities are the current native exceptions.\n\nThe reason is solver-specific: a BigFloat is mutable, ordinary zeros(BigFloat, ...)/fill! storage can alias the same object, and arbitrary-precision task-local matrices grow quickly with worker count. SDPX therefore uses independently owned workspace entries plus allocation-reusing MPFR scalar kernels. For the reduced-arrow exception, each preparation task owns a disjoint block workspace and two panel rows, and each SYRK task owns a complete lower-triangular output tile. Inputs are read-only and no writable BigFloat object crosses tasks. The equality specialization likewise assigns complete local row blocks during forward/back substitution and complete lower Gram tiles during Btil' * Btil; it never allocates one full Gram per worker.\n\nThe current fixed-trace release campaign intentionally stops at 32 workers (J40: 1/2/4/8/16/32; J80: 8/16/32, BLAS=1). The wider measurements below are archived scaling evidence from earlier campaigns, not current template defaults or recommendations.\n\nThe two equality matrix-vector products in each KKT solve also partition complete output ranges. Every dot product keeps the serial reduction order, and a work crossover caps the task count when the panel cannot amortize startup. Fine-grained all-local block, triangular, GEMV, predictor/corrector, line-search, and update phases are additionally capped at 64 ownership tasks; the disjoint equality Gram tiles may use the full requested width. On a 512-bit 16,400-by-230 synthetic CSDR-shaped system, the equality Gram scaled from 58.618 seconds at one worker to 1.339 seconds at 128 workers on a dual-socket EPYC 7742 node while retaining a 5.64e-152 relative KKT residual. Use numactl --interleave=all on that eight-NUMA-domain node and keep BLAS/OMP at one thread for this MPFR path.\n\nThe cap comes from an end-to-end certificate run, not the synthetic Gram alone. On the J40 BigFloat512 model, a uniform 128-worker schedule took 495.811 seconds, versus 368.704 seconds at 64 workers. The phase-aware cap reduced the 128-worker time to 425.880 seconds and peak RSS by 6.6%, while reproducing the 158-iteration objective, gap, residuals, PSD margin, off-grid residual, and disk certificate bit-for-bit. Because 64 remains 15.5% faster, a 96-worker check was also run; it took 398.303 seconds, still 8.0% slower than\n\nThose results motivated measuring rather than assuming the widest\n\nallocation; current fixed-trace templates do not request more than 32 workers.\n\nThe 64-worker path was separately validated at a fixed 1,024-bit precision. It passed the J40 physical certificate in 157 iterations and took 553.959 seconds, with 4,268,480 KiB peak RSS. Equality Gram (115.491 seconds) and the 170-by-170 equality factorization (42.838 seconds) became the main precision-scaling costs. The corresponding 512-bit solve remained faster at 368.704 seconds and had a tighter terminating relative gap on this once-rounded Float64x4 input, so 512 bits remains the recommended model-specific setting.\n\nUse Float64x4 or another fixed-width MultiFloats type when its precision and Float64 exponent range are sufficient and broader solver phases need multicore speedup. The experimental mixed_precision_kkt=:float64x4 mode is another path for exact singleton-local 2x2 arrows: it constructs and factors the reduced shared system in Float64x4, checks residuals and refines in BigFloat, and automatically falls back to native BigFloat if correction is unsafe. Only the Float64x4 panel/factorization uses the requested workers; the native MPFR phases remain serial. This mode did not deliver a clear end-to-end improvement on the medium CSDR benchmark and therefore remains off by default.\n\nFor general non-arrow arbitrary precision, run independent BigFloat instances as separate processes or scheduler-array elements.\n\nOn the medium exact-arrow CSDR model at 256 bits, the ownership-safe native path measured 205.202 / 191.701 / 110.741 / 86.752 seconds with 1 / 2 / 4 / 8 Julia threads. The matched one-thread legacy path took 280.011 seconds. All thread counts produced the same 41-iteration certified result. This scaling is specific to the exact reduced-arrow structure; it is not evidence for enabling arbitrary threaded MPFR loops.","title":"BigFloat policy"},{"category":"section","location":"threading/#Phase-aware-BLAS-threads","page":"Threading","text":"Block-parallel phases issue many small linear-algebra calls. SDPX temporarily sets BLAS to one thread there so Julia workers × BLAS workers does not oversubscribe the node. Dense KKT Cholesky is a single large operation, so the solver restores a bounded BLAS width for that phase and restores the caller's setting afterward, including on exceptions.","title":"Phase-aware BLAS threads"},{"category":"section","location":"threading/#Apple-Accelerate","page":"Threading","text":"On macOS, AppleAccelerate.jl can replace OpenBLAS through libblastrampoline. Load it explicitly before solving:\n\nusing SDPX\nusing AppleAccelerate\n\nSDPX.set_blas_threads!(1)\n@assert SDPX.blas_backend() == :apple_accelerate\n@assert SDPX.blas_threads() == 1\n\nUse SDPX.set_blas_threads!, rather than LinearAlgebra.BLAS.set_num_threads, when Apple Accelerate is loaded. Accelerate uses BLASSetThreading; the ordinary libblastrampoline setter does not switch it into strict single-threaded mode. AppleAccelerate remains an optional dependency because loading it changes the process-wide BLAS/LAPACK backend for every Julia package in the process.","title":"Apple Accelerate"},{"category":"section","location":"threading/#Linux-BLAS-backends","page":"Threading","text":"Linux deployments can benchmark alternative libblastrampoline backends in a release-specific environment. Load the backend before SDPX, then use the same backend-aware thread controller:\n\nusing MKL                 # or: using BLISBLAS\nusing SDPX\n\nSDPX.set_blas_threads!(16)\n\nDo not assume that a vendor backend is faster on every CPU. On the cluster's dual-socket AMD EPYC 7742 node, equal eight-core, eight-iteration Task_Low08 jobs took 10.816 seconds with OpenBLAS, 13.018 seconds with MKL, and 21.999 seconds with BLIS. OpenBLAS therefore remains the selected backend there. MKL and BLIS are benchmark-only environment dependencies, not SDPX dependencies.\n\nDo not start by assigning both Julia and BLAS every core. Use one core per Julia thread, set a realistic process limit, and measure complete iteration time. Nested or concurrent solves share the process-global BLAS setting and are not a supported way to obtain parallelism; use separate processes.","title":"Linux BLAS backends"},{"category":"section","location":"threading/#Medium-reduced-arrow-affinity-and-NUMA-result","page":"Threading","text":"The J/K/Na/Nmu = 32/4/16/100 Float64x4 CSDR model has 1,700 uniform 2x2 blocks and a 144-column reduced shared system. On a dual-socket, eight-NUMA- domain AMD EPYC 7742 node, keep BLAS at one thread and start Julia with an exact compute pool (--threads=N,0 --gcthreads=1,0). Setting JULIA_EXCLUSIVE=1 is important: at 64 workers it reduced the median solve from 5.981 to 4.791 seconds. Without it, 56 of 62 sampled workers migrated between CPUs and visited 9.32 CPUs each on average; exclusive affinity pinned every sampled worker to one CPU and reduced voluntary context switches from 2.45 million to 0.72 million.\n\nBind the selected CPUs and allocate memory locally. A 64-worker run bound to CPUs 0–63 and NUMA nodes 0–3 took 5.981 seconds before the affinity gain; forcing interleaving over the same nodes took 6.304 seconds and was rejected. At 32 exclusive workers, contiguous block ownership improved the same-node median from 5.010 to 4.555 seconds. At 64 workers, LPT was slightly faster than contiguous ownership (4.789 versus 4.866 seconds), so the automatic contiguous crossover stops at 32 tasks. The final pre-factor scaling sweep measured 44.293 / 23.382 / 12.969 / 7.526 / 4.880 / 4.709 / 4.614 / 12.444 seconds at 1 / 2 / 4 / 8 / 16 / 32 / 64 / 128 workers. The 128-worker process really reached all 128 workers, but averaged only 13.53 cores, incurred 24.89 million voluntary context switches, and was 2.70 times slower than 64. The short phases and 144-column triangle do not contain enough work for that team width.\n\nThe later factor A/B used 32-worker pools and reduced the two process medians from 4.710/4.793 to 3.422/3.418 seconds. Those results do not imply that a larger Julia pool is free: even an eight-task factor can be scheduled across all NUMA domains of a 128-thread process. Launch the smallest pool that wins the full scaling sweep; a large scheduler reservation is not evidence that all reserved cores helped the solve.\n\nThe retained narrow-geometry solver cap keeps the scheduling decision based on the original request but limits actual workspace and numerical work to 64 workers at requests of 96 or more. For a 128-thread Julia runtime this preserves 32 contiguous fine-grained bins and 64 Schur tasks. An alternating same-node A/B reduced the combined solve median from 12.216 to 11.839 seconds and allocation from 216.41 to 166.54 MB per solve; a genuine 64-thread control was neutral. Diagnostics distinguish threads=128, effective_threads=64, fine_grained_block_tasks=32, schur_threads=64, and factor_threads=8.\n\nThe remaining 128-pool loss was predominantly Julia worker wake-up overhead. With otherwise identical retained code, setting JULIA_THREAD_SLEEP_THRESHOLD=10000000 before Julia starts reduced a 20-sample full-solver median from 11.635 to 3.409 seconds. That was within 1.0% of permanently awake workers, but used about 12.5 CPU-core equivalents over the complete process instead of 113. The 10 ms processes still reached 124–126 cores during parallel bursts and cut voluntary context switches from about 27.5 million to 108–115 thousand. All 35 comparison rows were Optimal in 41 iterations with bit-for-bit identical printed objectives, residuals, gaps, and PSD certificates.\n\nThe threshold is expressed in nanoseconds and is consumed by the Julia runtime before SDPX loads. It is therefore a launch recommendation for this measured EPYC topology, not solver-global state or a portable default. Use it only in an exclusive allocation, and repeat the exact-pool sweep on materially different hardware or problem geometry. Permanent spinning is rejected for routine use because it consumes the entire node during serial work without a meaningful solve-time advantage over 10 ms.\n\nThese are topology-specific measurements, not a reason to hide resource usage. Verify each new node with process RSS, per-thread CPU affinity, active worker counts, BLAS width, and numastat samples. A large reservation can improve queue placement on this site, but it does not make unused cores part of the numerical speedup.","title":"Medium reduced-arrow affinity and NUMA result"},{"category":"section","location":"threading/#Final-exact-pool-Float64x4-audit","page":"Threading","text":"The retained exact-pool campaign reserved all 128 physical cores of one dual-socket EPYC 7742 node, but launched a separate Julia process for each measured pool. Every process used --threads=N,0 --gcthreads=1,0, JULIA_EXCLUSIVE=1, one BLAS/OMP worker, three complete warm-ups, and five recorded solves. The finite 10-millisecond worker-sleep policy was used for the table below. The 32-worker row combines two independently launched five-solve controls; other rows contain five solves.\n\nJulia workers Solve median (s) Speedup vs 1 Whole-process mean cores Peak sampled cores Maximum active workers Peak RSS (GiB)\n1 44.112 1.00x 1.00 1.00 1 2.332\n2 22.954 1.92x 1.50 1.99 2 2.307\n4 12.450 3.54x 2.05 3.98 4 2.358\n8 6.946 6.35x 2.57 7.95 8 2.582\n16 4.275 10.32x 3.09 15.89 16 2.955\n32 3.286 13.42x 4.15 31.79 32 3.041\n48 2.967 14.87x 5.25 45.38 48 3.144\n64 3.074 14.35x 6.70 62.53 64 3.304\n96 3.540 12.46x 9.42 94.87 96 3.291\n128 3.403 12.96x 12.39 125.78 128 3.359\n\nThe mean CPU column covers package/model loading, compilation, serialization, and serial regions as well as all warm-up and measured solves; it is not a parallel-kernel utilization claim. The peak samples and per-thread affinity records independently prove that every requested worker was created, pinned, and active. At 48 workers, 48 Julia compute threads were pinned one per CPU over CPUs 0–47. At 128, all 128 were pinned over CPUs 0–127. The solver's diagnostics additionally report effective_threads=64, fine_grained_block_tasks=32, schur_threads=64, and factor_threads=8 for the over-wide 96/128 narrow-arrow requests; runtime/model phases may still activate the complete Julia pool. Use a 48-thread runtime when solving this geometry rather than relying on the defensive wide-pool cap.\n\nNUMA sampling confirms that placement requests affected real memory. The 48-worker socket-local run placed about 2,696 / 177 / 107 / 27 MiB on nodes 0–3 (3,009 MiB total at the sampled peak). The 128-worker interleaved run placed 349–474 MiB on each of nodes 0–7 (3,231 MiB total). A prior 64-worker same-domain interleave experiment was 5.4% slower than local first touch, so the recommended 48-worker launch uses --physcpubind=0-47 --membind=0-3.\n\nThe one-worker phase split was dominated by Schur assembly (24.540 seconds, 55.6%), predictor (6.421 seconds), corrector (6.681 seconds), and residual work (4.417 seconds). At the 48-worker optimum those phases were 0.741, 0.371, 0.513, and 0.222 seconds. Schur therefore scales well but the many short predictor/corrector, residual, line-search, and update regions impose the limiting barriers. Increasing from 48 to 64 makes Schur 80 milliseconds faster but adds more synchronization and NUMA cost elsewhere; 96/128 lose further despite genuinely activating those pools.\n\nThree final structure-specific changes were retained after this sweep. Lazy legacy Schur partials reduced the 48-worker solve from 2.982 to 2.940 seconds and allocation from 148.46 to 118.08 MiB. Cache-hot singleton-local factor preparation then removed 99.96% of the local-elimination phase and reduced a 14-solve median from 2.9268 to 2.9042 seconds. Finally, allocation-free MultiFloatVec RHS/recovery kernels reduced predictor/corrector linear solves by 6.76%/8.88% and a separate 14-solve median from 2.9134 to 2.8952 seconds. Each retained A/B used reverse ordering, all 48 workers, BLAS=1, and exact certificate comparison. Contiguous 48-bin ownership (0.52%), a 100-millisecond sleep policy, and an eight-block recovery grouping were rejected because their gains were absent or negative under repeat measurement.\n\nRecommended PBS launch separation for this site:\n\n# Reserve the complete node for predictable placement, but create only the\n# measured useful Julia pool for this 1,700-block / 144-shared-column model.\nexport JULIA_EXCLUSIVE=1\nexport JULIA_THREAD_SLEEP_THRESHOLD=10000000\nexport OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1\n\nnumactl --physcpubind=0-47 --membind=0-3 \\\n  julia --threads=48,0 --gcthreads=1,0 --project=. solve.jl\n\nExecuted solve diagnostics expose arrow_linear_solve in addition to the effective/block/Schur/factor widths, so resource audits can distinguish the Float64x4 SIMD singleton route from the scalar fallback.","title":"Final exact-pool Float64x4 audit"},{"category":"section","location":"threading/#Measured-scheduler-behavior","page":"Threading","text":"The latest isolated benchmarks used Julia 1.12.6 on an Apple M4 process exposing four hardware threads, with BLAS restricted to one thread. The eight-worker rows intentionally oversubscribe that host and are not eight-core results.\n\nArithmetic and case 1 worker Best measured Speedup\nFloat64 dense, L/m/k=8/96/6 0.199 ms 0.196 ms at 4 1.02x\nFloat64 dense, 8/512/10 6.561 ms 3.998 ms at 2 1.64x\nFloat64x4 dense, 8/128/8 137.318 ms 33.669 ms at 8 4.08x\nFloat64 sparse arrow, blocks/shared=512/24 0.254 ms 0.230 ms at 8 1.11x\nFloat64x4 sparse arrow, 256/16 5.415 ms 1.453 ms at 8 3.73x\n\nThe small Float64 crossover held the build near its serial 0.20 ms value and reduced calling-task allocations to 768 bytes. Maximum relative Schur errors were 2.67e-16 for dense Float64, 3.57e-65 for dense Float64x4, 1.67e-15 for sparse Float64, and 1.33e-64 for sparse Float64x4.\n\nSee bench/threading/RESULTS.md for the full protocol, all 1/2/4/8-worker rows, reduction timings, memory estimates, and reproduction commands.","title":"Measured scheduler behavior"},{"category":"section","location":"threading/#Memory-limits","page":"Threading","text":"Dense task-local partials still occupy a full m × m matrix per active bin. The bin planner caps this storage from current available memory. Available memory is the minimum usable value reported by:\n\nhost free memory;\nLinux cgroup v2 (memory.max/memory.current) or v1 counters;\nthe optional SDPX_MEMORY_LIMIT_BYTES environment ceiling.\n\nAccepted explicit units include B, KB, KiB, MB, MiB, GB, GiB, TB, and TiB. SDPX keeps additional headroom and may choose fewer workers or reject optional panel packing even when the requested fraction appears to fit.\n\nFor PBS/Slurm jobs, set the explicit ceiling slightly below the scheduler request when the cgroup does not expose a reliable limit. See the cluster workflow.","title":"Memory limits"},{"category":"section","location":"threading/#Remaining-multicore-limits","page":"Threading","text":"Float64x4 reduced-arrow factors of order 128–256 now use cached reciprocals, SIMD panel rows, and at most eight trailing-update workers. Larger reduced factors and generic native extended-precision dense Cholesky still need a scalable factorization backend.\nGeneric non-arrow Schur/KKT factorization is dense and remains a dominant phase on large lattice problems. The Float64x4 refinement matvec is threaded, but native extended-precision dense Cholesky is still serial.\nDense task-local Schur matrices are not packed; changing their workspace layout could nearly halve partial-storage memory.\nWorker teams are created per region; persistent teams could remove approximately 20–100 microseconds of repeated launch cost.\nAutomatic topology discovery, portable NUMA first-touch policy, and distributed factorization have not been implemented. Explicit socket-local binding is validated only for the medium EPYC reduced-arrow profile above.","title":"Remaining multicore limits"},{"category":"section","location":"development/#Development","page":"Development","text":"Instantiate and run the package tests:\n\njulia --project=. -e 'using Pkg; Pkg.instantiate(); Pkg.test()'\nJULIA_NUM_THREADS=4 julia --project=. -e 'using Pkg; Pkg.test()'\n\nBuild the manual locally:\n\njulia --project=docs -e 'using Pkg; Pkg.develop(path=\".\"); Pkg.instantiate()'\njulia --project=docs docs/make.jl\n\nOnly the small benchmark tier belongs in routine CI:\n\njulia --project=bench benchmark/runner.jl micro --output=/tmp/sdpx-micro.toml\n\nPerformance changes must state the model, arithmetic, tolerances, hardware, thread configuration, warm-up, timing boundary, numerical result, and repeated runtime statistic, and must preserve the original-coordinate certificate boundary. BigFloat threads may only own disjoint mutable storage.\n\nSee CONTRIBUTING.md for the review standard and THIRDPARTYNOTICES.md for provenance.\n\nOperational references are kept as focused documents: the bridge schema, cluster workflow, adaptive dense/sparse optimization, adaptive parameter policy, and threading guides. Measured results and provenance live in the benchmark directories and bench/RESULTS.md.","title":"Development"},{"category":"section","location":"preprocessing/#Preprocessing","page":"Preprocessing","text":"SDPX runs a typed structural preprocessing pipeline before scaling and the numerical solve. The pipeline operates on SDPProblem{T} and preserves the original arithmetic throughout. JuMP and MathOptInterface are adapters; none of the reduction logic depends on JuMP.","title":"Preprocessing"},{"category":"section","location":"preprocessing/#Stages","page":"Preprocessing","text":"The public entry point is:\n\nprepared = SDPX.preprocess(problem, options)\n\nprepared contains the transformed problem, a typed PreprocessPlan, a ReconstructionMap, and a structured PreprocessReport. The ordinary solve and solve! entry points run the same pipeline automatically and return the solution in the original coordinates.\n\nThe automatic stages are:\n\nextract single-variable scalar PSD bounds and merge exact lower and upper limits;\neliminate only variables whose lower and upper limits are exactly equal in arithmetic type T;\nremove exact zero, duplicate, and proportional equalities;\nreuse the existing arithmetic-aware equality rank presolve;\nrun the existing block-aware scaling after structural reduction;\nestimate primal and dual formulation costs;\nestimate chordal clique and overlap costs; and\nreconstruct and certify the result against the original problem.\n\nApproximate fixed-variable elimination is not performed. Near-proportional equalities are counted for diagnostics but retained. Numerical equality dependencies are removed only after both the column relation and right-hand side reconstruction pass arithmetic-aware checks. If a numerical relation is ambiguous, the original equalities are retained.\n\nPrimal-to-dual conversion and chordal transformation are analysis-only. The estimators may report a potentially cheaper alternative, but neither transformation is selected automatically.\n\nSparse Schur planning is also completed before a workspace is allocated. problem.structure.schur_analysis records the active-constraint overlap graph, estimated upper-triangle nonzeros, density, and per-block active counts; problem.structure.schur_plan records the deterministic :dense, :sparse, or :block_sparse choice and its reason. A selected Float64 sparse plan freezes one CSC pattern and SchurAssemblyMap, so later iterations update only nzval. SDPX.sparse_schur_diagnostics(workspace) reports structural/numeric nonzeros, factor fill, pattern reuse, overlap/block counts, and assembly timing. Generic MultiFloat/BigFloat sparse-Schur equality recovery remains explicitly fail-closed: an explicit sparse=:sparse request raises before workspace construction, while sparse=:auto may choose the dense route when its plan does not have a supported provider.","title":"Stages"},{"category":"section","location":"preprocessing/#Bound-storage","page":"Preprocessing","text":"MOI supports VariableIndex and ScalarAffineFunction constraints in GreaterThan, LessThan, and Interval sets. A scalar affine constraint is recognized as a variable bound only when it has exactly one nonzero coefficient.\n\nSingle-variable scalar blocks use CompactScalarCoefficientVector{T}. It preserves the historical Asp[block][variable] indexing contract while avoiding a length-m reference vector for each bound. Active-only ingestion, classification, chordal analysis, scaling, LP extraction, and precision preparation avoid scanning inactive variables.\n\nThe merged bound plan uses contiguous Vector{T} lower and upper arrays plus compact activity flags and original source indices. No bound is converted through Float64.","title":"Bound storage"},{"category":"section","location":"preprocessing/#Reconstruction","page":"Preprocessing","text":"The reconstruction map records:\n\nretained-to-original variable, block, and equality indices;\nfixed variables and their typed values;\nthe eliminated objective offset;\nremoved bound constraints needed for nonnegative dual reconstruction; and\nthe equality multiplier map for exact and numerical equality reductions.\n\nWarm starts may be supplied in original coordinates. Final primal and dual objectives, affine residuals, cone residuals, and PSD certificates are recomputed against the original unscaled problem. A failed final certificate downgrades an otherwise authoritative solver status.","title":"Reconstruction"},{"category":"section","location":"preprocessing/#Options","page":"Preprocessing","text":"Typical callers can leave preprocessing at its defaults:\n\nsettings = Settings{Float64}(\n    presolve=:auto,\n    scaling=:auto,\n)\nresult = optimize!(model; settings=settings)\n\nThe public structural controls are presolve=:auto|:on|:off and scaling=:auto|:none|:equilibrate. Detailed cleanup stages are one atomic presolve pipeline rather than separate public toggles.\n\nExact zero, duplicate, and proportional equalities are always verified in the problem arithmetic before they are removed. Near-proportional detection is a diagnostic only: it never changes the model or reconstruction map. Its work is bounded to eight equivalent passes over the retained equality matrix. If a large dense system would exceed that budget, SDPX records an explicit warning and skips only the approximate diagnostic; exact cleanup and the subsequent target-arithmetic rank presolve remain enabled.\n\nUse diagnostics(result).presolve.preprocessing to inspect stage timings, allocations, dimension changes, bound counts, equality cleanup, formulation costs, chordal estimates, and warnings.","title":"Options"},{"category":"section","location":"preprocessing/#Target-model-behavior","page":"Preprocessing","text":"The maintained dense bootstrap input has 6,119 variables, 482 supplied equalities, and 32 PSD blocks, but no scalar bound blocks. Its equality matrix has rank 394, and the aggregate PSD patterns are 99.84% dense even though individual coefficient matrices are sparse. Consequently:\n\nbound extraction and fixed-variable elimination make no change;\nequality presolve reduces 482 columns to 394;\nchordal analysis rejects decomposition before constructing clique data; and\nthe existing sparse-coefficient, dense-Schur numerical path remains selected.\n\nThis distinction matters: individual coefficient sparsity is not evidence that a PSD block or its final Schur complement is sparse. The canonical medium CSDR model contains 1,700 dense-pattern 2×2 PSD blocks and no explicit equalities or scalar bounds, so preprocessing is a no-change regression path there; its optimized reduced-arrow and SIMD kernels remain responsible for performance.\n\nSparse equilibration uses the same active-incidence representation. On the cluster model, copying only active matrices and sharing one read-only empty CSC matrix per block reduced the median equilibration call from 1.241 to 0.633 seconds and allocation from 685.8 to 269.7 MB; the old and new scaled-problem checksums were identical. This optimization does not share writable coefficient or BigFloat storage.","title":"Target-model behavior"},{"category":"section","location":"preprocessing/#Arithmetic-limitations","page":"Preprocessing","text":"Float64, MultiFloats.Float64x4, and BigFloat are covered by solve and reconstruction tests. BigFloat copies use independently owned MPFR storage. Preprocessing itself does not introduce shared writable values.\n\nMultiFloats 3.x does not implement the scalar multiplication network required by Float64x8, and the validated AMD EPYC nodes expose AVX2 but not AVX-512. SDPX therefore does not claim a working Float64x8 solver path. The type is not selected automatically; Float64x4 is the supported SIMD extended-precision configuration on those nodes.","title":"Arithmetic limitations"},{"category":"section","location":"providers/#Linear-algebra-providers","page":"Linear-algebra providers","text":"The SDPX solver owns all problem semantics; linear algebra is a replaceable arithmetic seam underneath it. The dependency direction is:\n\nSDPX planner / sparse symbolic structure / KKT assembly / certification\n    -> unified LA API (plan, instantiate, la_* dispatchers)\n       -> StandardLinearAlgebra (LinearAlgebra/BLAS/LAPACK)\n       -> bundled SDPXLegacyLA provider (owned legacy k* kernels)\n       -> optional MFLA provider (MultiFloatLinearAlgebra extension)\n       -> optional BFLA provider (BigFloatLinearAlgebra extension)\n          -> arithmetic libraries","title":"Linear-algebra providers"},{"category":"section","location":"providers/#Planning-and-execution","page":"Linear-algebra providers","text":"plan_la_backend and instantiate_la_backend are the only authorized entry points. Solver code requests :auto, :standard, :bfla, :multifloat, or :legacy; the planner resolves once while the ExecutionPlan is built. Numerical execution never retries another provider implicitly. Executed diagnostics record the provider, ownership contract, planned fallback reason, and runtime fallback reason; they never infer the backend from the presence of a type or a global.\n\nProvider availability may satisfy a selected formulation, but it never selects the mathematical formulation itself. The formulation planner consumes mathematical input facts and equality-basis evidence only. A capability is advertised by the SDPX extension only when both the upstream capability and the corresponding semantic adapter are present.\n\nThe plan records the selected provider/formulation pair and any authorized runtime fallback. A provider failure does not implicitly change that plan.","title":"Planning and execution"},{"category":"section","location":"providers/#Standard-and-legacy-providers","page":"Linear-algebra providers","text":"Julia's LinearAlgebra (BLAS/LAPACK) covers ordinary Float64 dense work and is the reference route. The bundled SDPXLegacyLA provider owns the historical k* kernels behind the la_* dispatcher seam for generic and BigFloat arithmetic. It advertises exactly SDPX_LEGACY_LA_CAPABILITIES: :dot, :norminf, :mul, :mul_owned, :syrk, :chol, :cholesky_factor!, :solve, :cholsolve_owned, :trsm, :trsv_lower, :trsv_transpose, :axpby, and :axpby_owned.\n\nThe solver-facing la_syrk! contract is lower-triangle-authoritative.  A provider may leave the upper triangle unchanged, poisoned, or materialized as an implementation detail; dense equality Cholesky, pivoted compatibility, and augmented assembly select the lower triangle explicitly.  The bundled legacy kernel may still mirror both triangles, but no migrated solver path relies on that extra work.  Provider tests deliberately poison the inactive upper triangle to keep this boundary auditable.\n\nCallers use la_* dispatchers, never k* directly. kdot!, kdot_columns!, alloc_zeros, and copy_owned! are implementation helpers or shared ownership infrastructure today, not provider capabilities.","title":"Standard and legacy providers"},{"category":"section","location":"providers/#MultiFloatLinearAlgebra","page":"Linear-algebra providers","text":"When the complete provider is loaded, fixed Float64x2, Float64x3, and Float64x4 dense-Cholesky plans may resolve to the MFLA extension. The ExecutionPlan remains the only selection authority; a failed MFLA operation is never retried through generic LinearAlgebra or the bundled legacy provider unless a structural fallback is already present in the plan.\n\nThe migrated ordinary dense path uses MFLA for dot, GEMV/GEMM, lower SYRK, TRSV/TRSM, Cholesky factorization, vector/multiple-RHS solves, and equality rank-revealing QR. The provider owns one reusable MFWorkspace for sequential factorizations. MFLA factors snapshot their LU/LDLT/RRQR metadata, so workspace reuse or growth does not invalidate live factors; only the destructively factorized matrix remains borrowed. Concurrent factorization must still use distinct solver/provider workspaces. Packed GEMM calls may share the provider workspace because MFLA serializes that scratch internally.\n\nMFLA also supplies ordinary residuals, normwise backward error, explicit x2 -> x3, x2 -> x4, and x3 -> x4 residuals, plus exactly one requested factor correction. SDPX still decides whether to request a promoted residual, whether to accept a correction, and whether to repeat refinement. Structured KKT residuals, cone mappings, stopping policy, and certification stay in SDPX.\n\nPivoted LDLT status, block layout, final permutation, and inertia are queried through MFLA's lightweight public accessors.  Full factor_diagnostics is a cold observability operation: an augmented factorization records it once for the accepted factor or final rejected candidate, rather than rescanning every retry for each metadata field.\n\nLU and LDLT are provider capabilities and tested internal seams. The mathematical formulation is still chosen first: an explicit augmented request, or the provider-neutral static planner, may require LDLT; only then may backend planning select MFLA. MFLA availability never causes an augmented choice by itself.","title":"MultiFloatLinearAlgebra"},{"category":"section","location":"providers/#BigFloatLinearAlgebra","page":"Linear-algebra providers","text":"The optional BFLA extension supplies provider-owned Cholesky, LU, LDLT, QR, factor-owned solve, residual/refinement primitives, triangle/precision/ ownership validation, and Native/Generic backend provenance. BFLA QR no longer has an exact-zero default rank policy; SDPX determines equality rank from its explicit relative tolerance and the packed R diagonal.\n\nOne precision-matched BFLAWorkspace is created lazily per instantiated SDPX provider and reused by sequential factorizations and predictor, corrector, and correction solves.  Because SDPX owns the factor buffer and does not mutate it between those solves, the adapter uses BFLA's explicit trusted solve boundary; the checked public BFLA semantics remain unchanged for every other caller. The workspace is never shared concurrently.  BFLA's one-step correction is used only when it is the same retained factor equation (for example the full augmented LDLT); SDPX still forms the KKT residual and owns acceptance, stopping, precision escalation, and fallback.\n\nA successful BFLA Cholesky is a factorization fact, not an equality-rank decision.  SDPX compares the public lower-factor diagonal quality against its requested-accuracy and explicit factor/problem precision, then either accepts normal equations, invokes only a plan-authorized RRQR/pivoted route, or fails closed.  Ambient MPFR precision is not part of that decision.\n\nThe following routes remain SDPX-specific formulations rather than ordinary dense provider work: native LP and fixed-trace Q3/SOCP kernels, sparse and block-arrow Schur assembly and global-ID scatter, packed block-local reduction/merge paths, BigFloat paths served by BFLA or explicit legacy rollback, and solver-local AXPBY, stopping norms, fraction-to-boundary, and iterate-update loops.","title":"BigFloatLinearAlgebra"},{"category":"section","location":"providers/#Sparse-providers","page":"Linear-algebra providers","text":"The sparse layer is a separate provider-neutral seam. Float64 uses CHOLMOD through Julia's SuiteSparse. Float64x2/Float64x3/Float64x4 and BigFloat use the arithmetic-generic simplicial sparse Cholesky. Capabilities and ownership are explicit (CHOLMOD_SPARSE_CAPABILITIES, GENERIC_SPARSE_CAPABILITIES); a changed CSC pattern fails closed, while unchanged patterns support numeric refactorization without repeating symbolic ordering. See sparse-execution.md.","title":"Sparse providers"},{"category":"section","location":"adaptive-parameter-policy/#Adaptive-Interior-Point-Parameter-Policy","page":"Adaptive parameter policy","text":"This document records the implementation audit, controller design, safety rules, and benchmark gate for SDPX's iteration-level parameter selection. The guarded adaptive policy is the public default:\n\noptions = SolverOptions{Float64}(\n    parameter_strategy=:adaptive,\n)\n\nUse parameter_strategy=:fixed only for reproducibility with the historical trajectory. Automatic cold-start selection through parameter_policy=:auto is a separate policy that runs one generic automatic Mehrotra controller after scaling and before the iteration-level policy. The public resolver recommended_parameters reports profile=:post_scaling_mehrotra; the immutable execution plan records the neutral, deferred identity :automatic_mehrotra; and executed diagnostics record the post-scaling resolution as :post_scaling_mehrotra. An explicit fixed policy is recorded as :user_fixed. The adaptive controller itself returns fixed parameters while the normalized cold-start merit is at least one, and it permanently falls back after repeated instability.","title":"Adaptive Interior-Point Parameter Policy"},{"category":"section","location":"adaptive-parameter-policy/#Audit-of-the-existing-Newton-method","page":"Adaptive parameter policy","text":"For each PSD block, SDPX solves\n\nA_l(x) - C_l = X_l >= 0\nc - A*(Y) - B*y = 0\nB'*x = b\nX_l*Y_l = 0\n\nAt the current iterate it forms the primal matrix residual P_l=A_l(x)-C_l-X_l, the equality residual p=b-B'*x, and the dual residual d=c-A*(Y)-B*y. Linearizing complementarity gives\n\ndX_l = A_l(dx) + P_l\nX_l*dY_l + dX_l*Y_l = R_l\n\nEliminating dY_l produces the Schur terms <A_i, X_l^-1*A_j*Y_l>. SDPX factors the resulting Schur/equality system once per outer iteration and reuses that factor for predictor and corrector right-hand sides. Block directions are recovered after each solve. This is the HRVW/KSH/M-type direction used by the original code, implemented with symmetric-square or structure-specific Schur kernels.\n\nThe historical fixed SDP path is a predictor/corrector path, but it is not canonical Mehrotra in every phase:\n\npredictor=:classic includes the current block target mu_l*I in the predictor.\npredictor=:sdpb switches to a pure affine target -X_l*Y_l only after primal and dual residuals are already below tolerance.\nthe historical corrector includes -dX_aff*dY_aff, but chooses a block-local centering multiplier from a unit trial and the fixed beta.\n\nThe dedicated LP path was already a conventional Mehrotra predictor-corrector: it computes affine primal and dual boundary steps, mu_aff, a second-order corrector, and independent accepted primal and dual steps.\n\nThe adaptive SDP path now uses the same canonical sequence:\n\nbuild a pure affine predictor;\ncompute exact or bisected PSD boundary steps;\ncompute global average complementarity mu=sum_l <X_l,Y_l>/sum_l k_l;\nevaluate mu_aff at the independent affine boundary steps;\nselect bounded iteration parameters;\nbuild R_l=sigma*mu*I-X_l*Y_l-dX_aff_l*dY_aff_l;\nsolve the corrector with the existing factor;\nchoose independent primal and dual feasible steps.","title":"Audit of the existing Newton method"},{"category":"section","location":"adaptive-parameter-policy/#Existing-parameters-and-defaults","page":"Adaptive parameter policy","text":"Quantity Default Fixed behavior Adaptive behavior\nbeta (β) 0.1 Block complementarity reduction/centering target Fallback value; result.parameter_history.beta aliases the selected sigma\ngamma (γ) 0.9 Backtracking contraction; also the exact fraction-to-boundary safety Fallback value; split into two fractions and one backtracking factor\nsigma (σ) no separate option Equal to beta Selected from mu_aff/mu, centrality, and recent progress\nmu (μ) derived Per-block target is beta*<X_l,Y_l>/k_l Global average complementarity is used by the canonical corrector\nstep rule :auto Exact fraction-to-boundary for blocks no larger than 2x2; otherwise PSD backtracking Same structural rule, with separately selected primal and dual safety values\nrefinement :auto, cap 8 Residual-driven unless a narrow exact-factorization skip is safe Selector may reduce the cap on a clean factor and restores it on a difficult factor\ntolerances 1e-10 Relative gap, primal residual, and dual residual Unchanged\niteration limit 200 Hard outer-iteration limit Unchanged\n\nparameter_policy=:auto runs the generic automatic Mehrotra controller before the iteration-level policy. It selects no benchmark-, size-, cone-, or precision-specific parameter profile: beta, gamma, predictor, and parameter_strategy keep the SolverOptions defaults or user choices. After presolve and scaling, LP, SDP, and NativeSOC use their existing planned KKT route to solve one primal and one dual affine right-hand side, followed by typed cone-interior shifts and deterministic complementarity mass balancing. The automatic path does not use OmegaP or OmegaD. The public resolver reports profile=:post_scaling_mehrotra; the immutable execution plan records only the deferred :automatic_mehrotra identity; and the executed solve records :post_scaling_mehrotra. parameter_policy=:fixed uses the supplied values exactly and records :user_fixed.\n\nThis document concerns iteration parameters. The storage and structure classifications reported by StructureAnalysis.profile (for example :sparse_coefficients_dense_psd_dense_schur) describe data layout and kernel selection, not iteration parameters; they are unaffected by the generic controller.\n\nAfter initialization, NativeSOC retains its compact Lorentz-specific mu_aff / mu centering rule and exact cone boundary kernel. The new affine KKT cold start only replaces the old automatic identity heads; it does not replace that iteration controller. FixedTraceQ3 uses the same Lorentz margin and mass-balancing policy through its verified tail-map representation.","title":"Existing parameters and defaults"},{"category":"section","location":"adaptive-parameter-policy/#Existing-numerical-safeguards","page":"Adaptive parameter policy","text":"Dense and reduced-arrow Schur factors first try an unregularized Cholesky. Failure triggers relative diagonal shifts beginning at sqrt(eps(T)) and increasing by a factor of ten, with six bounded attempts.\nThe LP KKT path starts at max(sqrt(eps(T)), 1e-12), decays a previously successful shift by ten, and escalates a failing factor by ten for at most eight attempts.\nEquality Gram factors use rank-revealing pivoted Cholesky when the ordinary factor is numerically unacceptable.\nIterative refinement uses a target-type residual and stops on achieved residual reduction. Mixed-precision KKT paths have separate condition, correction-budget, monotonicity, and native-fallback guards.\nTermination uses normalized primal residual, normalized dual residual, and relative gap, followed by certification in the original coordinates. AlmostOptimal distinguishes a relaxed certificate, and InsufficientPrecision distinguishes an arithmetic floor from algorithmic failure.\nA moving-window stagnation detector monitors tolerance-normalized residual, gap, and complementarity progress.\nBigFloat working_precision_policy=:auto is an outer staged solve: it may retry at the requested precision after failed certification. It does not mutate MPFR precision in the middle of a Newton iteration.","title":"Existing numerical safeguards"},{"category":"section","location":"adaptive-parameter-policy/#Policy-component","page":"Adaptive parameter policy","text":"The implementation is isolated in src/adaptive_parameters.jl:\n\nabstract type AbstractParameterPolicy end\n\nstruct FixedParameterPolicy{T} <: AbstractParameterPolicy\n    # Exact historical values\nend\n\nstruct AdaptiveParameterPolicy{T} <: AbstractParameterPolicy\n    # Bounds, fallback, and controller settings\nend\n\nselect_parameters(policy, diagnostics, history)::IterationParameters\n\nselect_parameters is pure and does not mutate the solver. The mutable AdaptiveIPMController only owns history and the one-way fallback state. IterationDiagnostics records:\n\nnormalized primal and dual residuals;\nrelative gap, mu, and mu_aff;\naffine and previous accepted primal/dual steps;\nbacktracking and refinement counts;\nrelative regularization and a Cholesky-diagonal quality proxy;\npredicted and observed feasibility reduction;\nprimal and dual block Cholesky-margin proxies;\nwhether the current metrics are at the arithmetic precision floor.\n\nIterationParameters returns sigma, independent primal and dual fraction-to-boundary values, the backtracking factor, regularization hints, the refinement target/cap, the centrality target, and fallback provenance. Every accepted iteration is copied into result.parameter_history.","title":"Policy component"},{"category":"section","location":"adaptive-parameter-policy/#Conservative-selection-rules","page":"Adaptive parameter policy","text":"All constants are constructed in the solver arithmetic; BigFloat and MultiFloat diagnostics are never narrowed through Float64.\n\nsigma is based on (mu_aff/mu)^p, with p=3 after a good affine step and p=2 after a short step.\nThe candidate is blended as 80% current estimate and 20% previous sigma.\nsigma is normally bounded by [0.02, 0.50], expanded only as necessary to include the configured fixed fallback. The generic automatic controller uses the 0.50 upper bound; the expert adaptive_sigma_max option can reproduce or override this selection.\nShort affine steps, residual growth, frequent backtracking, or poor factors impose centrality floors of 0.08 or 0.20.\nThe primal and dual fraction-to-boundary values are selected independently in [0.80, 0.99], again expanded only to retain the fallback. Small recent steps and repeated backtracking make them more conservative.\nThe backtracking contraction is selected in [0.55, 0.90], with the configured fallback always representable.\nClean factors can use at most two refinement passes. A poor factor or a previous correction restores the configured cap.\nNon-finite diagnostics, an explicit rank-revealing equality factorization, or repeated unstable outcomes switch permanently to the complete fixed path for the remainder of the solve. Severe complementarity or feasibility growth triggers that recovery without waiting for a second event.\n\nThe fallback restores the fixed predictor construction as well as fixed beta, gamma, corrector, step, and refinement behavior. This distinction is important: changing only the scalar values while leaving the affine predictor enabled is not a true rollback.\n\nRegularization and precision decisions remain deliberately layered. The policy observes and records their state, while the factorization kernels keep their failure-driven regularization and the outer BigFloat driver keeps its certification-driven precision retry. This avoids refactoring a current factor from parameters selected only after its affine predictor has been computed.","title":"Conservative selection rules"},{"category":"section","location":"adaptive-parameter-policy/#Arithmetic-behavior","page":"Adaptive parameter policy","text":"Float64 uses BLAS-backed factors and the same typed controller calculations.\nMultiFloats, including Float64x4, use exact-type ratios and bounds. Existing owned blocked kernels and mixed-precision fallbacks are unchanged.\nBigFloat selection executes at the active MPFR precision. Native BigFloat remains ownership-safe; policy selection does not create shared mutable matrix entries or add threaded MPFR writes.\n\nThe formulas are the same across arithmetic types. Only eps(T), factor quality, refinement thresholds, precision-floor detection, and existing backend selection differ.","title":"Arithmetic behavior"},{"category":"section","location":"adaptive-parameter-policy/#Validation-policy","page":"Adaptive parameter policy","text":"Automatic-controller changes are accepted only after the same implementation passes LP, SDP, NativeSOC, dense/sparse/equality, and arithmetic-type regression gates. Every gate compares status, iterations, residuals, relative gap, original-coordinate certification, executed provider/formulation provenance, and parameter history. Performance claims require repeated same-node runs; an improvement on one benchmark never creates a new automatic branch.\n\nHistorical benchmark-specific sweeps remain in dated benchmark reports rather than this maintained policy document. They can be reproduced with explicit beta, gamma, OmegaP, OmegaD, predictor, scaling, and adaptive_sigma_max options, but they do not influence automatic selection.","title":"Validation policy"},{"category":"section","location":"adaptive-parameter-policy/#Remaining-limitations","page":"Adaptive parameter policy","text":"The SDP line search certifies cone interiority. It does not recompute the complete residual or merit at every trial because that would repeat the dominant contractions; observed reduction is checked at the next Newton residual evaluation.\nPSD and factorization margins are inexpensive Cholesky-diagonal proxies, not per-iteration minimum eigenvalue computations. Exact eigenvalue checks remain part of final validation.\nThe returned SDP regularization fields are controller hints and diagnostics; current-factor regularization remains failure-driven inside the KKT backend.\nBigFloat precision escalation is between complete solve attempts rather than within one factorization.","title":"Remaining limitations"},{"category":"section","location":"adaptive-parameter-policy/#Public-algorithm-references","page":"Adaptive parameter policy","text":"The design uses published behavior and public documentation, not undocumented commercial heuristics:\n\nS. Mehrotra, “On the implementation of a primal-dual interior point method,” SIAM Journal on Optimization 2(4), 1992.\nR. D. C. Monteiro and Y. Zhang, “A unified analysis for a class of path-following primal-dual interior-point algorithms for semidefinite programming,” Mathematical Programming 81, 1998.\nK. C. Toh, M. J. Todd, and R. H. Tütüncü, SDPT3 implementation paper.\nSDPA documentation and source.\nSeDuMi reference guide.\nSDPB source and papers.\nClarabel solver settings and implementation paper.\nMOSEK conic termination criteria and public parameter reference.","title":"Public algorithm references"},{"category":"section","location":"parameters/#Parameters","page":"Parameters","text":"The public policy is typed and attached to the same Model that is passed to optimize!:\n\nmodel = SDPX.Model(Float64)\nx = SDPX.variable!(model, :x, 1; domain=SDPX.Nonnegative())\nSDPX.constraint!(model, :lower, x[1] - 1, SDPX.Nonnegative())\nSDPX.objective!(model, SDPX.Minimize(), x[1])\nsettings = SDPX.Settings(\n    model;\n    tolerances=SDPX.Tolerances(Float64; primal=1e-8, dual=1e-8, gap=1e-8),\n    limits=SDPX.Limits(iterations=200, time=Inf, threads=1),\n    algorithm=:auto,\n    presolve=:auto,\n    scaling=:auto,\n    sparse=:auto,\n    diagnostics=:summary,\n    certification=true,\n    verbosity=1,\n)\nresult = SDPX.optimize!(model; settings=settings)\n\nSettings{T} accepts only typed policy fields: tolerances, limits, scaling, formulation, provider, presolve, algorithm, sparse, equality_solver, working_precision_policy, diagnostics, verbosity, timing, certification, and blas_threads. Route selection is pure and fail-closed: :lp is valid only for a pure LP model, :socp only for a pure SOC/RSOC model, and :sdp only for a pure SDP model; :auto follows the classified family. Public formulation values are :auto, :variable_space_schur, and :dense_augmented_kkt.\n\nThe default controller adapts centering, fraction-to-boundary values, backtracking, and refinement from the measured Newton iteration. Presolve, scaling, kernel selection, working precision, and scheduling also default to automatic policies. The detailed tables below retain the mature SolverOptions{T} field names for algorithm and compatibility notes only; that qualified low-level record is not the public quickstart interface.","title":"Parameters"},{"category":"section","location":"parameters/#Interior-point-method-and-initialization","page":"Parameters","text":"Parameter Default Meaning\nβ 0.1 Fixed SDP centering/complementarity reduction target and the safe fallback for adaptive sigma.\nγ 0.9 Fixed backtracking reduction factor and exact fraction-to-boundary safety.\nΩp 1 Expert fixed-policy primal PSD identity scale. Ignored by the default automatic KKT cold start.\nΩd 1 Expert fixed-policy dual PSD identity scale. Ignored by the default automatic KKT cold start.\npredictor :classic Predictor rule: :classic or :sdpb.\nrefine_steps 1 Number of iterative-refinement passes for the KKT predictor/corrector solutions.\nstep_rule :auto :backtrack, exact 2x2-optimized :fraction_to_boundary, or :auto.\nparameter_policy :auto Automatic cold-start Mehrotra controller; :fixed preserves supplied values exactly.\nparameter_strategy :adaptive Guarded per-iteration Mehrotra policy with fixed fallback when cold-start or stability diagnostics are unreliable.\nadaptive_sigma_max 0 Expert adaptive-centering cap; zero uses the generic 0.5 maximum.\nrefine_policy :auto :auto/:adaptive stop KKT refinement from its residual; :fixed runs exactly refine_steps passes.\n\nWith parameter_policy=:auto, the automatic Mehrotra controller keeps β, γ, predictor, and parameter_strategy from the defaults or user choices. After presolve and scaling, the solver builds an identity-metric KKT system, solves one primal and one dual affine right-hand side with the same factor, then shifts the cone variables in their native coordinates. A minimal identity shift raises orthant/PSD starts, and Lorentz sides still at the typed cone-vertex envelope, to unit identity mass before complementarity cross-centering; bounded structured correction reuses an accepted SDP factor when an original-KKT residual remains above the existing gate. Ωp and Ωd do not participate in this path. The public compatibility resolver reports profile=:post_scaling_mehrotra, the plan records the deferred identity :automatic_mehrotra, and executed diagnostics record :post_scaling_mehrotra plus the separate cold-start report. parameter_policy=:fixed uses the supplied values exactly and records :user_fixed.","title":"Interior-point method and initialization"},{"category":"section","location":"parameters/#Convergence-and-stopping","page":"Parameters","text":"Parameter Default Meaning\nϵ_gap 1e-10 Relative primal-dual gap tolerance.\nϵ_primal 1e-10 Primal residual tolerance.\nϵ_dual 1e-10 Dual residual tolerance.\ntermination :relative Scale-normalized stopping tests; :legacy is the legacy absolute/nonnegative-gap convention.\niter_max 200 Maximum outer iterations (legacy keyword iterMax).\nmax_time Inf End-to-end wall-clock limit in seconds, including automatic-pipeline setup and model compilation.\ncallback nothing Called after every iteration; returning true stops with UserStopped.","title":"Convergence and stopping"},{"category":"section","location":"parameters/#Restarts-and-numerical-safeguards","page":"Parameters","text":"Parameter Default Meaning\nrestart true Rescale the collapsed side and continue after step-size collapse.\nmin_step 1e-10 A backtracking step below this value triggers a convergence-tail check or restart.\nomega_step 1e5 Per-restart multiplier applied to the collapsed X or Y side.\nmax_restarts 5 Maximum restarts in the new API.\nmax_omega 1e50 Compatibility field for the qualified legacy engine; public Settings has no equivalent.\n\nFor fixed-exponent types such as Float64x4, SDPX limits the effective restart multiplier and returns NumericalBreakdown when an iterate becomes non-finite.","title":"Restarts and numerical safeguards"},{"category":"section","location":"parameters/#Precision,-equilibration,-and-storage","page":"Parameters","text":"Parameter Default Meaning\nprecision_bits 997 Working precision for BigFloat only.\nworking_precision_policy :auto May start lower and retry at precision_bits unless certification passes.\nminimum_working_precision_bits 192 Lower bound for the staged BigFloat selector.\nconvert_inputs false Normalize independent BigFloat storage to precision_bits; cannot recover lost digits.\nscaling :auto LP geometric scaling and adaptive-pass Ruiz congruence/variable scaling for SDP.\nsparse :auto Storage selection during model compilation; distinguishes sparse coefficients from aggregate PSD/Schur density.\nformulation :auto Static dense KKT formulation selection (:normal_equations, :augmented, :primal).\nlinear_algebra_backend :auto Resolves once during planning: :standard, :bfla, :multifloat, :legacy.\nextended_precision_blas type-dependent Conservative :auto for fixed-width extended types and BigFloat, :off for Float64.\nextended_precision_memory_fraction 0.10 Maximum fraction of available memory for packed extended-precision panels.\nmixed_precision_kkt type-dependent :auto for BigFloat and fixed-width extended arithmetic, :off for Float64.\nmixed_precision_condition_limit type-dependent 1e14 for Float64x4, 1e8 otherwise.\nmixed_precision_refine_max_steps 32 Maximum correction solves before native fallback.\nmixed_precision_memory_fraction 0.10 Maximum fraction of available memory for Float64 factors and conversion scratch.\nequality_solver :auto Normal equations with rank-revealing QR fallback when factor diagnostics justify it.\nforce_gc false Run a full collection after each accepted iteration and return free allocator pages where supported.\n\nUnless refine_tol is explicitly positive, dense mixed-precision refinement uses max(64 * eps(T), min(ϵ_gap, ϵ_primal, ϵ_dual)^2). Native KKT refinement normally targets 64 * eps(T); the large regularized sparse Float64 SDP route uses a looser tolerance retaining two guard digits beyond the requested certificate. A positive user-supplied refine_tol always takes precedence.\n\nSparse equilibration rebuilds derived sparse caches after scaling. On the public route, request it with Settings(model; scaling=:equilibrate); the model and all warm-start values remain in their original coordinates:\n\nsettings = SDPX.Settings(model; scaling=:equilibrate)\nresult = SDPX.optimize!(model; settings=settings)\n\nWarm starts are supplied in original input coordinates. SDPX maps x0, X0, and Y0 through the selected equilibration and maps y0 through equality presolve as needed; callers should not pre-scale them.","title":"Precision, equilibration, and storage"},{"category":"section","location":"parameters/#Output,-timing,-and-checkpoints","page":"Parameters","text":"Parameter Default Meaning\nverbosity 1 0 is silent; values of 1 or higher print iteration information.\ntiming false Records total and phase-level timing.\ncheckpoint_every 0 Save an iterate-level warm-restart checkpoint every N iterations; 0 disables.\ncheckpoint_path \"\" Atomic checkpoint destination used by the SDP path.\nmode OPTIMIZE OPTIMIZE or the internal feasibility mode FEASIBILITY.\n\nResume restores the primal/dual iterate, centering targets, and iteration/restart counters. It intentionally reinitializes adaptive-parameter history, stagnation windows, phase timers, and best-iterate history; a resumed adaptive solve is therefore not bit-for-bit equivalent to an uninterrupted run. Checkpoint resume is not currently supported by the dedicated LP path.","title":"Output, timing, and checkpoints"},{"category":"section","location":"parameters/#Qualified-compatibility-defaults","page":"Parameters","text":"The mature low-level engine retains historical names such as sdp, SolverOptions, β, γ, Ωp, and Ωd for compatibility and algorithm audits. Those names are qualified implementation details; they are not additional v0.5 public entry points. Public code should express stopping and resource policy with Tolerances, Limits, and Settings as shown above.\n\nThe full Newton-method audit, exact diagnostic fields, controller bounds, fallback rules, arithmetic behavior, and fixed-versus-adaptive results are in Adaptive Interior-Point Parameter Policy.","title":"Qualified compatibility defaults"},{"category":"section","location":"benchmarks/#Benchmarks-and-evidence","page":"Benchmarks and evidence","text":"SDPX keeps measured decisions close to the code or benchmark that produced them. This page records where the evidence lives and how to read it; it does not repeat every table.","title":"Benchmarks and evidence"},{"category":"section","location":"benchmarks/#Benchmark-locations","page":"Benchmarks and evidence","text":"bench/soc_fixed_trace/ — the native fixed-trace Q3 versus PSD2 campaign, including models, launcher policy, certificate checks, and provenance.\nbench/RESULTS.md — retained historical context and the small smoke tier index.\nbench/automatic_pipeline/PARAMETER_SELECTION.md — LP cold-start crossover and distant-start sweeps.\nbench/threading/RESULTS.md — Schur scheduling kernel measurements.\nbench/mixed_precision_kkt/RESULTS.md — mixed-precision KKT promotion evidence.\nbench/opt2026/ — native BigFloat optimization, preprocessing, and cluster campaign reports.","title":"Benchmark locations"},{"category":"section","location":"benchmarks/#Reporting-policy","page":"Benchmarks and evidence","text":"Performance claims must state the model, arithmetic, tolerances, hardware, thread configuration, warm-up, timing boundary, numerical result, and repeated-run statistic. The independent original-coordinate certificate is the validation boundary: a row without a valid certificate is not a solve claim. Benchmark results are workload-specific evidence and must not be generalized to other solvers, hardware, tolerances, or problem families.\n\nMeasurements that gate a production default are recorded in the code at the constant they gate. Changing one requires re-running the sweep, not one benchmark. The fixed-trace campaign, for example, records that the native Q3 specialization is now selected by structure whenever its exact reduction verifies; its matched certificate/performance gates remain documented with the benchmark, not encoded as size gates in the planner.","title":"Reporting policy"},{"category":"section","location":"benchmarks/#Native-SOC-evidence","page":"Benchmarks and evidence","text":"The lightweight scoreboard in the SOC reports warms each route once and then measures fresh solves. The native fixed-trace specialization is primarily a dimensional reduction plus local elimination: for many independent fixed-head Q3 blocks it uses a fraction of the GeneralLorentz workspace and is substantially faster, while the PSD reference is retained only as a correctness and comparison route. At very small block counts the specialization's dispatch overhead can outweigh its structural saving, which is why the automatic selection is structural rather than a blanket performance preference.\n\nThe Q3 HKM direction remains the validated default after matched gates; the Nesterov–Todd direction is an explicit research option. Historical PSD2- versus-Q3 campaign evidence is arithmetic-dependent: in the retained matched BigFloat measurements the PSD2 reference won on total runtime, but that is campaign evidence, not a current route policy. NativeSOC BigFloat general and fixed-trace execution are production and tested today.","title":"Native SOC evidence"},{"category":"section","location":"benchmarks/#Adaptive-parameter-evidence","page":"Benchmarks and evidence","text":"The adaptive parameter controller and its fixed fallback are documented in docs/adaptive-parameter-policy.md, with equations, exact bounds, and arithmetic-specific behavior. Measured fixed-versus-adaptive decisions appear in the benchmark locations above.","title":"Adaptive parameter evidence"},{"category":"section","location":"bridge-schema/#SDPX-bridge-schema-(version-1)","page":"Bridge schema","text":"The language-independent contract between SDPX and foreign runtimes. The first transport is a command-line bridge (bin/sdpx_solve.jl, used by mathematica/SDPXLink.wl); the schema — not the transport — is the stable part, and it is versioned so a future persistent-server or LibraryLink transport can speak the same files.\n\nconsumer ── problem.json ──▶  julia --project=bin bin/sdpx_solve.jl in.json out.json  ──▶ result.json\n\nExit code 0 means result.json contains a solve outcome; any other exit code means it contains a structured error. The result file is written in both cases — a consumer never has to parse a Julia stack trace.","title":"SDPX bridge schema (version 1)"},{"category":"section","location":"bridge-schema/#Numbers","page":"Bridge schema","text":"Every numeric field accepts either a JSON number or a string in scientific notation (\"4.898979485566356196394568149411e0\"). Strings are the only safe carrier above Float64: JSON numbers are IEEE doubles in most parsers and silently round anything wider. The bridge always emits strings. BigFloat strings are parsed at settings.precision_bits, so digits beyond that precision are rounded, not lost silently at 53 bits.","title":"Numbers"},{"category":"section","location":"bridge-schema/#Problem-file","page":"Bridge schema","text":"{\n  \"sdpx_schema\": 1,                  // required, exactly 1\n  \"precision\": \"auto\",               // auto | Float64 | Float64x2/x3/x4 | BigFloat\n  \"objective\": [2.0, 3.0],           // c, length m\n  \"blocks\": [                        // one per PSD block: Σᵢ xᵢ Aᵢ − C ⪰ 0\n    {\n      \"dimension\": 2,\n      \"constant\":     {\"rows\": [1,2], \"cols\": [2,1], \"values\": [1.0, 1.0]},   // C, sparse COO, 1-based\n      \"coefficients\": [              // one entry per variable with nonzeros\n        {\"variable\": 1, \"rows\": [1], \"cols\": [1], \"values\": [1.0]},\n        {\"variable\": 2, \"rows\": [2], \"cols\": [2], \"values\": [1.0]}\n      ]\n    }\n  ],\n  \"equalities\": {                    // optional: Bᵀx = b, B is m×n in COO\n    \"rows\": [1], \"cols\": [1], \"values\": [1.0], \"rhs\": [0.5]\n  },\n  \"settings\": {\n    \"dualityGapThreshold\": \"auto\",   // or e.g. \"1e-80\"\n    \"primalErrorThreshold\": \"auto\",\n    \"dualErrorThreshold\": \"auto\",\n    \"maximumIterations\": \"auto\",\n    \"maxRuntime\": \"auto\",\n    \"threads\": \"auto\",\n    \"verbosity\": \"auto\",\n    \"precision_bits\": \"auto\",        // BigFloat only; CLI --precision=N sets this\n    \"algorithm\": \"auto\",\n    \"presolve\": \"auto\",\n    \"scaling\": \"auto\",\n    \"sparse\": \"auto\",\n    \"return_matrices\": false,        // include X/Y blocks in the result\n    \"certificate\": true              // include the independent certificate\n  }\n}\n\nCOO matrices are dense on arrival — the schema carries structure, the solver decides storage. Matrices are stored as written; symmetry is the caller's responsibility (SDPX's ingest symmetrizes within its tolerance and errors on gross asymmetry).","title":"Problem file"},{"category":"section","location":"bridge-schema/#Result-file","page":"Bridge schema","text":"{\n  \"sdpx_schema\": 1,\n  \"success\": true,                   // false ⇒ only \"status\"/\"error\" are meaningful\n  \"error\": null,\n  \"precision\": \"Float64\",\n  \"status\": \"Optimal\",               // SDPX SolveStatus name as a string\n  \"optimal\": true,\n  \"message\": \"\",\n  \"objective\": \"4.89897950663398\",   // all numbers as strings\n  \"dual_objective\": \"…\", \"relative_gap\": \"…\",\n  \"primal_residual\": \"…\", \"dual_residual\": \"…\",\n  \"iterations\": 17,\n  \"x\": [\"…\"], \"y\": [\"…\"],\n  \"certificate\": {                   // present unless settings.certificate=false\n    \"valid\": true, \"gap\": \"…\",\n    \"primal_residual\": \"…\", \"dual_residual\": \"…\",\n    \"primal_psd\": true, \"dual_psd\": true, \"failures\": []\n  },\n  \"X\": [[\"…\"]], \"Y\": [[\"…\"]],        // only with return_matrices; column-major\n  \"block_dimensions\": [2]            //   flattened blocks, reshape k×k\n}\n\nstatus is honest: an iteration-limited solve reports \"IterLimit\" with optimal: false and success: true — a solver outcome, not a bridge error. success: false is reserved for \"no solve happened\": unreadable input, schema violations, unknown precision, internal errors.","title":"Result file"},{"category":"section","location":"bridge-schema/#Upgrade-path-(deliberately-not-implemented-yet)","page":"Bridge schema","text":"One process per solve costs Julia startup plus package load (~3–5 s) per call, which is negligible against high-precision solves and dominant for tiny ones. When that matters, the planned steps — in order, each speaking this same schema — are:\n\nPersistent server: a long-lived Julia process reading length-prefixed schema-v1 JSON over stdin/stdout or a socket; the Mathematica side keeps RunProcess semantics behind the same SDPXOptimize signature.\nLibraryLink / WSTP: in-process transport for zero-copy of large dense blocks; the schema then only describes the layout, not the bytes.\n\nConsumers that stick to the file contract above will not notice the change.","title":"Upgrade path (deliberately not implemented yet)"},{"category":"section","location":"bridge-schema/#All-auto-policy-and-SDPB-style-CLI","page":"Bridge schema","text":"Schema-v1 remains backward compatible with the original tolerance, maximum_iterations and time_limit keys.  New code should prefer the independent threshold names above.  Missing policy fields resolve through the same SolveOptions midend used by the Julia API.\n\nThe friendly wrapper bin/sdpx.jl accepts:\n\nsdpx problem.json result.json \\\n  --precision=840 \\\n  --dualityGapThreshold=1e-80 \\\n  --primalErrorThreshold=1e-80 \\\n  --dualErrorThreshold=1e-80\n\nThe response additionally contains resolved_options and a compact plan record (algorithm, storage/classification, KKT backend, Gram kernel, schedule and threads).","title":"All-auto policy and SDPB-style CLI"},{"category":"section","location":"diagnostics/#Diagnostics-and-certificates","page":"Diagnostics and certificates","text":"optimize! returns the single typed v0.5 result. Terminal state and the compact original-coordinate certificate are always available through public accessors:\n\nresult = optimize!(model; settings=settings, outputs=outputs)\nstatus(result)\n\ncert = certificate(result)\ncert.available\ncert.valid\ncert.reason\ncert.primal_objective\ncert.dual_objective\ncert.primal_residual\ncert.dual_residual\ncert.relative_gap\n\nAn optimal core status is exposed as :optimal only when the independent original-coordinate certificate is valid. The certificate checks affine equations, cone membership, dual stationarity, objectives, and gap after all presolve, scaling, and reconstruction maps have been undone. Non-finite or uncertified results fail closed rather than acquiring an optimal public status.\n\nA stopped solve keeps its non-optimal core status. Absence of an optimal certificate is never interpreted as feasibility or infeasibility.","title":"Diagnostics and certificates"},{"category":"section","location":"diagnostics/#Retention-policy","page":"Diagnostics and certificates","text":"Outputs controls the potentially large result payloads. Requested primal, constraint-dual, and dual-slack entries are read with value, dual, and dual_slack. Objectives use primal_objective and dual_objective. Accessing a payload that was not retained raises ResultFieldNotRetained instead of re-solving or returning a placeholder.\n\nRetention applies to the returned Result; it does not shrink the numerical workspace used during the solve. Iteration history is route-dependent and may be empty even when retained if the selected core does not publish iteration records.\n\noutputs = Outputs(\n    :all,\n    :all,\n    :all;\n    objectives=true,\n    certificate=:summary,\n    diagnostics=:full,\n    history=true,\n    trace=true,\n)","title":"Retention policy"},{"category":"section","location":"diagnostics/#Execution-provenance","page":"Diagnostics and certificates","text":"execution_plan(result) returns the immutable route/formulation/provider plan used by the solve. When diagnostics were retained, diagnostics(result) adds classification, presolve facts, planned-versus-executed algorithms, workspace and memory estimates, phase timings, warnings, and fallback provenance. iteration_history(result) and performance_trace(result) return their retained payloads.\n\nAutomatic BigFloat precision staging is one predeclared same-route ladder, not an alternate cone formulation. Full diagnostics retain its executed rungs and each rung's child execution plan. See architecture, parameters, and precision for the planning and arithmetic invariants.","title":"Execution provenance"},{"category":"section","location":"cluster-workflow/#Cluster-Deployment-and-Execution","page":"Cluster workflow","text":"This guide describes a generic single-node PBS workflow for SDPX. It keeps source releases immutable, preserves previous campaigns, separates the Julia environment from the source tree, and promotes a release only after numerical validation. Site-specific accounts, login hosts, filesystem paths, module commands, and node topology belong in private operator documentation.\n\nThe examples use PBS (qsub, qstat, and qdel) and its conventional normal queue. Adapt resource syntax if the local PBS installation differs. Do not place passwords, private keys, access tokens, complete process environments, or private site topology in source control or result logs.","title":"Cluster Deployment and Execution"},{"category":"section","location":"cluster-workflow/#Required-site-variables","page":"Cluster workflow","text":"Define these values in the shell or a private site-environment helper before using the examples:\n\nexport CLUSTER=pbs-login\nexport SDPX_REMOTE_BASE=/absolute/path/to/SDPX.jl\n# export SDPX_SITE_ENV=/absolute/path/to/site-environment.sh  # optional\nexport JULIA_BIN=/absolute/path/to/julia  # unless SDPX_SITE_ENV defines it\n\nCLUSTER is an SSH alias or login hostname.\nSDPX_REMOTE_BASE is a new canonical deployment root owned by the user.\nSDPX_SITE_ENV is optional; it may load compiler/runtime modules and define JULIA_BIN or an existing shared JULIA_DEPOT_PATH.\nJULIA_BIN must resolve to the Julia executable used on login and compute nodes.\n\nAlways pass an explicit --project=...; a site helper may otherwise select an unrelated default project. Never reuse an older solver checkout as the release root or overwrite its manifests. Historical inputs may be copied or referenced only after their documented size and checksum have been verified.","title":"Required site variables"},{"category":"section","location":"cluster-workflow/#Immutable-release-layout","page":"Cluster workflow","text":"Use one directory per full Git commit:\n\n$SDPX_REMOTE_BASE/\n├── current -> releases/<full-commit>/source\n├── data/\n│   └── csdr-40-4-20-100/\n├── depot/\n├── incoming/\n├── jobs/\n│   └── <full-commit>/\n├── releases/\n│   └── <full-commit>/\n│       ├── environment/\n│       │   ├── Project.toml\n│       │   └── Manifest.toml\n│       ├── metadata/\n│       │   ├── archive-sha256.txt\n│       │   ├── commit.txt\n│       │   ├── environment-sha256.txt\n│       │   └── previous-current.txt\n│       └── source/\n└── results/\n    └── <full-commit>/\n        └── <PBS-job-id>/\n\nThe source directory is exactly one git archive. The generated environment is separate so package resolution never modifies the source tree. A job writes only below its unique result directory. The current symlink is an activation pointer, not a working checkout.\n\nThis layout has three useful properties:\n\na result names the exact source commit and resolved Julia environment;\na failed candidate cannot corrupt the active release;\nrollback changes one symlink and does not delete releases or results.","title":"Immutable release layout"},{"category":"section","location":"cluster-workflow/#Prepare-a-release-locally","page":"Cluster workflow","text":"Deploy only committed source. First confirm the intended branch, tests, and working tree:\n\ncd /path/to/SDPX.jl\ngit status --short --branch\ngit diff --check\ngit rev-parse HEAD\n\nDo not archive a dirty tree. After the final commit is available, create an archive whose top-level directory is source:\n\nCOMMIT=\"$(git rev-parse HEAD)\"\nARCHIVE=\"/tmp/sdpx-${COMMIT}.tar.gz\"\n\ngit archive \\\n  --format=tar.gz \\\n  --prefix=source/ \\\n  --output=\"$ARCHIVE\" \\\n  \"$COMMIT\"\n\nARCHIVE_SHA=\"$(shasum -a 256 \"$ARCHIVE\" | awk '{print $1}')\"\nprintf 'commit=%s\\narchive=%s\\nsha256=%s\\n' \\\n  \"$COMMIT\" \"$ARCHIVE\" \"$ARCHIVE_SHA\"\n\nThe archive intentionally excludes ignored benchmark inputs and results. Move large inputs separately and verify them by content hash.","title":"Prepare a release locally"},{"category":"section","location":"cluster-workflow/#Install-a-candidate-without-overwriting-anything","page":"Cluster workflow","text":"Inspect the configured target before the first write:\n\n: \"${CLUSTER:?set CLUSTER to the PBS login SSH alias}\"\n: \"${SDPX_REMOTE_BASE:?set SDPX_REMOTE_BASE to the deployment root}\"\n\nssh \"$CLUSTER\" \\\n  \"test ! -e '$SDPX_REMOTE_BASE/releases/$COMMIT' &&\n   test ! -e '$SDPX_REMOTE_BASE/releases/$COMMIT.tmp'\"\n\nCreate only the new canonical root and its children:\n\nssh \"$CLUSTER\" \\\n  \"mkdir -p \\\n    '$SDPX_REMOTE_BASE/releases' \\\n    '$SDPX_REMOTE_BASE/incoming' \\\n    '$SDPX_REMOTE_BASE/depot' \\\n    '$SDPX_REMOTE_BASE/data' \\\n    '$SDPX_REMOTE_BASE/results' \\\n    '$SDPX_REMOTE_BASE/jobs'\"\n\nUpload to a temporary name, verify the archive remotely, and extract into a new temporary release:\n\nscp \"$ARCHIVE\" \\\n  \"$CLUSTER:$SDPX_REMOTE_BASE/incoming/$COMMIT.tar.gz.part\"\n\nssh \"$CLUSTER\" \"\n  set -eu\n  uploaded='$SDPX_REMOTE_BASE/incoming/$COMMIT.tar.gz.part'\n  actual=\\$(sha256sum \\\"\\$uploaded\\\" | awk '{print \\$1}')\n  test \\\"\\$actual\\\" = '$ARCHIVE_SHA'\n  mv \\\"\\$uploaded\\\" '$SDPX_REMOTE_BASE/incoming/$COMMIT.tar.gz'\n  mkdir '$SDPX_REMOTE_BASE/releases/$COMMIT.tmp'\n  tar -xzf '$SDPX_REMOTE_BASE/incoming/$COMMIT.tar.gz' \\\n    -C '$SDPX_REMOTE_BASE/releases/$COMMIT.tmp'\n  mkdir -p \\\n    '$SDPX_REMOTE_BASE/releases/$COMMIT.tmp/environment' \\\n    '$SDPX_REMOTE_BASE/releases/$COMMIT.tmp/metadata'\n  printf '%s\\n' '$COMMIT' \\\n    > '$SDPX_REMOTE_BASE/releases/$COMMIT.tmp/metadata/commit.txt'\n  printf '%s\\n' '$ARCHIVE_SHA' \\\n    > '$SDPX_REMOTE_BASE/releases/$COMMIT.tmp/metadata/archive-sha256.txt'\n  mv '$SDPX_REMOTE_BASE/releases/$COMMIT.tmp' \\\n    '$SDPX_REMOTE_BASE/releases/$COMMIT'\n\"\n\nEvery final path is new. If a temporary or final release already exists, stop and inspect it. Do not delete or overwrite it automatically.","title":"Install a candidate without overwriting anything"},{"category":"section","location":"cluster-workflow/#Resolve-the-Julia-environment-on-the-login-node","page":"Cluster workflow","text":"Compute nodes cannot be assumed to reach Julia package servers. Resolve, download, and precompile dependencies on the login node before submitting a job. Use a release-specific environment and a private depot layered in front of any site-provided shared cache:\n\nBASE=\"$SDPX_REMOTE_BASE\"\nRELEASE=\"$BASE/releases/$COMMIT\"\nSOURCE=\"$RELEASE/source\"\nENVIRONMENT=\"$RELEASE/environment\"\n\nif [ -n \"${SDPX_SITE_ENV:-}\" ]; then\n  source \"$SDPX_SITE_ENV\"\nfi\n: \"${JULIA_BIN:?set JULIA_BIN directly or through SDPX_SITE_ENV}\"\n\nexport JULIA_DEPOT_PATH=\"$BASE/depot${JULIA_DEPOT_PATH:+:$JULIA_DEPOT_PATH}\"\nexport SDPX_RELEASE_SOURCE=\"$SOURCE\"\n\n\"$JULIA_BIN\" \\\n  --startup-file=no \\\n  --project=\"$ENVIRONMENT\" \\\n  -e '\n    using Pkg\n    Pkg.develop(PackageSpec(path=ENV[\"SDPX_RELEASE_SOURCE\"]))\n    Pkg.add([\n        PackageSpec(name=\"Aqua\", version=\"0.8\"),\n        PackageSpec(name=\"MultiFloats\", version=\"3\"),\n        PackageSpec(name=\"JLD2\"),\n        PackageSpec(name=\"JSON\"),\n        PackageSpec(name=\"JuMP\", version=\"1\"),\n        PackageSpec(name=\"StableRNGs\"),\n    ])\n    Pkg.instantiate()\n    Pkg.precompile()\n    Pkg.status(; mode=Pkg.PKGMODE_MANIFEST)\n  '\n\nsha256sum \\\n  \"$ENVIRONMENT/Project.toml\" \\\n  \"$ENVIRONMENT/Manifest.toml\" \\\n  > \"$RELEASE/metadata/environment-sha256.txt\"\n\nThe explicit packages include optional backends and the complete test target. Do not copy either manifest from a historical SDPX directory: those projects have a different package UUID and dependency graph.\n\nAfter this step, compute jobs should set JULIA_PKG_OFFLINE=true. If an offline package load fails, return to the login node and complete the environment; do not run Pkg.add from a compute node.","title":"Resolve the Julia environment on the login node"},{"category":"section","location":"cluster-workflow/#External-benchmark-data","page":"Cluster workflow","text":"Store large inputs outside both Git and the release source. Name immutable inputs by their SHA-256 digest when practical.\n\nThe validated J/K/N_a/N_mu = 40/4/20/100 Float64x4 input has:\n\nsize:   614325622 bytes\nsha256: 8622330ad83989a3c183e82b7b0cedc5bbcd1572e96c35e8cc19750129dc4d4a\n\nIts canonical cluster path is:\n\n$SDPX_REMOTE_BASE/data/csdr-40-4-20-100/8622330ad83989a3c183e82b7b0cedc5bbcd1572e96c35e8cc19750129dc4d4a.bin\n\nUpload it to a .part file, verify both size and hash remotely, then rename it with mv -n. If the final path already exists, verify it instead of replacing it.\n\nThe validated Task_Low08 Float64 input has:\n\nsize:   4640880 bytes\nsha256: 5763deada4260e152c73a38c41a4d8d9a5bb3903a099ae00ee231c90715816b3\n\nStore it at a site-selected immutable path, for example:\n\n$SDPX_REMOTE_BASE/data/task-low08/5763deada4260e152c73a38c41a4d8d9a5bb3903a099ae00ee231c90715816b3.bin\n\nSet TASK_LOW08_INPUT to that verified path before submitting validation.","title":"External benchmark data"},{"category":"section","location":"cluster-workflow/#Preprocessing-and-model-classification","page":"Cluster workflow","text":"Leave presolve=:auto, scaling=:auto, and formulation=:auto for ordinary cluster runs. The typed preprocessing report is available at:\n\nreport = result.diagnostics.presolve.preprocessing\n\nRecord its input/output dimensions, extracted-bound counts, fixed-variable count, exact equality cleanup, formulation estimate, chordal decision, elapsed time, allocation, and warnings with the benchmark result.\n\nTask_Low08 should report no scalar bounds, equality reduction from 482 to 394, and rejected chordal analysis because the aggregate PSD pattern is 99.84% dense. The medium J/K/Na/Nmu = 32/4/16/100 CSDR model should report no structural change. Treat any different classification as an input or release regression before comparing solve times.\n\nSparse equilibration copies only active coefficient matrices and shares one read-only empty CSC object per block. Keep the scaled-problem checksum in an A/B result when changing this code. On the canonical medium model the validated Float64x4 medians were 1.241 seconds and 685.8 MB before the change, and 0.633 seconds and 269.7 MB after it.","title":"Preprocessing and model classification"},{"category":"section","location":"cluster-workflow/#Thread-and-memory-policy","page":"Cluster workflow","text":"Use one process per solve. The following is a conservative starting policy; adjust PBS resources after measuring the target node:\n\nArithmetic and workload Reserved PBS cores Julia threads per solve BLAS threads Strategy\nFloat64x4 sparse/block-arrow 8 sweep 1, 2, 4, 8 1 SDPX Julia block and Schur scheduling\nMedium Float64x4 CSDR, 1,700 uniform 2x2 blocks / 144 shared variables 128 site-specific placement reservation 48 (re-sweep 32/48/64 after material geometry changes) 1 exact Julia compute pool, 10 ms worker-sleep threshold, CPUs 0–47 and NUMA nodes 0–3; do not count the unused reservation as speedup\nBigFloat, general native site's minimum allocation 1 1 one serial solve; use separate jobs for independent cases\nBigFloat, exact singleton-local 2x2 arrow 8 sweep 1, 2, 4, 8 1 native block preparation and disjoint reduced-Schur tiles may be parallel; residual/refinement remains serial\nBigFloat, all-local 2x2 cells plus equalities measured node allocation sweep through 64; use 96/128 only for a larger measured panel 1 block/GEMV/triangular tasks cap at 64; tiled lower equality Gram may use the full width\nBigFloat, experimental mixed arrow 8 sweep 1, 2, 4, 8 1 the Float64x4 reduced panel/factorization is parallel; BigFloat residual/refinement remains serial\nTask_Low08 Float64 validation 8 8 8 sparse assembly plus dense OpenBLAS KKT factorization\nTask_Low08 Float64 performance on dual EPYC 7742 16 16 16 OpenBLAS with numactl --interleave=all; measured, hardware-specific\nTask_Low08 Float64x4 mixed KKT 16 16 1 lower-only sparse Schur plus owned-row refinement; request at least 64 GiB\nTask_Low08 native BigFloat, 256 bit 1 1 1 request at least 128 GiB; the measured 95.720 GiB estimate is unsafe in a 64 GiB job\nSmall package validation 8 4 for tests, 1 for high-precision smoke 1 sequential validation phases\n\nGeneral native BigFloat uses one solver thread. Exact singleton-local 2x2 arrows and all-local 2x2 equality-cell systems may use additional cores only for ownership-safe block work and disjoint triangular Schur or Gram tiles. For the all-local equality path, fine-grained MPFR phases automatically use at most 64 ownership tasks while the equality Gram may use a wider requested allocation. On the certified J40 BigFloat512 model, 64 workers took 368.704 seconds; uniform 128 workers took 495.811 seconds, and phase-capped 128 workers took 425.880 seconds with the exact same certificate. A 96-worker crossover took 398.303 seconds, so start at 64 workers for this geometry. The opt-in mixed reduced-arrow backend may also use those cores for its Float64x4 panel and factorization, while exact BigFloat residual and refinement work remains serial. It must be benchmarked rather than assumed to be faster.\n\nThe fixed BigFloat1024 support gate (job 196296) also passed at 64 workers: 157 iterations, 553.959 seconds solver time, 4,268,480 KiB peak RSS, valid physical certificate, and no restart, regularization, refinement, or fallback. It was slower and did not terminate with a tighter relative gap than the 512-bit run because this archived model is rounded once to Float64x4. Retain 512 bits for routine runs of this input; use 1,024 bits only when the input and requested certificate actually carry enough precision to justify it.\n\nDo not start a Float64x4 block-parallel run with both Julia and BLAS set to the full allocation. Keep BLAS and OMP at one thread so the 1/2/4/8 comparison changes only SDPX's scheduler width.\n\nFor the canonical medium CSDR geometry, use an exact compute pool and pin it explicitly. The validated node60 launch shape is:\n\nexport JULIA_EXCLUSIVE=1\nexport JULIA_THREAD_SLEEP_THRESHOLD=10000000\nexport OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1\nnumactl --physcpubind=0-47 --membind=0-3 \\\n  julia --threads=48,0 --gcthreads=1,0 --project=. solve_medium.jl\n\nThe final exact-pool sweep measured 44.112 / 22.954 / 12.450 / 6.946 / 4.275 / 3.286 / 2.967 / 3.074 / 3.540 / 3.403 seconds at 1 / 2 / 4 / 8 / 16 / 32 / 48 / 64 / 96 / 128 workers. Every requested worker was observed active and BLAS remained at one thread. The 48-worker process reached 45.38 sampled CPU cores, placed about 3,009 MiB across local NUMA nodes 0–3, and was 14.87x faster than one worker. Wider pools lost time to barriers, worker wake-up, and NUMA traffic. At 64 workers, forced NUMA interleaving was 5.4% slower than bound local allocation. Reserve 128 cores only when required by this site's placement policy; create the measured 48-worker runtime pool for this 144-column system. Re-run the 32/48/64 bracket when the shared dimension or block count changes materially.\n\nJULIA_THREAD_SLEEP_THRESHOLD is consumed before Julia starts. The measured 10-millisecond value removed the wide-pool wake-up collapse without the full CPU consumption of permanent spinning. It is a site profile for exclusive jobs, not a package default. Record process CPU time, active-worker samples, BLAS width, RSS, affinity, NUMA placement, and wall time in every new sweep.\n\nUse full-model warm-ups for source-to-source comparisons. On a fresh Float64x4 process the first solve can spend about 32 seconds compiling while the steady solve is below five seconds. The medium campaign uses three warm-ups and at least five measured solves per process, alternates source order, and reports the combined median. Benchmark drivers must parse the warm-up count as a nonnegative integer; treating it as a Boolean can silently disable warm-up when a value greater than one is requested.\n\nFor dense TaskLow08 Float64x4 runs, `mixedprecisionkkt=:onis an expert configuration: Float64 factorization is accepted only while target-precision predictor and corrector residual corrections decrease monotonically. If those corrections fail, the solver may allocate a guardedFloat64x2` factor workspace (0.596 GiB for TaskLow08), rebuild it from the current Schur matrix, and accept each solve only after a Float64x4 residual check. Native Float64x4 factorization remains the final fallback. Keep extended_precision_blas=:auto, Julia at 16 threads, and BLAS at one thread. The 16-thread conservative planner estimate is 43.339 GiB and the measured process peak is approximately 21 GiB; do not use 32 task-local Schur workers in a 64 GiB request because the estimate rises to 70.120 GiB.\n\nFor dense-Schur Float64 jobs, Julia and BLAS widths are separate tuning parameters. Task_Low08 has only 32 PSD blocks, while its 6119 x 6119 dense Cholesky remains a level-3 LAPACK operation. The measured dual-EPYC optimum used 16 Julia threads, 16 OpenBLAS threads, and interleaved NUMA allocation. Thirty-two Julia threads were within 1%, while 64–128 were slower and 128 used substantially more memory. Treat these values as a site profile, not universal solver defaults.\n\nSet a planning ceiling below the PBS memory request:\n\nexport SDPX_MEMORY_LIMIT_BYTES=28GiB  # for a 32 GiB validation job\nexport SDPX_MEMORY_LIMIT_BYTES=56GiB  # for a 64 GiB benchmark job\n\nSDPX also reads host and Linux cgroup memory signals, retains headroom, and may reduce Schur workers or reject optional packing.","title":"Thread and memory policy"},{"category":"section","location":"cluster-workflow/#Validation-job","page":"Cluster workflow","text":"This example candidate gate uses the normal queue, one node, eight reserved cores, 32 GiB, and a two-hour limit. Adapt resource values to site policy without changing the numerical gates:\n\n#!/bin/bash\n#PBS -N sdpx_validate\n#PBS -q normal\n#PBS -l nodes=1:ppn=8\n#PBS -l mem=32gb\n#PBS -l walltime=02:00:00\n#PBS -j oe\n\nset -euo pipefail\n\n: \"${SDPX_COMMIT:?submit with qsub -v SDPX_COMMIT=<full-commit>}\"\n: \"${SDPX_REMOTE_BASE:?submit with SDPX_REMOTE_BASE set}\"\n: \"${TASK_LOW08_INPUT:?submit with the checksum-verified Task_Low08 path}\"\n\nBASE=\"$SDPX_REMOTE_BASE\"\nRELEASE=\"$BASE/releases/$SDPX_COMMIT\"\nSOURCE=\"$RELEASE/source\"\nENVIRONMENT=\"$RELEASE/environment\"\nRESULT=\"$BASE/results/$SDPX_COMMIT/$PBS_JOBID\"\n\nmkdir -p \"$RESULT\"\n\nif [ -n \"${SDPX_SITE_ENV:-}\" ]; then\n  source \"$SDPX_SITE_ENV\"\nfi\n: \"${JULIA_BIN:?set JULIA_BIN directly or through SDPX_SITE_ENV}\"\n\nexport JULIA_DEPOT_PATH=\"$BASE/depot${JULIA_DEPOT_PATH:+:$JULIA_DEPOT_PATH}\"\nexport JULIA_PKG_OFFLINE=true\nexport OPENBLAS_NUM_THREADS=1\nexport OMP_NUM_THREADS=1\nexport SDPX_MEMORY_LIMIT_BYTES=28GiB\n\n{\n  printf 'commit=%s\\n' \"$SDPX_COMMIT\"\n  printf 'pbs_job_id=%s\\n' \"$PBS_JOBID\"\n  printf 'pbs_cores=%s\\n' \"${PBS_NP:-8}\"\n  printf 'node_class=%s\\n' \"${SDPX_NODE_CLASS:-unspecified}\"\n  printf 'pbs_memory_request=%s\\n' \"32gb\"\n  printf 'openblas_threads=%s\\n' \"$OPENBLAS_NUM_THREADS\"\n  printf 'omp_threads=%s\\n' \"$OMP_NUM_THREADS\"\n  printf 'memory_limit=%s\\n' \"$SDPX_MEMORY_LIMIT_BYTES\"\n  \"$JULIA_BIN\" --version\n} > \"$RESULT/environment.txt\"\n\n\"$JULIA_BIN\" \\\n  --project=\"$ENVIRONMENT\" \\\n  --startup-file=no \\\n  -e 'using Pkg; Pkg.status(; mode=Pkg.PKGMODE_MANIFEST)' \\\n  > \"$RESULT/package-status.txt\"\n\n\"$JULIA_BIN\" \\\n  --project=\"$ENVIRONMENT\" \\\n  --startup-file=no \\\n  -t 4 \\\n  -e 'using Pkg; Pkg.test(\"SDPX\"; coverage=false)' \\\n  2>&1 | tee \"$RESULT/tests.log\"\n\n\"$JULIA_BIN\" \\\n  --project=\"$ENVIRONMENT\" \\\n  --startup-file=no \\\n  -t 1 \\\n  \"$SOURCE/docs/evidence/bench/extended_precision_blas/small_solve_validation.jl\" \\\n  \"$RESULT/small-solve.csv\" \\\n  2>&1 | tee \"$RESULT/small-solve.log\"\n\n/usr/bin/time \\\n  -v \\\n  -o \"$RESULT/task-low08.time.txt\" \\\n  \"$JULIA_BIN\" \\\n  --project=\"$ENVIRONMENT\" \\\n  --startup-file=no \\\n  -t 4 \\\n  \"$SOURCE/docs/evidence/bench/lattice_bootstrap/benchmark_sdpx_float64_solve.jl\" \\\n  \"$TASK_LOW08_INPUT\" \\\n  \"$RESULT/task-low08-float64.json\" \\\n  4 600 0.1 0.85 100 0.001 sdpb 100 1e-6 \\\n  2>&1 | tee \"$RESULT/task-low08.log\"\n\n\"$JULIA_BIN\" \\\n  --project=\"$ENVIRONMENT\" \\\n  --startup-file=no \\\n  -t 8 \\\n  \"$SOURCE/docs/evidence/bench/threading/benchmark_sparse_schur_scheduler.jl\" \\\n  \"$RESULT/sparse-threading.csv\" \\\n  2>&1 | tee \"$RESULT/sparse-threading.log\"\n\nfind \"$RESULT\" \\\n  -type f \\\n  ! -name artifact-sha256.txt \\\n  -exec sha256sum {} + \\\n  | sort \\\n  > \"$RESULT/artifact-sha256.txt\"\n\nSubmit from a release-specific job directory so PBS's default output filename is unique:\n\ncd \"$SDPX_REMOTE_BASE/jobs/$COMMIT\"\nPBS_VARS=\"SDPX_COMMIT=$COMMIT,SDPX_REMOTE_BASE=$SDPX_REMOTE_BASE\"\nPBS_VARS=\"$PBS_VARS,TASK_LOW08_INPUT=$TASK_LOW08_INPUT\"\nif [ -n \"${SDPX_SITE_ENV:-}\" ]; then\n  PBS_VARS=\"$PBS_VARS,SDPX_SITE_ENV=$SDPX_SITE_ENV\"\nfi\nqsub -v \"$PBS_VARS\" validate.pbs\n\nDo not log env, set, export -p, or printenv. Those commands can capture unrelated credentials or tokens inherited by a scheduler job. The template records only explicitly approved reproducibility fields. SDPX_NODE_CLASS should be a public, non-identifying label such as zen4-64c; do not substitute a hostname. Keep detailed hostname, lscpu, NUMA, and node-memory captures only in private campaign storage, not in public result artifacts.","title":"Validation job"},{"category":"section","location":"cluster-workflow/#Candidate-acceptance-gates","page":"Cluster workflow","text":"Inspect the PBS exit status, complete log, structured result files, and peak RSS. Queue state alone is not validation.\n\nThe candidate may be promoted only when:\n\nthe full test suite exits successfully;\nthe analytic Float64x4 and 256-bit BigFloat solves in both kernel modes return Optimal;\nanalytic objective relative error is at most 1e-18 for Float64x4 and 1e-28 for BigFloat;\nanalytic primal and dual residuals, relative gap, and minimum PSD eigenvalue remain within the tolerances recorded by the driver;\nTask_Low08 returns Optimal with abs(objective - 0.653291393898) <= 1e-6;\nTask_Low08 has relative gap, primal residual, and dual residual at most 1e-6;\nTask_Low08 maximum equality residual is at most 1e-9;\nthe original-coordinate certificate reports primal_psd.ok and dual_psd.ok; record raw minimum eigenvalues as diagnostics, but do not replace the solver's scale-aware tolerance * max(norm(block, Inf), 1) PSD allowance with a fixed absolute threshold;\nthe sparse scheduler benchmark reports no meaningful Schur error and no unexpected thread or memory selection;\nno phase exceeds its memory request or reports an ownership/aliasing warning.\n\nThe local release-candidate suite contained 1,272 passing assertions with four Julia threads and no failures when this workflow was written. Treat exit status and the tests present in the candidate commit as authoritative; a later commit may legitimately change the count.\n\nThe Float64 TaskLow08 gate must pass before any extended-precision lattice run. A successful small BigFloat solve or Schur benchmark is not evidence that a full BigFloat TaskLow08 solve fits memory or has completed.\n\nThe final c9d6514 cross-problem gate (job 196277, node58) used 16 Julia and 16 OpenBLAS threads inside a 64-core reservation. It returned Optimal in 28 iterations: primal/dual objectives 0.6532912655025964 / 0.6532910479425099, relative gap 2.176e-7, primal/dual residuals 3.316e-10 / 9.534e-12, and maximum original equality residual 3.316e-10. The original-coordinate certificate was valid; minimum primal and dual PSD eigenvalues were -7.126e-11 and 1.975e-15. Solver and driver times were 33.846 and 35.585 seconds, and /usr/bin/time recorded 4,469,000 KiB peak RSS. This measurement is a correctness gate for the BigFloat scheduler change; it is not a replacement for the warmed backend comparison.","title":"Candidate acceptance gates"},{"category":"section","location":"cluster-workflow/#Sparse-1/2/4/8-and-BigFloat-benchmark-job","page":"Cluster workflow","text":"After the validation gate, use a separate normal-queue job with one node, eight cores, 64 GiB, and up to eight hours. Run the Float64x4 40/4/20/100 Schur benchmark sequentially with Julia -t 1, -t 2, -t 4, and -t 8; keep BLAS and OMP at one. Measure both extended_precision_blas=:off and :auto, use at least three timed repetitions, and retain sampled Schur errors and peak RSS.\n\nThe body of the benchmark job can use:\n\n: \"${SDPX_REMOTE_BASE:?set SDPX_REMOTE_BASE}\"\nINPUT=\"$SDPX_REMOTE_BASE/data/csdr-40-4-20-100/8622330ad83989a3c183e82b7b0cedc5bbcd1572e96c35e8cc19750129dc4d4a.bin\"\n\nexport OPENBLAS_NUM_THREADS=1\nexport OMP_NUM_THREADS=1\nexport SDPX_MEMORY_LIMIT_BYTES=56GiB\n\nfor threads in 1 2 4 8; do\n  for mode in off auto; do\n    /usr/bin/time \\\n      -v \\\n      -o \"$RESULT/float64x4-${threads}t-${mode}.time.txt\" \\\n      \"$JULIA_BIN\" \\\n      --project=\"$ENVIRONMENT\" \\\n      --startup-file=no \\\n      -t \"$threads\" \\\n      \"$SOURCE/docs/evidence/bench/extended_precision_blas/benchmark_schur.jl\" \\\n      --problem=sparse \\\n      --input=\"$INPUT\" \\\n      --output=\"$RESULT/float64x4-schur.csv\" \\\n      --arithmetic=float64x4 \\\n      --mode=\"$mode\" \\\n      --repetitions=3 \\\n      --validation-samples=128 \\\n      --memory-fraction=0.10 \\\n      2>&1 | tee \"$RESULT/float64x4-${threads}t-${mode}.log\"\n  done\ndone\n\nRun the BigFloat comparison in the same allocation only with Julia -t 1:\n\nexport SPARSE_INPUT=\"$SDPX_REMOTE_BASE/data/csdr-40-4-20-100/8622330ad83989a3c183e82b7b0cedc5bbcd1572e96c35e8cc19750129dc4d4a.bin\"\nexport BIGFLOAT_BITS=256\nexport BENCH_SAMPLES=3\nexport BENCH_OUTPUT=\"$RESULT/bigfloat-sparse-schur.csv\"\n\n/usr/bin/time \\\n  -v \\\n  -o \"$RESULT/bigfloat-sparse-schur.time.txt\" \\\n  \"$JULIA_BIN\" \\\n  --project=\"$ENVIRONMENT\" \\\n  --startup-file=no \\\n  -t 1 \\\n  \"$SOURCE/docs/evidence/bench/bigfloat_sparse_schur/benchmark.jl\" \\\n  2>&1 | tee \"$RESULT/bigfloat-sparse-schur.log\"\n\nFor exact sparse 2x2 block-arrow problems, the current runtime uses the fused compute-and-scatter kernel and bypasses packed Gram panels. Historical packed BigFloat measurements are useful implementation history, but they are not a substitute for measuring the current fused path on the target node.\n\nThe historical 80/4/40/100 input is a later scale-up case, not a replacement for the requested 40/4/20/100 gate. Run it only after the smaller target passes and request at least 128 GiB with a longer wall-time. Verify its input hash before use and keep all output under the new commit's result directory. The documented artifact has:\n\nsize:   2353958026 bytes\nsha256: ed65290d3517ef24b334abca8b8043b7b7fadbf6911ab02c904113e364203e42\n\nStore it under a checksum-named site-selected path such as:\n\n$SDPX_REMOTE_BASE/data/csdr-80-4-40-100/ed65290d3517ef24b334abca8b8043b7b7fadbf6911ab02c904113e364203e42.bin","title":"Sparse 1/2/4/8 and BigFloat benchmark job"},{"category":"section","location":"cluster-workflow/#Promotion","page":"Cluster workflow","text":"Do not move current when a job is merely queued or running. After all acceptance gates pass, record the previous target and atomically replace only the symlink:\n\n: \"${SDPX_REMOTE_BASE:?set SDPX_REMOTE_BASE}\"\nBASE=\"$SDPX_REMOTE_BASE\"\nRELEASE=\"$BASE/releases/$COMMIT\"\n\ncd \"$BASE\"\n\nif [ -L current ]; then\n  readlink current > \"$RELEASE/metadata/previous-current.txt\"\nelif [ -e current ]; then\n  printf 'current exists but is not a symlink\\n' >&2\n  exit 1\nelse\n  printf 'none\\n' > \"$RELEASE/metadata/previous-current.txt\"\nfi\n\nln -s \"releases/$COMMIT/source\" \".current-$COMMIT\"\nmv -Tf \".current-$COMMIT\" current\nreadlink -f current\n\nOnce active, make the archived source read-only:\n\nchmod -R a-w \"$RELEASE/source\"\n\nJobs should continue to use their explicit release path, not current, so a later promotion cannot change a running campaign.","title":"Promotion"},{"category":"section","location":"cluster-workflow/#Rollback","page":"Cluster workflow","text":"Rollback never deletes the failed release or its diagnostics. Read the candidate's recorded previous target, verify that it still exists, and replace the symlink atomically:\n\n: \"${SDPX_REMOTE_BASE:?set SDPX_REMOTE_BASE}\"\nBASE=\"$SDPX_REMOTE_BASE\"\nFAILED_COMMIT=<full-commit>\nPREVIOUS=\"$(\n  cat \"$BASE/releases/$FAILED_COMMIT/metadata/previous-current.txt\"\n)\"\n\ntest \"$PREVIOUS\" != none\ntest -d \"$BASE/$PREVIOUS\"\n\ncd \"$BASE\"\nln -s \"$PREVIOUS\" \".current-rollback-$FAILED_COMMIT\"\nmv -Tf \".current-rollback-$FAILED_COMMIT\" current\nreadlink -f current\n\nPreserve the failed candidate, PBS output, structured results, input hashes, and environment manifest for diagnosis.","title":"Rollback"},{"category":"section","location":"cluster-workflow/#Result-collection","page":"Cluster workflow","text":"Copy a completed result directory back without deleting the cluster original:\n\n: \"${CLUSTER:?set CLUSTER to the PBS login SSH alias}\"\n: \"${SDPX_REMOTE_BASE:?set SDPX_REMOTE_BASE}\"\nrsync -av \\\n  \"$CLUSTER:$SDPX_REMOTE_BASE/results/$COMMIT/$JOB_ID/\" \\\n  \"./results/$COMMIT/$JOB_ID/\"\n\nVerify artifact-sha256.txt after transfer. Keep raw multi-gigabyte inputs, Julia depots, manifests created for cluster campaigns, and machine-specific result directories outside Git. Commit reusable drivers and concise English summaries only.","title":"Result collection"},{"category":"section","location":"architecture/#Architecture","page":"Architecture","text":"SDPX is organized as a canonical cone frontend, a deterministic midend, and a typed numerical backend behind one ExecutionPlan.\n\n                 Frontend\n   Model builder / JuMP-MOI / CLI / file loaders\n                       |\n                       v\n             Canonical cone semantics\n          Linear | Lorentz | PositiveSemidefinite\n                       |\n                       v\n                  Midend\n analyze -> presolve -> scale -> formulate -> plan\n                       |\n                  ExecutionPlan\n                       |\n            +----------+----------+\n            |          |          |\n            v          v          v\n           LP         SOCP        SDP\n          kernel     kernel      kernel\n            \\          |          /\n             +---------+---------+\n                       v\n             system assembly / KKT\n                       v\n        factorization -> refinement\n                       v\n          original-coordinate certificate","title":"Architecture"},{"category":"section","location":"architecture/#Frontend-boundaries","page":"Architecture","text":"The public boundary is one typed Model builder. variable!, constraint!, and objective! record affine expressions and cone domains; optimize! compiles that model once, classifies its native cone family, and dispatches to the corresponding typed lowerer. The public domains are Reals, the scalar orthants and ZeroCone (LP), LorentzCone/RotatedLorentzCone (SOC), and PSDCone (SDP). Settings and Outputs carry the typed solve policy and retention policy; result accessors read the single typed result boundary.\n\nJuMP and MathOptInterface remain adapters over the same boundary: MOI caches a completed model and copy_to finalizes cone incidence before the SDPX optimizer lowers it. The CLI parses JSON through the mature qualified loader into the same numerical pipeline; it is not a separate solver route.\n\nRoute classification is fail-closed. A model containing more than one nonfree family (for example LP + SOC, LP + SDP, or SOC + SDP) raises a typed error before lowering. There is no production conversion from a Lorentz block to a PSD block; SOC models stay in native Lorentz coordinates.","title":"Frontend boundaries"},{"category":"section","location":"architecture/#Midend:-presolve,-scale,-formulate,-plan","page":"Architecture","text":"Every public optimize! runs a conservative preparation pipeline:\n\nclassify cone, storage, arithmetic, size, and predicted Schur density;\nrank-revealing equality presolve in the problem arithmetic;\nmerge scalar bounds and eliminate exactly fixed variables;\nselect scaling or Ruiz equilibration;\nestimate formulation, kernel, factorization, memory, and scheduling costs;\nbuild an immutable ExecutionPlan that freezes the mathematical formulation, storage plan, LA backend, provider, and thread schedule;\nsolve; and\nreconstruct and certify the result in the original coordinates.\n\nExecutionPlan is authoritative. Deterministic choices made after plan construction are architecture bugs. Runtime numerical fallback is allowed only where the plan authorized it, and it must be recorded as planned-versus- executed backend, formulation, and fallback reason. The original-coordinate certificate remains the final status authority.","title":"Midend: presolve, scale, formulate, plan"},{"category":"section","location":"architecture/#Cone-formulation-ownership","page":"Architecture","text":"Cone family and numerical formulation are separate decisions. Native SOC and the arrow-specialized Q3 implementation share one Lorentz semantic IR; the specialization is selected only inside the native SOC route. A PSD model stays on the SDP route even when its blocks have a structure that admits a Lorentz interpretation. Derivation and test fixtures may compare the two mathematical cones, but execution never silently changes family.\n\nThe automatic planner uses structure facts first, then checks backend feasibility. Providers and arithmetic names never choose the mathematical formulation; execution uses the provider/formulation pair frozen in the plan.","title":"Cone formulation ownership"},{"category":"section","location":"architecture/#KKT-formulations","page":"Architecture","text":"For the general dense SDP path, formulation=:auto makes a real pre-execution choice between two implemented mathematical formulations:\n\nDenseNormalEquations, followed by Cholesky;\nDenseAugmentedKKT, followed by pivoted symmetric LDLT.\n\nThe planner is deliberately small, conservative, and deterministic. It does not add a third formulation and never changes formulation after numerical execution begins. The first policy uses only two numerical-risk indicators:\n\nthe scale spread of retained equality rows; and\nthe relative diagonal quality from the normalized RRQR that equality presolve already performs for correctness.\n\nIt does not compute a condition number, SVD, eigendecomposition, or a second RRQR. If a verified retained equality basis is unavailable, auto planning conservatively keeps dense normal equations. Augmented KKT is selected only when strong normal-equation risk is present and the candidate passes structural, backend, and memory feasibility.\n\nExplicit requests are never overridden. Public Settings.formulation accepts :auto, :variable_space_schur, and :dense_augmented_kkt; a request that is incompatible with the classified route fails during planning. The first two names lower to the mature normal-equation and augmented-KKT implementations. Qualified low-level option records retain additional compatibility spellings, but those records are internal and are not a second public modeling API.\n\nThe dense augmented KKT system is the equality-augmented Schur system\n\nK = [ S   -B ]       rhs = [ r ]\n    [-B'   0 ]             [-p]\n\nwith unknowns u = [dx; dy], not the much larger full cone KKT matrix containing explicit dX_l and dY_l unknowns. Solving it produces the same dx and dy as the dense normal-equation elimination; block recovery and certification are formulation-independent. Only the lower triangle of K is authoritative. On LDLT failure the augmented route regularizes only the primal S block; the equality block remains exactly zero, residual and refinement always evaluate the original unregularized K, and failure never switches formulation, provider, or precision.\n\nSDPX.plan_formulation is a qualified internal dense-KKT formulation step used by build_execution_plan; it returns a FormulationDecision with requested/preferred/selected formulation, reasons, required capabilities, feasibility, equality scale/RRQR evidence, and memory estimates. The execution plan and result diagnostics expose the summary through parameters.formulation_decision and selected_algorithms.formulation_decision.","title":"KKT formulations"},{"category":"section","location":"architecture/#Dedicated-routes","page":"Architecture","text":"The dedicated LP path owns scalar-cone preprocessing, its normal matrix and Cholesky/LU choices, and the sparse normal-equation backend. NativeSOC owns Lorentz algebra, the Q3 reduction, its equality Gram, and original-Lorentz certification. Block-arrow elimination, sparse Schur assembly, fixed-trace Q3, native SOC kernels, reduced LP systems, mixed-precision KKT logic, and structured refinement are solver algorithms, not duplicate provider linear algebra. They stay SDPX-owned even when a provider supplies the underlying factor operations.","title":"Dedicated routes"},{"category":"section","location":"architecture/#Validation-boundary","page":"Architecture","text":"The certificate(result) accessor exposes a post-solve check that recomputes objectives, affine residuals, componentwise backward errors, complementarity, and PSD margins in the original coordinates. A solver status is not authoritative when this independent check fails. The direct primal-dual iteration is not a full homogeneous self-dual embedding, so infeasibility certificates are produced only when a validated homogeneous ray is found. See diagnostics.md.","title":"Validation boundary"},{"category":"section","location":"architecture/#Current-limitations","page":"Architecture","text":"The public API is experimental and may change before 1.0.\nLP, native SOC/RSOC, and SDP are implemented as separate typed routes.\nThe direct primal-dual iteration does not carry HSD τ/κ variables, so it may fail to produce a ray for some infeasible models.\nEquality-constrained LPs use dense LU; null-space/range-space selection is future work.\nSparse augmented/indefinite LDL is not implemented.\nGeneral BigFloat work is serial; ownership-safe independent blocks and exact local arrow phases may use workers.\nNested solves in one process are not supported because BLAS thread count is process-global.\n\nOperational details and measured trade-offs live in providers.md, benchmarks.md, preprocessing.md, and the root research notes docs/adaptive-dense-sparse-optimization.md and docs/adaptive-parameter-policy.md.","title":"Current limitations"},{"category":"section","location":"internals/#Qualified-internals","page":"Experimental internals","text":"These names support inspection, custom preprocessing, and solver research. Use them with explicit SDPX.name qualification; they are intentionally not exported and their result layouts may change before 1.0.\n\nThe SDPX module itself exports only the stable v0.5 modeling and result surface. The entries below document selected qualified implementation records that are useful for diagnostics and solver development.","title":"Qualified internals"},{"category":"section","location":"internals/#Problem-and-execution-descriptions","page":"Experimental internals","text":"","title":"Problem and execution descriptions"},{"category":"section","location":"internals/#Adaptive-controller","page":"Experimental internals","text":"","title":"Adaptive controller"},{"category":"section","location":"internals/#Preprocessing","page":"Experimental internals","text":"","title":"Preprocessing"},{"category":"section","location":"internals/#Formulation-and-sparse-planning","page":"Experimental internals","text":"The static formulation planner and provider-neutral sparse layer are exposed through their diagnostic entry points; see architecture.md, providers.md, and sparse-execution.md.","title":"Formulation and sparse planning"},{"category":"section","location":"internals/#Spectrum-and-BLAS-inspection","page":"Experimental internals","text":"","title":"Spectrum and BLAS inspection"},{"category":"type","location":"internals/#SDPX.SDPProblem","page":"Experimental internals","text":"SDPProblem{T}\n\nIngested, validated problem data. Construct via the qualified internal SDPX.ingest function — user-facing input stays Vector{Array{T,3}} for A (§1.2); this is the one-time-converted internal layout everything else operates on.\n\n\n\n\n\n","title":"SDPX.SDPProblem"},{"category":"type","location":"internals/#SDPX.SolveMode","page":"Experimental internals","text":"SolveMode\n\nWhether solve! is chasing an optimum (OPTIMIZE) or a feasibility certificate (FEASIBILITY, used by the qualified internal feasibility path). Replaces the old global mode::String (P8): it now lives inside SolverOptions, so two concurrent solves never share state.\n\n\n\n\n\n","title":"SDPX.SolveMode"},{"category":"type","location":"internals/#SDPX.ActiveSparseCoefficientVector","page":"Experimental internals","text":"ActiveSparseCoefficientVector{T}\n\nRead-only sparse coefficient block that stores only structurally active variables. It preserves the historical AbstractVector{SparseMatrixCSC} interface without allocating an m-entry reference vector for every PSD block. This is important for very large block-arrow SDPs: a model with 40,400 blocks and 40,453 variables otherwise needs more than 1.6 billion mostly-empty references before numerical work begins.\n\nactive_variables must be strictly increasing. getindex uses binary search; hot 2x2 kernels consume SparseCons.active and packed2 directly, so this lookup is confined to setup, validation, and other non-dominant paths.\n\n\n\n\n\n","title":"SDPX.ActiveSparseCoefficientVector"},{"category":"type","location":"internals/#SDPX.StructureAnalysis","page":"Experimental internals","text":"StructureAnalysis\n\nStructural statistics and the resulting automatic execution plan. SDPX keeps three notions of sparsity separate:\n\ncoefficient_density: density of the individual affine coefficient matrices, which controls coefficient storage and Schur assembly;\nblock_pattern_density: density of the union pattern inside each PSD block, which controls whether sparse/chordal PSD structure exists;\nschur_density: structural density of the variable Schur complement, which controls the KKT backend.\n\nThis distinction is important for bootstrap SDPs: their coefficient matrices can be extremely sparse even when the aggregate PSD blocks and Schur complement are dense.\n\n\n\n\n\n","title":"SDPX.StructureAnalysis"},{"category":"type","location":"internals/#SDPX.ProblemClassification","page":"Experimental internals","text":"ProblemClassification\n\nImmutable structural description used by the automatic solve pipeline. A model containing only 1×1 PSD blocks is an LP in SDPX's geometric form. Lorentz-compatible blocks are classified as cone=:socp for structural diagnostics. Blocks of side at most two use the exact Q3 <-> S_+^2 isomorphism and specialized scalar kernels; all explicit SDPProblem inputs execute through the ordinary semidefinite primal-dual route. Other larger PSD blocks are classified and solved as SDP.\n\n\n\n\n\n","title":"SDPX.ProblemClassification"},{"category":"type","location":"internals/#SDPX.PresolveReport","page":"Experimental internals","text":"PresolveReport\n\nSummary of transformations performed before numerical factorization. equality_keep maps reduced equality multipliers back to the original ordering. Scalar-cone row maps are held by the LP engine because they also reconstruct primal slacks and dual multipliers.\n\n\n\n\n\n","title":"SDPX.PresolveReport"},{"category":"type","location":"internals/#SDPX.ExecutionPlan","page":"Experimental internals","text":"ExecutionPlan\n\nAlgorithms selected before a solve. This is deliberately descriptive: it is returned to callers in diagnostics so automatic decisions are inspectable and reproducible.\n\n\n\n\n\n","title":"SDPX.ExecutionPlan"},{"category":"type","location":"internals/#SDPX.SolveDiagnostics","page":"Experimental internals","text":"SolveDiagnostics\n\nStructured metadata accompanying a solve. timings contains phase-level seconds, memory contains estimated solver workspace and process peak RSS, and warnings records non-fatal fallbacks or numerical caveats.\n\n\n\n\n\n","title":"SDPX.SolveDiagnostics"},{"category":"type","location":"internals/#SDPX.FixedParameterPolicy","page":"Experimental internals","text":"FixedParameterPolicy{T}\n\nThe exact fixed-parameter behavior selected by parameter_strategy=:fixed. sigma is the historical β; both fraction-to-boundary values and the backtracking contraction are the historical γ.\n\n\n\n\n\n","title":"SDPX.FixedParameterPolicy"},{"category":"type","location":"internals/#SDPX.AdaptiveParameterPolicy","page":"Experimental internals","text":"AdaptiveParameterPolicy{T}\n\nSafe bounds and controller settings for the guarded adaptive policy.  All numeric constants are converted to the solver arithmetic once, at policy construction; selection never narrows a MultiFloat or BigFloat diagnostic through Float64.\n\n\n\n\n\n","title":"SDPX.AdaptiveParameterPolicy"},{"category":"type","location":"internals/#SDPX.IterationDiagnostics","page":"Experimental internals","text":"IterationDiagnostics{T}\n\nNumerical state available after the affine predictor and before the corrector. Residuals are normalized by the same data scales as termination.  mu and mu_aff are global average complementarity values over the total cone dimension.  A factorization quality of one is ideal; values near zero indicate a small Cholesky-diagonal ratio.\n\n\n\n\n\n","title":"SDPX.IterationDiagnostics"},{"category":"type","location":"internals/#SDPX.IterationParameters","page":"Experimental internals","text":"IterationParameters{T}\n\nParameters selected for one corrector and accepted step.  SDP currently has a single Schur regularization, so the primal/dual values are kept equal by the built-in policies; separate fields make the policy interface usable by the LP and future symmetric-indefinite KKT paths without another API change.\n\n\n\n\n\n","title":"SDPX.IterationParameters"},{"category":"function","location":"internals/#SDPX.select_parameters","page":"Experimental internals","text":"select_parameters(policy, diagnostics, history) -> IterationParameters\n\nPure parameter selection.  No solver state is mutated, making the controller independently testable and deterministic for a supplied diagnostic history.\n\n\n\n\n\n","title":"SDPX.select_parameters"},{"category":"function","location":"internals/#SDPX.preprocess","page":"Experimental internals","text":"preprocess(problem, options=SolverOptions{T}()) -> PreprocessedProblem\n\nRun exact structural reductions in the original arithmetic. Scaling and arithmetic-aware dependent-equality presolve remain in the existing solve pipeline and therefore run after this function.\n\n\n\n\n\n","title":"SDPX.preprocess"},{"category":"type","location":"internals/#SDPX.PreprocessReport","page":"Experimental internals","text":"Structured report returned by preprocess.\n\n\n\n\n\n","title":"SDPX.PreprocessReport"},{"category":"type","location":"internals/#SDPX.PreprocessStageReport","page":"Experimental internals","text":"Diagnostics for one conservative preprocessing stage.\n\n\n\n\n\n","title":"SDPX.PreprocessStageReport"},{"category":"type","location":"internals/#SDPX.ReconstructionMap","page":"Experimental internals","text":"ReconstructionMap{T}\n\nMaps the structurally reduced problem back to the original variable, PSD-block, and equality order. A zero in reduced_to_original_blocks denotes the internal always-satisfied scalar block used when every original scalar block was eliminated.\n\n\n\n\n\n","title":"SDPX.ReconstructionMap"},{"category":"type","location":"internals/#SDPX.SpectrumResult","page":"Experimental internals","text":"SpectrumResult <: AbstractVector\n\nSpectrum records with solve-wide metadata stored exactly once. The wrapper behaves as an ordinary vector for iteration, indexing, and broadcasting while avoiding quadratic metadata growth on models with thousands of cone blocks.\n\n\n\n\n\n","title":"SDPX.SpectrumResult"},{"category":"function","location":"internals/#SDPX.blas_backend","page":"Experimental internals","text":"Return the registered dense BLAS/LAPACK backend controller.\n\n\n\n\n\n","title":"SDPX.blas_backend"},{"category":"function","location":"internals/#SDPX.blas_threads","page":"Experimental internals","text":"blas_threads() -> Int\n\nReturn the effective thread count of the active dense BLAS/LAPACK backend. Unlike LinearAlgebra.BLAS.get_num_threads, this also reports the actual Accelerate mode when the optional AppleAccelerate extension is loaded.\n\n\n\n\n\n","title":"SDPX.blas_threads"},{"category":"function","location":"internals/#SDPX.set_blas_threads!","page":"Experimental internals","text":"set_blas_threads!(count) -> Int\n\nSet the effective dense BLAS/LAPACK thread count and return the value reported by the active backend. On Apple Accelerate, 1 selects strict single-threaded mode and values greater than one select Accelerate-managed multithreading.\n\n\n\n\n\n","title":"SDPX.set_blas_threads!"},{"category":"section","location":"cli/#SDPX-command-line-frontend","page":"Command line","text":"The command-line frontend resembles the small set of high-value options used in SDPB workflows while keeping SDPX's LP/SOCP/SDP automatic pipeline in control.","title":"SDPX command-line frontend"},{"category":"section","location":"cli/#Setup","page":"Command line","text":"From the repository root:\n\njulia bin/setup_cli.jl\n./bin/sdpx --help\n\nbin/setup_cli.jl creates the isolated CLI environment and develops the local checkout into it. The solver core still has no JSON dependency.","title":"Setup"},{"category":"section","location":"cli/#Normal-use:-everything-automatic","page":"Command line","text":"./bin/sdpx problem.json\n\nThe result is written to problem.result.json. The output contains both resolved_options and plan, so auto is inspectable rather than opaque.","title":"Normal use: everything automatic"},{"category":"section","location":"cli/#SDPB-style-high-precision","page":"Command line","text":"./bin/sdpx problem.json result.json \\\n  --precision=840 \\\n  --dualityGapThreshold=1e-80 \\\n  --primalErrorThreshold=1e-80 \\\n  --dualErrorThreshold=1e-80\n\nAn integer --precision=N means BigFloat with N bits, matching the meaning users normally expect from SDPB's precision flag. High-precision coefficients in the JSON input should be strings so that they are parsed only after the BigFloat precision scope has been established.\n\nThe three stopping thresholds are independent. If any is omitted or set to auto, the frontend selects a conservative target using roughly one third of the available decimal precision, with a Float64 floor of 1e-8. For example, 840 bits resolves to approximately 1e-84. This is only a stopping-policy heuristic; final success remains governed by original-coordinate certification.","title":"SDPB-style high precision"},{"category":"section","location":"cli/#Automatic-controls","page":"Command line","text":"The ordinary frontend exposes policies, not low-level IPM constants:\n\n--algorithm=auto|lp|socp|sdp\n--presolve=auto|on|off\n--scaling=auto|none|equilibrate\n--sparse=auto|on|off\n--formulation=auto|primal|normal_equations|augmented\n--equalitySolver=auto|normal_equations|qr\n--workingPrecisionPolicy=auto|fixed\n--threads=auto|N\n--certificate=auto|on|off\n\nUnder :auto, the static formulation planner selects the dense KKT formulation before execution. normal_equations and augmented are expert overrides; primal preserves the historical primal-formulation policy without disabling sparse or block-arrow routes. dual is not a production formulation and fails closed.\n\nParameters such as centering constants, Q3 Gram strategies, mixed-precision condition limits, and refinement micro-policy remain expert/internal controls. They should not become command-line flags unless a benchmarked scientific workflow proves that users need to override them.","title":"Automatic controls"},{"category":"section","location":"cli/#Julia-frontend","page":"Command line","text":"The typed Model frontend exposes the same policy boundary without JSON:\n\nusing SDPX\n\nmodel = Model(Float64)\nx = variable!(model, :x, 1; domain=Nonnegative())\nconstraint!(model, :lower, x[1] - 1, Nonnegative())\nobjective!(model, Minimize(), x[1])\nsettings = Settings(model; algorithm=:lp, verbosity=0)\nresult = optimize!(model; settings=settings)\n\nHigh-precision policy can be explicit:\n\nmodel = Model(BigFloat; precision_bits=840)\nsettings = Settings(\n    model;\n    tolerances=Tolerances(\n        BigFloat;\n        primal=big\"1e-80\",\n        dual=big\"1e-80\",\n        gap=big\"1e-80\",\n    ),\n    algorithm=:sdp,\n)\n\nThe model's arithmetic is chosen at construction. A request for 840 bits therefore requires Model(BigFloat; precision_bits=840), while the CLI parses JSON inside the requested precision scope. Settings.algorithm selects the LP, native SOC/RSOC, or SDP implementation used for the compiled model.\n\nThe CLI implementation lowers its JSON policy through qualified compatibility option records, but those records are not part of the public Julia quickstart surface.","title":"Julia frontend"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"The v0.5 public surface is deliberately small: build one typed Model, solve it with optimize!, and inspect the returned result through the accessors below. Anything not documented here, and anything prefixed with _, is an internal implementation detail and may change without notice.","title":"API reference"},{"category":"section","location":"api/#Modeling","page":"API reference","text":"","title":"Modeling"},{"category":"section","location":"api/#Settings-and-solve","page":"API reference","text":"","title":"Settings and solve"},{"category":"section","location":"api/#Results-and-diagnostics","page":"API reference","text":"","title":"Results and diagnostics"},{"category":"section","location":"api/#MathOptInterface","page":"API reference","text":"SDPX.Optimizer implements MathOptInterface, so SDPX can be used from JuMP or any other MOI client that emits the supported linear, Lorentz, rotated Lorentz, and positive-semidefinite cone sets:\n\nusing JuMP, LinearAlgebra, SDPX\nmodel = Model(() -> SDPX.Optimizer(sparse=:auto, verbosity=0))\n\nThe MOI adapter is the only secondary frontend. It uses the same typed native solver and does not add a parallel SDPX-specific modeling or solve API.","title":"MathOptInterface"},{"category":"type","location":"api/#SDPX.Model","page":"API reference","text":"SDPX.Model{T<:AbstractFloat}\n\nUser-facing mathematical model for the v0.5 frontend. T is the arithmetic type of every number that enters the model; the immutable arithmetic::ArithmeticSpec{T} records and validates the precision contract.\n\nConstructors\n\nModel(Float64) — IEEE binary64 arithmetic.\nModel(BigFloat; precision_bits=256) — arbitrary-precision arithmetic; any precision_bits >= 2 (for example 128, 256, 412, or 512) is accepted.\nModel(Float64x2) / Model(Float64x4) — only when the optional MultiFloats package is loaded.\n\nThe mutable fields are the minimal builder state for B1: 1-based id counters, concrete Vector{VariableRef} / Vector{ConstraintRef} registries, and the typed variable-block records owned by the model (VariableBlockRecord{T}; see modeling/model.jl). Constraint and objective data, dualization state, and solver choices never live here. The block registry uses concrete typed vectors and a Dict{Symbol,Int} name→record map; no Any-typed state is stored.\n\n\n\n\n\n","title":"SDPX.Model"},{"category":"function","location":"api/#SDPX.variable!","page":"API reference","text":"variable!(model, name, n; domain=Reals())\nvariable!(model, name, n, n; domain=PSDCone())\n\nAdd one native product-cone block. Vector orthants and Lorentz cones remain one vector block. An n×n PSD variable remains one lower-packed PSD block.\n\n\n\n\n\n","title":"SDPX.variable!"},{"category":"function","location":"api/#SDPX.constraint!","page":"API reference","text":"constraint!(model, name, expression, domain)\n\nAdd one affine block with canonical semantics expression in domain. The compiler later stores each row as A*x-rhs in domain, with rhs=-constant.\n\n\n\n\n\n","title":"SDPX.constraint!"},{"category":"function","location":"api/#SDPX.objective!","page":"API reference","text":"Set the model's single scalar affine objective.\n\n\n\n\n\n","title":"SDPX.objective!"},{"category":"function","location":"api/#SDPX.set_start!","page":"API reference","text":"Set an owned primal warm start for one native variable block.\n\n\n\n\n\n","title":"SDPX.set_start!"},{"category":"function","location":"api/#SDPX.set_dual_start!","page":"API reference","text":"Set an owned dual warm start for one affine constraint block.\n\n\n\n\n\n","title":"SDPX.set_dual_start!"},{"category":"function","location":"api/#SDPX.set_dual_slack_start!","page":"API reference","text":"Set an owned variable-dual-slack warm start for one native variable block.\n\n\n\n\n\n","title":"SDPX.set_dual_slack_start!"},{"category":"type","location":"api/#SDPX.Reals","page":"API reference","text":"SDPX.Reals\n\nThe full real line. A scalar variable (or vector block) constrained to Reals is unconstrained. This type intentionally mirrors the MathOptInterface.Reals mathematical set but is SDPX-owned so the frontend never depends on MOI's type identity.\n\n\n\n\n\n","title":"SDPX.Reals"},{"category":"type","location":"api/#SDPX.Nonnegative","page":"API reference","text":"SDPX.Nonnegative\n\nThe closed nonnegative orthant {x >= 0}. As a product-cone block it is one vector block; it is never split into scalar blocks.\n\n\n\n\n\n","title":"SDPX.Nonnegative"},{"category":"type","location":"api/#SDPX.Nonpositive","page":"API reference","text":"SDPX.Nonpositive\n\nThe closed nonpositive orthant {x <= 0}. As a product-cone block it is one vector block.\n\n\n\n\n\n","title":"SDPX.Nonpositive"},{"category":"type","location":"api/#SDPX.ZeroCone","page":"API reference","text":"SDPX.ZeroCone\n\nThe zero / equality subspace {x == 0}. As a product-cone block it is one vector block.\n\n\n\n\n\n","title":"SDPX.ZeroCone"},{"category":"type","location":"api/#SDPX.LorentzCone","page":"API reference","text":"SDPX.LorentzCone\n\nThe second-order cone of vector dimension n: { (t, x) : ||x||_2 <= t } with t first, n >= 1. The dimension n belongs to the block shape (SDPX.NativeBlock), not to this type.\n\n\n\n\n\n","title":"SDPX.LorentzCone"},{"category":"type","location":"api/#SDPX.RotatedLorentzCone","page":"API reference","text":"SDPX.RotatedLorentzCone\n\nThe rotated second-order cone of vector dimension n: { (u, v, x) : 2uv >= ||x||_2^2, u >= 0, v >= 0 }, n >= 3. The dimension n belongs to the block shape.\n\n\n\n\n\n","title":"SDPX.RotatedLorentzCone"},{"category":"type","location":"api/#SDPX.PSDCone","page":"API reference","text":"SDPX.PSDCone\n\nThe positive semidefinite cone over real symmetric matrices. This type carries no dimension: an n × n PSD block records its matrix dimension n and lower-authoritative packed storage metadata in its SDPX.NativeBlock descriptor and remains ONE block — never n(n+1)/2 unrelated scalar blocks.\n\n\n\n\n\n","title":"SDPX.PSDCone"},{"category":"type","location":"api/#SDPX.Minimize","page":"API reference","text":"SDPX.Minimize\n\nObjective sense marker: minimize c'x + constant.\n\n\n\n\n\n","title":"SDPX.Minimize"},{"category":"type","location":"api/#SDPX.Maximize","page":"API reference","text":"SDPX.Maximize\n\nObjective sense marker: maximize c'x + constant.\n\n\n\n\n\n","title":"SDPX.Maximize"},{"category":"type","location":"api/#SDPX.Settings","page":"API reference","text":"Settings{T<:AbstractFloat}\n\nTyped public solve settings in arithmetic T.  Every field is concrete; there is intentionally no Any field and no catch-all keyword sink, so orientation / :dual and every other unknown keyword fail loudly by absence.\n\nFields\n\ntolerances::Tolerances{T} — primal/dual/gap stopping targets.\nlimits::Limits — iterations, wall time, per-solve threads.\nscaling::Symbol — :auto or :none / :equilibrate.\nformulation::Symbol — numerical formulation selector.  Public names are :auto, :variable_space_schur (variable-space Schur complement; lowers to the existing :normal_equations policy) and :dense_augmented_kkt (dense augmented KKT; lowers to the existing :augmented policy).  The historical :primal and any dual-model :dual are deliberately not part of the public surface and are rejected.  :sparse_normal_equations is also not exposed: the existing policy space has no exact sparse-formulation name (sparse storage is selected separately by the sparse field).\nprovider::Symbol — :auto / :standard / :bfla / :multifloat / :legacy dense linear-algebra provider policy (documented metadata carried to SolveOptions.linear_algebra_backend; this file never instantiates or probes a provider).\npresolve::Symbol — structural presolve: :auto, :on, or :off.\nalgorithm::Symbol — :auto / :lp / :socp / :sdp.\nsparse::Symbol — sparse storage preference :auto, :on, or :off.\nequality_solver::Symbol — :auto / :normal_equations / :qr.\nworking_precision_policy::Symbol — :auto / :fixed.\ndiagnostics::Symbol — retention level :none, :summary, or :full for planning/diagnostic payloads.\nverbosity::Int — nonnegative output level.\ntiming::Bool — record frontend/solver phase timings.\ncertification::Bool — run post-solve certification.\nblas_threads::Union{Nothing,Int} — requested BLAS/LAPACK thread metadata only.  This layer never calls set_blas_threads!; an integrator may apply the value through the existing SDPX.set_blas_threads! seam without this file mutating global state.\n\nConstruct with Settings{Float64}() / Settings{Float64}(...), or Settings(model; ...) where the model is any object whose element type selects the arithmetic (SDPProblem, ConicProblem, Model).\n\n\n\n\n\n","title":"SDPX.Settings"},{"category":"type","location":"api/#SDPX.Tolerances","page":"API reference","text":"Tolerances{T<:AbstractFloat}\n\nTyped public stopping tolerances in arithmetic T.  Each field is optional (Tolerances(; primal=..., dual=..., gap=...)); omitted fields are nothing and mean: use SDPX's deterministic automatic target.\n\nValidation on construction:\n\nexplicit tolerances are finite and strictly positive in T;\nnothing is the only way to request automatic selection.\n\nThe default constructor is Tolerances{T}() — every tolerance automatic.\n\n\n\n\n\n","title":"SDPX.Tolerances"},{"category":"type","location":"api/#SDPX.Limits","page":"API reference","text":"Limits\n\nTyped public iteration and time limits.\n\niterations: nonnegative Int; 0 (or nothing) requests the automatic default, which is currently the existing SolveOptions default of 200.\ntime: finite and nonnegative Float64, or Inf for no limit.\nthreads: strictly positive Int; Limits(; threads=nothing) stores the process auto count (Base.Threads.nthreads()), and the existing per-solve scheduling limit is inherited from that stored value.\n\nthreads and maximum iteration/time are scheduling/weight limits interpreted by the existing option lowerer; no Julia thread pool or BLAS thread count is mutated anywhere in this file.\n\n\n\n\n\n","title":"SDPX.Limits"},{"category":"type","location":"api/#SDPX.Outputs","page":"API reference","text":"Outputs\n\nTyped public retention policy for result fields.\n\nFor the three raw-field groups (primal, constraint_dual, dual_slack) the value is either\n\n:all   — retain every component of the group;\n:none  — retain nothing from the group;\na concrete component vector, retaining only those components: primal and dual_slack take Vector{VariableRef} (raw primal variable / dual-slack components), while constraint_dual takes Vector{ConstraintRef} (affine-cone constraint components).  The groups are semantically typed: a constraint vector is rejected for primal/dual_slack, and a variable vector is rejected for constraint_dual.\n\nobjectives::Bool retains primal/dual objective values. certificate::Symbol is :none (no certificate payload), :summary (compact facts), or :full (raw requirement that every certificate input — primal, constraint dual, dual slack, objectives and diagnostics/facts — be retained).  diagnostics::Symbol is :none, :summary, or :full (:full keeps the detailed planning/diagnostic payload).  history::Bool / trace::Bool retain iteration history and performance-trace payloads respectively.\n\nThis policy controls what is retained in the returned Result; it does not change the solver workspace or guarantee a lower peak allocation during the solve. Iteration-history availability is route-dependent, so a retained history may be empty when the selected core does not publish per-iteration records.\n\nAll fields are validated on construction and normalized by the public normalize_outputs entry point.\n\n\n\n\n\n","title":"SDPX.Outputs"},{"category":"function","location":"api/#SDPX.optimize!","page":"API reference","text":"optimize!(model; settings=nothing, outputs=Outputs()) -> Result\n\nCompile and solve model through its single classified native LP, SOC, or SDP route. settings controls the numerical solve, while outputs controls which result data are retained. The returned Result is expressed in the original model coordinates.\n\n\n\n\n\n","title":"SDPX.optimize!"},{"category":"function","location":"api/#SDPX.execution_plan","page":"API reference","text":"Return the immutable execution plan that governed result.\n\n\n\n\n\n","title":"SDPX.execution_plan"},{"category":"function","location":"api/#SDPX.status","page":"API reference","text":"Return the stable public terminal-status symbol for result.\n\nThe numerical cores retain their typed SolveStatus enum internally.  The v0.5 Model interface deliberately does not export that legacy core enum, so the public getter translates it to one documented, allocation-free symbol.\n\n\n\n\n\n","title":"SDPX.status"},{"category":"function","location":"api/#SDPX.value","page":"API reference","text":"Return the retained packed primal vector in model variable order.\n\n\n\n\n\nReturn one retained scalar primal coordinate.\n\n\n\n\n\n","title":"SDPX.value"},{"category":"function","location":"api/#SDPX.dual","page":"API reference","text":"Return the retained packed affine-constraint dual vector.\n\n\n\n\n\n","title":"SDPX.dual"},{"category":"function","location":"api/#SDPX.dual_slack","page":"API reference","text":"Return the retained packed variable dual-slack vector.\n\n\n\n\n\n","title":"SDPX.dual_slack"},{"category":"function","location":"api/#SDPX.primal_objective","page":"API reference","text":"Return the retained primal objective in the original objective sense.\n\n\n\n\n\n","title":"SDPX.primal_objective"},{"category":"function","location":"api/#SDPX.dual_objective","page":"API reference","text":"Return the retained dual objective in the original objective sense.\n\n\n\n\n\n","title":"SDPX.dual_objective"},{"category":"function","location":"api/#SDPX.certificate","page":"API reference","text":"Return the original-coordinate certificate summary for result.\n\n\n\n\n\n","title":"SDPX.certificate"},{"category":"function","location":"api/#SDPX.diagnostics","page":"API reference","text":"Return the retained solver diagnostics.\n\nThrows ResultFieldNotRetained when diagnostics were disabled in the requested Outputs.\n\n\n\n\n\n","title":"SDPX.diagnostics"},{"category":"function","location":"api/#SDPX.iteration_history","page":"API reference","text":"Return a copy of the retained per-iteration history.\n\nThrows ResultFieldNotRetained when history retention was disabled.\n\n\n\n\n\n","title":"SDPX.iteration_history"},{"category":"function","location":"api/#SDPX.performance_trace","page":"API reference","text":"performance_trace(result::SDPResult) -> PerformanceTrace\n\nProject a finished solve into a stable four-section PerformanceTrace. Fields absent from the recorded result are returned as unavailable, never zeroed or guessed.\n\n\n\n\n\nperformance_trace(result::ConicResult) -> PerformanceTrace\n\nNative Lorentz-cone results project their own diagnostics. Test-only reference results carry SDP-shaped diagnostics and project them directly.\n\n\n\n\n\nReturn the retained phase-level performance trace.\n\nThrows ResultFieldNotRetained when trace retention was disabled.\n\n\n\n\n\n","title":"SDPX.performance_trace"},{"category":"type","location":"api/#SDPX.Optimizer","page":"API reference","text":"Optimizer{T}(; kwargs...)\nOptimizer(; kwargs...)\n\nCreate SDPX's non-incremental MathOptInterface optimizer. The untyped constructor uses Float64; select Optimizer{Float64x4} or Optimizer{BigFloat} for extended precision. JuMP normally wraps this optimizer in an MOI cache and copies the completed model into SDPX in one pass.\n\nCommon raw keywords include tolerance, max_iterations, time_limit, threads, precision, verbosity, and sparse.\n\n\n\n\n\n","title":"SDPX.Optimizer"},{"category":"section","location":"pipeline/#Automatic-pipeline","page":"Automatic pipeline","text":"Every public optimize! call compiles one typed model and builds an inspectable ExecutionPlan. The pipeline performs:\n\ncone, storage, arithmetic, and size classification;\nrank-revealing equality presolve with a consistency check and dual reconstruction map;\nredundant scalar-cone row elimination for LPs;\nautomatic scaling and equilibration selection;\nsolver, Gram/Schur kernel, KKT backend, thread schedule, and memory-budget selection;\nguarded adaptive interior-point parameter control with a fixed fallback;\nphase timing, workspace estimation, warnings, and result reconstruction.\n\nPure scalar-cone models are lowered once to the dedicated Mehrotra LP engine. Pure SOC/RSOC models use NativeSOC in original Lorentz coordinates; general Lorentz blocks use the Nesterov–Todd path, while exactly certified fixed-trace Q3 cells use the compact HKM specialization (see socp.md). Pure PSD models use the SDP engine. Mixed cone families fail during classification; there is no production SOC-to-PSD lift or model retry.","title":"Automatic pipeline"},{"category":"section","location":"pipeline/#Presolve","page":"Automatic pipeline","text":"Equality columns in B are normalized independently and reduced using column-pivoted QR in the problem's arithmetic type. This avoids narrowing away a direction that is meaningful to Float64x4 or BigFloat. Discarded columns are checked against both the retained basis and their right-hand sides in the same arithmetic; an ambiguous rank decision keeps all equalities. An inconsistent system returns InfeasibleCert before factorization. Reduced dual multipliers are mapped back to the original equality ordering, with zero assigned to non-unique discarded multipliers.\n\nThe LP presolver additionally removes:\n\nzero rows that are always satisfied;\ninfeasible zero rows;\npositive scalar multiples with the same left-hand side, retaining the strongest lower bound.","title":"Presolve"},{"category":"section","location":"pipeline/#Dedicated-LP-kernels-and-multicore-scheduling","page":"Automatic pipeline","text":"The LP normal matrix is\n\nH = G' * Diagonal(z ./ s) * G.\n\nFor Float64 and one thread, SDPX packs sqrt(z ./ s) .* G once and calls BLAS syrk. For sufficiently large models with single-threaded BLAS and more than one requested Julia thread, variables are split into coarse column panels. Each worker performs one independent BLAS-3 GEMM into a disjoint output panel. There are no partial-matrix reductions, atomics, or locks. BigFloat remains serial.\n\nFor Float64xN and BigFloat, extended_precision_blas=:auto permits the planner to choose the packed blocked triangular syrk! path only when its predicted benefit exceeds packing cost and it fits the memory budget. extended_precision_blas=:on is intended for diagnostics and still cannot override the memory-safety check. Float64 is never redirected by this option.\n\nThe equality-free LP Newton matrix is positive definite after regularization, so SDPX factors it with Cholesky and reuses the kernel triangular solves. Equality-constrained LPs retain dense LU because their augmented KKT matrix is indefinite. Small Float64 SDPs containing only 1×1/2×2 blocks and fewer than 1,000 variables are kept serial because repeated task barriers cost more than the block kernels. Fixed-width extended arithmetic retains multicore block scheduling.","title":"Dedicated LP kernels and multicore scheduling"},{"category":"section","location":"pipeline/#High-precision-ownership-and-allocation-policy","page":"Automatic pipeline","text":"Julia BigFloat values are mutable. Solver workspaces are therefore created with independent scalar objects instead of relying on zeros(BigFloat, ...) or fill!, which can install the same object into multiple slots. Internal *_owned! operations may mutate destinations only after that ownership invariant has been established.\n\nThe dedicated BigFloat layer covers copying, zeroing, fused vector updates, matrix products, triangular solves, Cholesky factorization/solve, Schur and KKT right-hand sides, weighted LP Hessian/KKT assembly, and blocked triangular syrk!/gemm! paths. Input conversion, result construction, and operations that must create independent output values may still allocate.","title":"High-precision ownership and allocation policy"},{"category":"section","location":"pipeline/#Extended-precision-crossover-and-memory-budget","page":"Automatic pipeline","text":"The packed-kernel selector uses arithmetic family, dimensions, active density, average nonzeros, expected Schur density, requested threads, and packing bytes. The current conservative automatic gates are:\n\nGate Fixed-width extended BigFloat\nMinimum columns 32 20\nMinimum pair-row work 200,000 50,000\nMinimum expected Schur density 0.20 0.05\nMinimum predicted speedup 1.18x 1.12x\nSparse average fill required 0.42 0.62\n\nFixed-width sparse blocks of dimension at most two retain their specialized small-block path. Every packed block must also fit its cumulative memory budget.\n\nAvailable memory is the minimum usable signal from host free memory, Linux cgroup v1/v2 counters, and the optional SDPX_MEMORY_LIMIT_BYTES ceiling. The requested packing budget is extended_precision_memory_fraction × available (default 0.10 ×), capped at half of the available amount so at least half remains as general headroom. If no reliable signal exists, optional packing is disabled instead of risking an out-of-memory kill.\n\nFor the fused exact-arrow 2×2 SDP path, transformed panels and packed pair buffers are not allocated because the compute-and-scatter kernel consumes neither. This specialization takes precedence over optional packed extended BLAS for both Float64x4 and BigFloat. Execution diagnostics report gram_kernel=:fused_arrow_2x2 and gram_kernel_reason=:fused_arrow_specialized.","title":"Extended-precision crossover and memory budget"},{"category":"section","location":"pipeline/#Automatic-cold-start-parameters","page":"Automatic pipeline","text":"parameter_policy=:auto runs one generic automatic Mehrotra controller before the first iteration. It selects no benchmark-, size-, cone-, or precision-specific parameter profile: beta, gamma, predictor, and parameter_strategy come from the SolverOptions defaults or user choices. After presolve and scaling, the selected formulation/provider factors an identity-metric initialization system once and solves its primal and dual right-hand sides. Cone-native strict-interior shifts are followed by the smallest aggregate identity shift that raises orthant/PSD starts, and Lorentz sides still within the typed cone-vertex envelope, to unit identity mass, then by deterministic complementarity cross-centering. This floor prevents a valid affine point at the cone vertex from remaining at machine-epsilon scale without renormalizing an already balanced Lorentz point; it is separate from barrier degree. An accepted regularized or mixed-precision SDP factor is reused for bounded structured residual corrections when either original-KKT right-hand side is above the existing cold-start gate. Omega_p and Omega_d are not read by this automatic path. The public resolver reports profile=:post_scaling_mehrotra; the controller is deferred in the plan as :automatic_mehrotra and resolved exactly once after scaling as :post_scaling_mehrotra; and the adaptive iteration controller uses the generic 0.50 sigma cap (adaptive_sigma_max remains the expert override). parameter_policy=:fixed uses the supplied values exactly and records :user_fixed.\n\nThis is separate from the storage/structure classification reported by StructureAnalysis.profile, which describes data layout and kernel selection rather than iteration parameters.","title":"Automatic cold-start parameters"},{"category":"section","location":"pipeline/#Adaptive-Newton-parameters","page":"Automatic pipeline","text":"parameter_strategy=:adaptive selects a bounded Mehrotra sigma from the affine complementarity ratio, centrality, factor quality, and recent progress. It separately selects primal and dual fraction-to-boundary values, the backtracking contraction, and the refinement target/cap. The complete fixed predictor/corrector path is restored after non-finite diagnostics, rank-revealing equality factorization, or unstable progress.\n\nWhen history retention is enabled, iteration_history(result) contains every accepted iteration, including sigma, mu, mu_aff, affine and accepted steps, the separate step safeguards, residual progress, factor/PSD-margin proxies, regularization, refinement, and fallback provenance. See Adaptive Interior-Point Parameter Policy for the audit, equations, exact bounds, and arithmetic-specific behavior.","title":"Adaptive Newton parameters"},{"category":"section","location":"pipeline/#Result-and-diagnostics","page":"Automatic pipeline","text":"The public result exposes terminal status, objectives, certificate residuals, the immutable execution plan, and retained primal/dual data through the v0.5 accessors. Full diagnostics add classification, presolve facts, analytical workspace estimates, process peak RSS, selected algorithms, timings, warnings, and refinement/fallback provenance. See diagnostics for the retention and accessor contract.","title":"Result and diagnostics"},{"category":"section","location":"pipeline/#Remaining-bottlenecks","page":"Automatic pipeline","text":"Large dense SDP Schur matrices and their factorization dominate the heavy benchmark models; the Float64 numerical path is intentionally unchanged.\nEquality-constrained LPs use dense LU. A null-space/range-space selector remains future work; sparse execution is intentionally restricted to equality-free frozen-CSC normal equations with provider-native Cholesky.\nLP panel GEMM computes full output panels. A lower-triangle-only blocked BLAS-3 kernel would reduce arithmetic further.\nGeneral-dimensional native SOCP is production; general BigFloat work is serial, while exact singleton-local 2×2 arrows may use ownership-safe panel preparation and disjoint Schur tiles.\nPresolve removes equality dependence and scalar-row redundancy; bound propagation, singleton substitution, coefficient strengthening, and chordal SDP decomposition remain future work.\nNested solves in one process are not supported because BLAS thread count is process-global. Use separate processes for concurrent instances.","title":"Remaining bottlenecks"},{"category":"section","location":"sparse-execution/#Sparse-execution","page":"Sparse execution","text":"Sparse execution keeps formulation, storage, arithmetic, and factorization as separate decisions. KKTStoragePlan records :dense or :sparse; sparse matrices are owned as frozen CSC (SparseKKTStorage) and numeric iterations update only nzval. SparseSymbolicAnalysis stores a pattern-only ordering, elimination tree, and factor row structure, so the same symbolic object can be reused for Float64, MultiFloat, and BigFloat values.","title":"Sparse execution"},{"category":"section","location":"sparse-execution/#Providers","page":"Sparse execution","text":"Float64 uses the CHOLMOD provider through Julia's SuiteSparse. Float64x2/Float64x3/Float64x4 and BigFloat use the provider-neutral simplicial generic sparse Cholesky. Generic factors retain only their CSC factor pattern and values; they never construct an n×n dense factor. BigFloat operands are checked for uniform precision and factor/workspace destinations are independently owned MPFR objects.\n\nanalyze_sparse_pattern, numeric_factorize!, sparse_factor_solve!, and sparse_factor_diagnostics form the provider-neutral API. A changed CSC pattern fails closed, while unchanged patterns support numeric refactorization without repeating symbolic ordering. Sparse augmented/ indefinite LDL is not part of the generic route; explicit unsupported requests report an error instead of silently falling back to dense.\n\nThe current generic ordering is a deterministic approximate minimum-degree ordering for small patterns (natural order for very large setup graphs). The CHOLMOD numeric provider retains SuiteSparse's AMD ordering. Fill diagnostics include dimension, input/factor nonzeros, fill ratio, ordering, provider, refactorization count, and (where available) minimum factor diagonal.","title":"Providers"},{"category":"section","location":"sparse-execution/#Dedicated-LP-sparse-path","page":"Sparse execution","text":"The dedicated LP path forms H = G' * Diagonal(z ./ s) * G and may select a sparse Newton system when G really is sparse. select_lp_formulation uses :auto, :dense, or :sparse; the crossover is a property of the fill-in of G'*D*G rather than of G, so a sparse factorization is used only when it is predicted to pay for itself:\n\nexplicit :dense always selects :dense_lu;\ngeneric extended arithmetic remains explicit-first: package availability must not silently change an :auto execution plan;\nstorage=:sparse with equalities raises because sparse execution supports only SPD normal equations (the sparse augmented LDL route is retired);\notherwise :auto selects :sparse_normal only at or above the dimension and density crossover.\n\nThe equality-free sparse Newton system is positive definite, so SDPX freezes its CSC pattern, computes a fill-reducing ordering and elimination tree once, and refactors only numeric values in later iterations. The final LP termination.sparse_schur_backend payload reports actual input/factor counts and reuse counters; factorization elapsed time remains the separate kkt_factorization timing.","title":"Dedicated LP sparse path"},{"category":"section","location":"sparse-execution/#SDP-sparse-Schur","page":"Sparse execution","text":"For eligible SDP models, the provider-neutral sparse-Schur path is selected before workspace allocation. ExecutionPlan.storage_plan is created before the post-presolve pattern exists, so storage_input_nnz=0 and density fields are explicit \"estimate unavailable\" markers, not measured matrix counts.","title":"SDP sparse Schur"},{"category":"section","location":"sparse-execution/#Scope","page":"Sparse execution","text":"Sparse code remains inside SDPX. A future extraction boundary would be the pattern/order/assembly-map and provider-neutral factor/solve API; formulation planning, LP/SDP assembly policy, scaling, refinement, and certification should remain SDPX-owned.","title":"Scope"},{"category":"section","location":"jump/#JuMP-and-MathOptInterface","page":"JuMP and MOI","text":"SDPX provides a non-incremental MathOptInterface optimizer. JuMP builds a cached model, then SDPX converts the completed model into the same typed cone representation used by the public Model route before solving it. The wrapper performs one route classification and one family lowering; it does not create a second modeling or numerical route.\n\nusing JuMP, LinearAlgebra, SDPX\n\nmodel = Model(() -> SDPX.Optimizer(\n    sparse=:auto,\n    tolerance=1e-8,\n    threads=4,\n    verbosity=0,\n))\n\n@variable(model, x[1:2])\n@constraint(\n    model,\n    Symmetric([x[1] -1.0; -1.0 x[2]]) in PSDCone(),\n)\n@objective(model, Min, 2x[1] + 3x[2])\noptimize!(model)\n\nThe analytic solution is x = [sqrt(3/2), sqrt(2/3)] with objective 2sqrt(6).\n\nUse GenericModel{T} and SDPX.Optimizer{T} for extended coefficient types:\n\nusing JuMP, LinearAlgebra, SDPX\nusing MultiFloats: Float64x4\n\nconst T = Float64x4\nmodel = GenericModel{T}(\n    () -> SDPX.Optimizer{T}(\n        sparse=:auto,\n        verbosity=0,\n        tol_gap=T(1e-8),\n        tol_primal=T(1e-8),\n        tol_dual=T(1e-8),\n    ),\n)\n\n@variable(model, x[1:2])\n@constraint(\n    model,\n    Symmetric([x[1] T(-1); T(-1) x[2]]) in PSDCone(),\n)\n@objective(model, Min, T(2) * x[1] + T(3) * x[2])\n\noptimize!(model)","title":"JuMP and MathOptInterface"},{"category":"section","location":"jump/#Supported-model-forms","page":"JuMP and MOI","text":"The wrapper supports:\n\nminimization, maximization, and feasibility objective senses;\nscalar affine and single-variable objectives;\nfree scalar variables;\nscalar affine or single-variable constraints in MOI.EqualTo, MOI.GreaterThan, MOI.LessThan, and MOI.Interval; pure scalar-cone models use the dedicated LP engine;\nvector affine or vector-of-variables constraints in MOI.Nonnegatives, MOI.Nonpositives, and MOI.Zeros (through batched linear rows);\nvector affine or vector-of-variables constraints in MOI.SecondOrderCone and MOI.RotatedSecondOrderCone (through native Lorentz SOC and the exact rotated map);\nvector affine or vector-of-variables constraints in MOI.PositiveSemidefiniteConeTriangle;\nvector affine constraints in MOI.Scaled{MOI.PositiveSemidefiniteConeTriangle}. Scaled PSD product variables are reported unsupported rather than entering an unavailable product-scaling path.\n\nThe wrapper converts MathOptInterface's column-major upper-triangle ordering to full symmetric SDPX blocks and applies the required sqrt(2) off-diagonal scaling for scaled PSD cones in both primal and dual results.\n\nResult attributes available through JuMP or MathOptInterface include termination, primal, dual, and raw statuses; primal and dual objective values and relative gap; variable primal values; equality and PSD constraint primal/dual values; solve time and barrier iteration count; and solver name, version, and raw solver result.","title":"Supported model forms"},{"category":"section","location":"jump/#Options","page":"JuMP and MOI","text":"Options can be passed to the constructor:\n\nmodel = Model(() -> SDPX.Optimizer(\n    tolerance=1e-8,\n    threads=4,\n    parameter_policy=:auto,\n    max_iterations=300,\n    time_limit=60.0,\n    verbose=0,\n))\n\nThey can also be set after model construction:\n\nset_optimizer_attribute(model, \"tol_gap\", 1e-8)\nset_optimizer_attribute(model, \"tol_primal\", 1e-8)\nset_optimizer_attribute(model, \"tol_dual\", 1e-8)\nset_time_limit_sec(model, 60.0)\nset_silent(model)\n\nThe adapter accepts these compatibility aliases. They lower to qualified engine options; native v0.5 applications should use Settings instead:\n\nAdapter attribute Qualified engine field Meaning\nbeta β complementarity reduction target\ngamma γ line-search backtracking factor\nomega_p Ωp expert fixed-policy primal PSD scale\nomega_d Ωd expert fixed-policy dual PSD scale\ntol_gap ϵ_gap relative gap tolerance\ntol_primal ϵ_primal primal residual tolerance\ntol_dual ϵ_dual dual residual tolerance\nmax_iter / max_iterations iter_max iteration limit\ntime_limit max_time end-to-end pipeline wall-clock limit in seconds\nnum_threads threads maximum Julia tasks used by one solve\nprecision precision_bits BigFloat working precision in bits\nverbose verbosity output level from 0 to 3\n\nSetting the raw \"tolerance\" attribute updates the gap, primal, and dual tolerances together. Every exact qualified engine field name is also accepted as a raw optimizer attribute, including sparse, scaling, predictor, refine_steps, max_restarts, extended_precision_blas, and extended_precision_memory_fraction. Unknown names are rejected. The attribute bridge uses the same LP, native SOC/RSOC, and SDP route selection as the typed Model API.\n\nparameter_policy=:auto (the default) runs the generic automatic Mehrotra controller: beta, gamma, predictor, and parameter_strategy keep the adapter defaults or user choices. After presolve and scaling, the selected KKT/provider route constructs a primal/dual affine point and applies typed cone-interior shifts plus deterministic complementarity mass balancing; omega_p and omega_d are ignored. The public resolver reports profile=:post_scaling_mehrotra, the plan records the neutral deferred identity :automatic_mehrotra, and executed diagnostics record :post_scaling_mehrotra. Use :fixed in expert mode to preserve supplied values exactly with provenance :user_fixed. parameter_strategy=:adaptive (the default) enables the bounded Mehrotra controller, independent primal/dual step safeguards, adaptive refinement limits, and complete fixed-path fallback. See the adaptive parameter policy.\n\nNative SDP checkpoints are iterate-level warm restarts, not full execution snapshots. The dedicated LP path does not currently support checkpoint resume. Julia must be started with at least as many threads as a solve may request (julia -t 4).","title":"Options"},{"category":"section","location":"jump/#Current-limitations","page":"JuMP and MOI","text":"The wrapper is non-incremental. Modifying and re-solving a JuMP model causes the finalized SDPX representation to be rebuilt.\nAt least one scalar, SOC, or PSD cone constraint is required.\nRotated SOC constraints use the exact sparse map (u,v,w) -> (u+v,u-v,sqrt(2)w) into NativeSOC; MOI primal and dual getters apply the inverse and adjoint maps. Nonnegatives, Nonpositives, and Zeros vector sets are lowered in batches. General Lorentz SOC constraints use NativeSOC directly; strict local fixed-trace Q3 products may select the compact specialization. Mixed PSD+SOC models fail clearly rather than silently lifting one cone family, and other nonsymmetric cones remain unsupported.\nSparse and dense coefficient storage both support internal equilibration; sparse derived caches are rebuilt after scaling.\nLP unboundedness and general conic infeasibility certificates are not yet available in every numerical-breakdown case.\n\nBootstrap integrations that already have final block arrays may continue to use the qualified SDPProblem/loader internals, but new native code should use the typed Model route. No production route converts SOC blocks into PSD blocks.","title":"Current limitations"},{"category":"section","location":"socp/#Native-SOC-and-fixed-trace-PSD-blocks","page":"SOCP and fixed trace","text":"The public SOC route is a typed Model with a LorentzCone or RotatedLorentzCone block. No PSD arrow matrix is needed:\n\nusing SDPX\n\n# minimise t subject to (t, x, y) in Q3 and x=3, y=4\nmodel = Model(Float64)\nq = variable!(model, :q, 3; domain=LorentzCone())\nconstraint!(model, :fix_tail, [q[2] - 3, q[3] - 4], ZeroCone())\nobjective!(model, Minimize(), q[1])\nresult = optimize!(\n    model;\n    settings=Settings(model; algorithm=:socp, verbosity=0),\n    outputs=Outputs(:all, :all, :all; objectives=true, certificate=:summary),\n)\n\nAn affine expression in a LorentzCone() or RotatedLorentzCone() is canonicalized as one native cone block. ZeroCone() and Reals() may appear alongside it; adding a non-free LP or PSD block makes the model a mixed route and causes optimize! to fail closed before lowering.\n\nJuMP/MOI rotated cones are supported through the exact isomorphism\n\n(u, v, w) in Qr  <=>  (u+v, u-v, sqrt(2)w) in Q.\n\nThe JuMP/MOI bridge builds sparse rows directly, maps duals by the transpose of this linear transformation, and maps primal values back through its inverse. The native constraint! API remains a standard-Lorentz API; RSOC is selected by the RotatedLorentzCone() domain rather than by a second solver.","title":"Native SOC and fixed-trace PSD blocks"},{"category":"section","location":"socp/#Native-Lorentz-execution","page":"SOCP and fixed trace","text":"Pure-SOC JuMP/MOI models and public Model instances run directly in Lorentz coordinates:\n\nModel / MOI\n  -> NativeConeProgram\n  -> ConeRepresentationPlan\n       -> GeneralLorentzExecution\n       -> FixedTraceQ3Execution (verified specialization)\n  -> FormulationPlan\n       -> DenseNormalEquations\n       -> DenseAugmentedKKT\n  -> LABackendConfiguration\n  -> NativeSOCWorkspace\n  -> original-coordinate certificate\n\nThe native SOC route never constructs PSD matrices. The compact API has no representation switch. An SDP model remains an SDP model even when a block has a Lorentz-compatible algebraic structure; algorithm=:sdp keeps the ordinary SDP route, while algorithm=:socp is accepted only for a pure SOC/RSOC model. Structural 2×2/fixed-trace kernels and storage decisions stay inside their classified family, and no family conversion is attempted.\n\nFor x = (t,u), SDPX uses the Euclidean Lorentz pairing, determinant t^2 - dot(u,u), margin t - norm(u), and Jordan product\n\n(t,u) o (s,v) = (t*s + dot(u,v), t*v + s*u).\n\nNesterov–Todd scaling, W, W^-1, and (W'W)^-1 operate directly on vectors. Determinants and boundary roots are scale-normalized, with a stable quadratic q formula. Actual trial points are rechecked for strict interior. Scalar nonnegative blocks contribute barrier degree one; proper Lorentz blocks contribute degree two.\n\nDense normal equations are the default. The primal metric is factored once per iteration and reused for predictor/corrector right-hand sides. Dependent equalities use rank-revealing QR only when equality_solver=:auto and the frozen LA plan authorizes it; explicit normal equations fail closed.\n\nformulation=:augmented assembles the symmetric-indefinite system\n\n[ H  -Aeq' ] [dx] = [r ]\n[-Aeq   0   ] [dy]   [-p]\n\nand requires pivoted symmetric LDLT, multi-RHS solve, and inertia metadata. Standard LA rejects this route. MFLA and BFLA execute it through provider-owned factor handles without a generic retry.","title":"Native Lorentz execution"},{"category":"section","location":"socp/#Q3-equivalence-and-fixed-trace-reduction","page":"SOCP and fixed trace","text":"For a real symmetric matrix\n\nX=beginbmatrixabbcendbmatrixqquad a+c=tau\n\nthe PSD constraint is exactly equivalent to\n\n(tau a-c 2b)inmathcal Q_3\n\nThus the common bootstrap block\n\nbeginbmatrixqrr2-qendbmatrixsucceq0\n\nis the unit disk (q-1)^2+r^2 <= 1. The Q3 <-> S_+^2 isomorphism is retained for derivation and tests only; production Q3 stays in Lorentz coordinates.\n\nSDPX.analyze_fixed_trace(problem) detects direct constant traces and, when the estimated relation-analysis work is conservative, traces implied by B'x=b. Relations are accepted only after an original-arithmetic residual check. Expensive large-block equality scans are recorded as skipped because no automatic larger-block basis reduction is promoted yet. Negative trace is reported as infeasible; zero trace implies a zero PSD block; positive 2×2 blocks are marked as SOC candidates; larger direct fixed-trace blocks are marked for a future traceless-basis reduction.\n\nFor sparse 2×2 models, solve setup records whether every coefficient is traceless. Dominant Schur and reduced-arrow contractions then use A11*(M11-M22)+2*A12*M12 and avoid the redundant second diagonal multiply without changing the persistent model or checkpoint layout.","title":"Q3 equivalence and fixed-trace reduction"},{"category":"section","location":"socp/#FixedTraceQ3-specialization","page":"SOCP and fixed trace","text":"FixedTraceQ3 is not a second solver. It is a payload inside NativeSOCPlan and shares residuals, NT scaling, predictor/corrector policy, line search, result type, diagnostics, providers, and certificate with GeneralLorentz.\n\nPromotion is strict: every block must have dimension three, a positive constant head, zero head coefficients, exactly two nonsingular local tail variables, no shared variables, and complete variable coverage. Under specialization=:auto, the compact execution is selected whenever the exact reduction verifies; otherwise :auto uses GeneralLorentz. A forced specialization=:fixed_trace requires that exact reduction.\n\nFor eligible blocks SDPX stores three local metric entries and a local 2-by-2 Cholesky factor. It eliminates each block locally and forms only the global equality panel/Gram when equalities exist. Diagnostics report executed_factorization=:native_local_cholesky and executed_backend=:fixed_trace_local_elimination.\n\nFixed trace removes the primal identity coordinate, but it does not reduce the two transformed equality rows of a generic (q,r) cell to one. The native backend therefore retains the validated block-diagonal equality elimination and forms the same triangular equality Gram from a 2L x n transformed panel. On large equality-heavy models the equality Gram can dominate after cone-local matrix work has been removed.\n\nFor Float64x4 and BigFloat, the two local Cholesky pivot reciprocals are computed once per cone and reused across every equality column plus both predictor/corrector triangular solves. Float64x4 CSDR layouts with exact adjacent two-row ownership additionally fuse the panel copy and local transform, so each worker reads immutable source rows and writes a disjoint destination range. Broader layouts and BigFloat keep the copy-then-transform path. Executed diagnostics expose local_pivot_kernel and equality_panel_transform.","title":"FixedTraceQ3 specialization"},{"category":"section","location":"socp/#Q3-direction-and-controller","page":"SOCP and fixed trace","text":"The default method is a compact Mehrotra predictor-corrector with an HKM-equivalent local direction. An ownership-safe Lorentz Nesterov–Todd direction is also implemented internally for Q3, but it is not exposed as a solve option; HKM remains the validated execution path after matched certificate and performance gates.\n\nThe compact backend currently owns its iteration controller: it selects sigma from the affine complementarity ratio and uses a 0.99 exact fraction-to-boundary safety. The general SDP AdaptiveIPMController options (beta, gamma, parameter_strategy, and adaptive_sigma_max) do not yet change this Q3 trajectory. For parameter_policy=:auto, NativeSOC first solves its identity-metric affine KKT system, then shifts each Lorentz head to strict interior, raises aggregate head mass to the Lorentz product identity mass <e,e> only when a side remains within the typed cone-vertex envelope, and cross-centers complementarity. Balanced nonvertex affine points are not renormalized. Barrier degree continues to normalize complementarity and does not change the identity-mass floor. OmegaP and OmegaD therefore do not participate in the automatic Q3 start. Explicit fixed policy retains the historical head scales exactly. Executed initialization diagnostics report the affine residuals, shifts, margins, complementarity, factor count, and two RHS solves.","title":"Q3 direction and controller"},{"category":"section","location":"socp/#Precision,-results,-and-certification","page":"SOCP and fixed trace","text":"NativeSOC workspaces use alloc_zeros; mutable BigFloat slots receive owned copies. Model coefficients are never installed by reference into scratch storage. MFLA/BFLA factor handles retain provider state and configured precision.\n\nThe public Result reconstructs primal slacks, cone duals, and equality multipliers in the original model coordinates. For MOI rotated cones the core uses the exact canonical Lorentz image and getters return the original rotated coordinates. Timings, termination counters, and diagnostics come directly from the NativeSOC run; no hidden PSD solve or result is stored. Certification independently recomputes affine/equality residuals, stationarity, primal and dual Lorentz margins, objectives, gap, and complementarity. Pure-SOC MOI input stays on the native route.\n\nLightweight development evidence for the native routes is summarized in benchmarks.md, with full reports under bench/soc_fixed_trace/README.md.","title":"Precision, results, and certification"},{"category":"section","location":"socp/#Native-SOC-implementation-references","page":"SOCP and fixed trace","text":"The explicit Q3 NT implementation follows published, open implementations rather than proprietary behavior. Clarabel's native SOC implementation stores the Nesterov–Todd point and applies W'W as a rank-one Lorentz update; for Q3 its dense packed scaling block has only six entries. Clarabel and ECOS use a rank-two KKT expansion for larger SOCs, but adding two extension variables per Q3 cell is not automatically advantageous for fixed-trace CSDR.\n\nClarabel.rs SOC operations\nClarabel.rs KKT numeric maps\nClarabel.jl SOC operations\nECOS cone scaling","title":"Native-SOC implementation references"},{"category":"section","location":"socp/#Reusing-a-model","page":"SOCP and fixed trace","text":"The public Model is mutable while it is being built and immutable from the solver's point of view after optimize! starts. A new objective direction is represented by a new model; warm starts can be attached with the exported set_start!, set_dual_start!, and set_dual_slack_start! helpers before a solve. Each call creates fresh numeric workspace and certification state.\n\nThe qualified prepared-session API remains available for compatibility integrations that need to reuse immutable preprocessing. It is sequential and non-reentrant, and is not part of the public v0.5 quickstart route.","title":"Reusing a model"},{"category":"section","location":"quickstart/#Quick-start","page":"Quick start","text":"","title":"Quick start"},{"category":"section","location":"quickstart/#Install","page":"Quick start","text":"Until SDPX is registered, install it directly from GitHub:\n\nusing Pkg\nPkg.add(url=\"https://github.com/yongjunx23-del/SDPX.jl\")","title":"Install"},{"category":"section","location":"quickstart/#The-public-model-route","page":"Quick start","text":"The v0.5 public interface has one modeling path. Create a typed Model, add product-cone variables with variable!, add affine cone constraints with constraint!, set one objective with objective!, and solve with optimize!. Settings selects policy and Outputs selects retained result fields; the result is read through accessors rather than by reaching into solver structs.\n\nusing SDPX\nusing LinearAlgebra\n\nmodel = Model(Float64)\nw = variable!(model, :w, 3; domain=Reals())\nconstraint!(model, :normalization, w[1] - 1, ZeroCone())\nconstraint!(model, :quartic_recurrence, w[1] - w[2] - w[3], ZeroCone())\nconstraint!(\n    model,\n    :moment_matrix,\n    [w[1] w[2]; w[2] w[3]],\n    PSDCone(),\n)\nobjective!(model, Maximize(), w[2])\n\nsettings = Settings(\n    model;\n    algorithm=:sdp,\n    limits=Limits(iterations=200, time=60.0, threads=1),\n    verbosity=0,\n)\noutputs = Outputs(:all, :all, :all; objectives=true, certificate=:summary)\nresult = optimize!(model; settings=settings, outputs=outputs)\n\n@assert status(result) == :optimal\nmoments = value(result, w)  # approximately [1, 0.618034, 0.381966]\nH = [moments[1] moments[2]; moments[2] moments[3]]\neigvals(Symmetric(H))       # nonnegative up to solver tolerance\nprimal_objective(result)    # approximately 0.618034\ncertificate(result).valid  # independent original-coordinate check\n\nThis is the g=1 two-by-two moment truncation of the quartic integral: W0=1, W0-W2-W4=0, and [W0 W2; W2 W4] is positive semidefinite.\n\nModel(Float64) is the default arithmetic. Model(BigFloat; precision_bits=256) and loaded MultiFloats types provide the other typed arithmetic choices; all model data is converted and owned in that arithmetic.","title":"The public model route"},{"category":"section","location":"quickstart/#The-three-pure-routes","page":"Quick start","text":"The route classifier looks only at the non-free cone families in the model. Use one family per model, with Reals and ZeroCone allowed as auxiliary blocks:\n\nRoute Product/constraint domains Settings.algorithm\nLP Nonnegative, Nonpositive, ZeroCone :auto or :lp\nnative SOC/RSOC LorentzCone, RotatedLorentzCone, ZeroCone :auto or :socp\nSDP PSDCone, ZeroCone :auto or :sdp\n\nFor a native Lorentz model, keep the cone block in Lorentz coordinates:\n\nsoc = Model(Float64)\nq = variable!(soc, :q, 3; domain=LorentzCone())\nconstraint!(soc, :fix_tail, [q[2] - 3, q[3] - 4], ZeroCone())\nobjective!(soc, Minimize(), q[1])\nsoc_result = optimize!(\n    soc;\n    settings=Settings(soc; algorithm=:socp, verbosity=0),\n    outputs=Outputs(:all, :all, :all; objectives=true, certificate=:summary),\n)\n@assert status(soc_result) == :optimal\n\nRotatedLorentzCone() is available for native RSOC blocks. A PSD variable is declared with two shape arguments and remains one matrix block:\n\nsdp = Model(Float64)\nX = variable!(sdp, :X, 2, 2; domain=PSDCone())\nconstraint!(\n    sdp,\n    :lower,\n    [X[1, 1] X[1, 2] - 1; X[2, 1] - 1 X[2, 2]],\n    PSDCone(),\n)\nobjective!(sdp, Minimize(), 2 * X[1, 1] + 3 * X[2, 2])\nsdp_result = optimize!(sdp; settings=Settings(sdp; algorithm=:sdp, verbosity=0))\n\nChoose algorithm=:lp, :socp, or :sdp to match the model family shown in the table. :auto asks SDPX to select the matching implemented route.","title":"The three pure routes"},{"category":"section","location":"quickstart/#Settings-and-retained-outputs","page":"Quick start","text":"Settings{T} is the typed policy boundary. The most useful fields are tolerances=Tolerances{T}(; primal=..., dual=..., gap=...), limits=Limits(; iterations=..., time=..., threads=...), algorithm, presolve, scaling, sparse, formulation, provider, equality_solver, working_precision_policy, diagnostics, verbosity, timing, certification, and blas_threads. Public formulation names are :auto, :variable_space_schur, and :dense_augmented_kkt; unsupported combinations fail closed.\n\nOutputs controls :all/:none (or typed reference vectors) for primal, constraint_dual, and dual_slack, plus objectives, certificate, diagnostics, history, and trace. Accessing a field that was not retained raises ResultFieldNotRetained instead of silently recomputing it.\n\nThe stable result accessors are status, value, dual, dual_slack, primal_objective, dual_objective, certificate, diagnostics, iteration_history, performance_trace, and execution_plan.","title":"Settings and retained outputs"},{"category":"section","location":"quickstart/#Qualified-compatibility-internals","page":"Quick start","text":"SDPX.ingest, SDPX.solve!, SDPX.linear_program, SDPX.second_order_program, SDPX.solve_socp, and SDPX.SolverOptions remain mature, qualified implementation interfaces used by algorithm tests and compatibility integrations. They are intentionally not the public v0.5 quickstart surface; new application code should use the typed Model route above.","title":"Qualified compatibility internals"},{"category":"section","location":"adaptive-dense-sparse-optimization/#Adaptive-Dense/Sparse-Optimization-for-SDPX","page":"Adaptive dense/sparse optimization","text":"Date: 2026-07-24\n\nStatus update (2026-07-25). This document preserves the dated kernel measurements below. Since those measurements, SDPX has added type-native equality presolve with dual reconstruction, original-coordinate result certification, a guarded adaptive parameter controller, owned BigFloat kernels, a fused exact-arrow path that allocates neither transformed panels nor pair buffers, and a usable-memory-aware scheduler. See the current extended-precision report, automatic-pipeline report, and threading results.","title":"Adaptive Dense/Sparse Optimization for SDPX"},{"category":"section","location":"adaptive-dense-sparse-optimization/#Executive-result","page":"Adaptive dense/sparse optimization","text":"The lattice-bootstrap Task_Low08 problem is not accurately described by a single \"sparse\" or \"dense\" label:\n\nStructure Measured value Consequence\nIndividual PSD coefficient density 0.1020% Store and transform coefficients sparsely\nVariable/block incidence density 58.57% Skip absent coefficient matrices\nAggregate PSD upper-pattern density 99.84% Keep each primal/dual PSD block dense\nSchur upper-pattern density 84.26% Assemble and factor a dense Schur complement\nEquality matrix rank 394 of 482 Type-native presolve removes 88 dependent columns and reconstructs the original dual multiplier layout\n\nSDPX now detects these structures independently. On this problem, sparse=:auto chooses the sparse_coefficients_dense_psd_dense_schur profile. This is the appropriate hybrid algorithm: a sparse coefficient representation does not imply a sparse PSD matrix or sparse Schur factor.","title":"Executive result"},{"category":"section","location":"adaptive-dense-sparse-optimization/#Implemented-changes","page":"Adaptive dense/sparse optimization","text":"Automatic structure analysis\nIngestion measures coefficient, incidence, aggregate block-pattern, and Schur-pattern density. SDPX.structure_summary(problem) exposes the selected plan. The user may still force :sparse or :dense.\nSparse-native Julia and MOI ingestion\nVector-of-sparse-matrix input no longer passes through a dense m × k × k representation. The MathOptInterface wrapper now constructs the same sparse-native form directly from affine terms. ActiveSparseCoefficientVector additionally stores only the sorted active variable ids and matrices for a block, avoiding the otherwise quadratic empty-reference grid in models with many blocks and variables.\nSparse two-sided PSD transforms\nFor general block dimensions, SDPX computes Y*A_i*inv(X) from the nonzeros of A_i. This replaces a dense cubic matrix product with work proportional to nnz(A_i) * k^2.\nMOSEK-style coefficient ordering\nActive coefficient matrices are ordered by decreasing nonzero count before Schur assembly. This follows the outer-product assembly guidance in MOSEK's published semidefinite implementation material.\nDensity-aware Schur assembly\nWhen coefficients are sparse but the Schur matrix is dense, SDPX streams block contributions into task-local dense accumulators. A memory planner caps the number of accumulators against currently usable memory. If per-block packed contributions are cheaper, it selects that representation instead.\nDense BLAS kernel\nThe native dense Float64 coefficient path uses triangular BLAS SYRK, then mirrors once. Generic arithmetic retains a type-stable kernel.\nParallel scheduling\nPSD blocks are assigned to threads by estimated work rather than block count. Julia threads parallelize sparse Schur assembly; BLAS threads accelerate the subsequent dense Cholesky phase. BigFloat remains serial in SDPX, while fixed-width Float64x4 uses Julia threads.","title":"Implemented changes"},{"category":"section","location":"adaptive-dense-sparse-optimization/#Task_Low08-measurements","page":"Adaptive dense/sparse optimization","text":"The exact benchmark has 6119 scalar variables, 482 equality constraints, and 32 PSD blocks. All reported Schur builds used X=Y=I. A deterministic sample of 256 entries was checked against direct sparse Frobenius products; the maximum absolute and relative errors were both zero.","title":"Task_Low08 measurements"},{"category":"section","location":"adaptive-dense-sparse-optimization/#Float64-thread-scaling","page":"Adaptive dense/sparse optimization","text":"Julia threads BLAS threads during Schur build Schur build (s) Speedup\n1 1 15.696 1.00x\n2 1 10.667 1.47x\n4 1 7.523 2.09x\n8 1 5.840 2.69x\n\nThe dense KKT factor phase was measured separately. It decreased from 1.989 seconds with one BLAS thread to 0.682 seconds with four BLAS threads. The equality factor used pivoting because the equality matrix is rank deficient. A separate SVD of the original 482 × 6119 equality matrix found rank 394 with the standard LAPACK tolerance.\n\nThe memory/performance trade-off is visible at larger thread counts:\n\nConfiguration Workspace Schur (s) KKT factor (s)\n1 Julia / 1 BLAS 635 MB 16.797 1.989\n4 Julia / 4 BLAS 1833 MB 8.264 0.682\n8 Julia / 4 BLAS 2440 MB 6.414 0.698\n\nThese are kernel timings, not complete solver timings. They isolate the part changed by this optimization and should not be compared directly with a full MOSEK solve.","title":"Float64 thread scaling"},{"category":"section","location":"adaptive-dense-sparse-optimization/#Precision-paths","page":"Adaptive dense/sparse optimization","text":"The exact Task_Low08 Float64x4 Schur build took 22.258 seconds with four Julia threads, used 4929 MB of workspace, and passed the same sampled zero-error check. Exact KKT factorization was deliberately not included: generic 6119-by-6119 Cholesky is a separate and substantially more expensive operation.\n\nThe full Task_Low08 BigFloat kernel was not run on the 16 GiB test machine. At 256-bit precision, the two persistent 6119-by-6119 BigFloat Schur matrices alone are estimated at 6.59 GB before equality buffers, coefficients, heap overhead, and MPFR temporaries. The current BigFloat path is also serial. Running the exact kernel would therefore create substantial memory pressure and a long local run; it was replaced by the scaled structural test below.\n\nLarger synthetic problems with the same structural profile provided a safe cross-precision check:\n\nArithmetic Sparse adaptive / forced-dense Schur speedup Relative error\nFloat64 3.72x 1.27e-16\nFloat64x4 72.90x 0\nBigFloat 4.22x 0\n\nThe cases have precision-appropriate sizes, so these rows validate dispatch and quantify within-case algorithmic speedup; they are not cross-type throughput comparisons.","title":"Precision paths"},{"category":"section","location":"adaptive-dense-sparse-optimization/#Current-full-solve-Float64-baseline","page":"Adaptive dense/sparse optimization","text":"A subsequent release-candidate run solved the exact Task_Low08 model after reducing the equality system from 482 to 394 independent columns:\n\nMetric Value\nStatus Optimal\nIterations 27\nSolve / total time 46.8814 s / 47.3922 s\nPrimal / dual objective 0.653291393898 / 0.653290938722\nRelative gap 4.55175e-7\nPrimal / dual residual 2.06455e-10 / 3.68147e-9\nEquality residual 2.06002e-12\nMinimum primal / dual PSD eigenvalue -8.77297e-15 / 2.12097e-14\n\nThis is a complete solver and original-coordinate certificate result, unlike the earlier Schur-only timing tables. It is not a solver-to-solver performance claim.","title":"Current full-solve Float64 baseline"},{"category":"section","location":"adaptive-dense-sparse-optimization/#MOSEK-lessons-applied","page":"Adaptive dense/sparse optimization","text":"MOSEK's current modeling guidance says that large sparse LMIs should normally be dualized so that the sparse coefficient count is retained. Its SDO API also uses a scaled triangular vectorization of symmetric matrices, avoiding duplicated off-diagonal input. SDPX now receives the benchmark in the corresponding sparse-coefficient form and stores each symmetric coefficient only as a sparse matrix.\n\nMOSEK's published SDO implementation slides describe Schur assembly as sums of outer products, triangular-only computation, and sorting coefficient columns by decreasing nonzero count. The present implementation adopts the ordering and sparse-product ideas. Because Task_Low08's Schur pattern is 84.26% dense, switching its final factorization to a generic sparse Cholesky would probably add symbolic overhead and fill without saving enough work; dense Cholesky is the correct default here.\n\nRelevant official references:\n\nMOSEK Modeling Cookbook: practical semidefinite modeling\nMOSEK semidefinite optimization tutorial\nMOSEK SDO implementation slides\nMOSEK presolve overview\nMOSEK thread parameter reference\n\nThe implementation slides are historical design material, not a claim about the exact internals of the latest MOSEK release.","title":"MOSEK lessons applied"},{"category":"section","location":"adaptive-dense-sparse-optimization/#Recommended-next-work","page":"Adaptive dense/sparse optimization","text":"","title":"Recommended next work"},{"category":"section","location":"adaptive-dense-sparse-optimization/#P0:-equality-presolve-with-a-reversible-dual-map-—-implemented","page":"Adaptive dense/sparse optimization","text":"SDPX now detects a numerical basis of the columns of B in the problem's arithmetic type, checks that discarded columns and their right-hand sides obey the same dependencies, solves with the 394 independent columns, and maps the compact dual multipliers back to the original 482-column layout. Ambiguous rank decisions retain all equalities, and inconsistent dependencies terminate with an infeasibility diagnostic before factorization.\n\nThis avoids narrowing a Float64x4 or BigFloat rank decision through Float64, reduces equality right-hand sides and normal-equation work, and preserves the public result shape. Symbolic-basis caching across repeated structurally identical solves remains future work.","title":"P0: equality presolve with a reversible dual map — implemented"},{"category":"section","location":"adaptive-dense-sparse-optimization/#P1:-lower-triangle-tiled-Schur-assembly","page":"Adaptive dense/sparse optimization","text":"The current task-local dense accumulators scale as threads * m^2 * sizeof(T), while the packed-block alternative scales with the sum of active pairs. Partition the lower triangle into fixed-size tiles and give each tile a single writer. This should preserve deterministic assembly while reducing peak memory, especially for Float64x4 and clusters with many threads.","title":"P1: lower-triangle tiled Schur assembly"},{"category":"section","location":"adaptive-dense-sparse-optimization/#P2:-packed-coefficient-structures-—-partially-implemented","page":"Adaptive dense/sparse optimization","text":"Flat COO coefficient storage, packed 2x2 coefficients, active-variable lists, and lazy shared-pattern groups are implemented. The exact-arrow fused kernel consumes coefficient values directly and deliberately creates neither transformed panels nor pair buffers. A fully unified block-level structure-of-arrays representation is still useful for general sparse blocks:\n\nvariable offsets;\npacked row/column indices;\ncontiguous values;\ngroups sharing the same sparsity pattern.\n\nThis will reduce pointer chasing, metadata, and garbage-collector pressure. Pattern groups can use small batched dense kernels or generated fixed-size contractions.","title":"P2: packed coefficient structures — partially implemented"},{"category":"section","location":"adaptive-dense-sparse-optimization/#P3:-phase-aware-parallelism-—-implemented,-with-scheduler-work-remaining","page":"Adaptive dense/sparse optimization","text":"The execution plan now assigns exact Julia worker counts, switches Julia and BLAS parallelism by phase, uses work-weighted scheduling, and keeps BigFloat serial. The cluster driver exposes both counts so oversubscription can be measured rather than assumed. Persistent worker teams, NUMA first-touch allocation, affinity reporting, and a concurrent-solve-safe BLAS policy remain future work. Distributed memory is not useful for this matrix until Schur assembly and factorization are explicitly distributed; merely launching several processes for one solve would duplicate multi-gigabyte workspaces.","title":"P3: phase-aware parallelism — implemented, with scheduler work remaining"},{"category":"section","location":"adaptive-dense-sparse-optimization/#P4:-mixed-precision-KKT-solve","page":"Adaptive dense/sparse optimization","text":"For Float64x4 or BigFloat solves, assemble residuals and updates in the target type but factor a scaled Float64 Schur matrix when conditioning permits, then apply high-precision iterative refinement. Fall back to a full target-type factor if refinement stagnates. This is the most plausible route to making large extended-precision problems practical.","title":"P4: mixed-precision KKT solve"},{"category":"section","location":"adaptive-dense-sparse-optimization/#P5:-adaptive-interior-point-parameters-—-implemented-and-guarded-by-default","page":"Adaptive dense/sparse optimization","text":"parameter_strategy=:adaptive now uses a separate typed policy component. It computes a pure affine predictor, global mu and mu_aff, a bounded Mehrotra sigma, independent primal/dual step safeguards, and adaptive refinement limits. Non-finite diagnostics, degraded equality factors, or unstable progress restore the complete fixed predictor/corrector path.\n\nThe warmed representative CSDR s15 solve improved from 19 iterations and 15.749 ms to 15 iterations and 13.260 ms, with both solutions certified. A controlled Task_Low08 run instead matched the fixed 24 iterations but was 1.7% slower after a safe iteration-19 fallback. The current default therefore adds a state-based cold-start guard and retains complete fixed-policy fallback. The complete audit and benchmark table are in Adaptive Interior-Point Parameter Policy.","title":"P5: adaptive interior-point parameters — implemented and guarded by default"},{"category":"section","location":"adaptive-dense-sparse-optimization/#P6:-chordal-decomposition-only-when-the-aggregate-PSD-graph-is-sparse","page":"Adaptive dense/sparse optimization","text":"Chordal conversion is valuable when the union sparsity pattern of a large PSD block is sparse and has small cliques. It is not appropriate for Task_Low08, whose aggregate pattern is 99.84% dense. The classifier should continue to guard this transformation rather than applying it merely because individual coefficient matrices are sparse.","title":"P6: chordal decomposition only when the aggregate PSD graph is sparse"},{"category":"section","location":"adaptive-dense-sparse-optimization/#Cluster-settings","page":"Adaptive dense/sparse optimization","text":"Start with:\n\nJULIA_NUM_THREADS=8 OPENBLAS_NUM_THREADS=4 julia --project=...\n\nThen measure 4/8/16 Julia threads with 1/4/8 BLAS threads. Select by total iteration time and peak resident memory, not Schur time alone. On a NUMA server, bind one solve to one socket first. BigFloat should use one Julia thread in the current SDPX implementation; use Float64x4 when roughly 200-bit precision and a Float64 exponent range are sufficient. Record the deployed commit and environment by following the cluster workflow.","title":"Cluster settings"},{"category":"section","location":"#SDPX.jl","page":"Home","text":"A native Julia semidefinite-programming solver: primal-dual interior-point method with the HRVW/KSH/M direction and a Mehrotra predictor-corrector, in arbitrary precision (Float64, MultiFloats.Float64xN, BigFloat). Built for bootstrap problems whose conditioning exceeds what Float64 can carry, with native LP and SOCP paths alongside the PSD engine.\n\nStatus: experimental, pre-1.0. The API may change between minor versions.","title":"SDPX.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url=\"https://github.com/yongjunx23-del/SDPX.jl\")","title":"Installation"},{"category":"section","location":"#First-solve","page":"Home","text":"The public route is a typed Model: declare product-cone variables, add affine cone constraints, set one objective, then call optimize!. This two-by-two quartic moment SDP uses only exported names and returns a typed result whose fields are read through accessors:\n\nusing SDPX\n\nmodel = Model(Float64)\nw = variable!(model, :w, 3; domain=Reals())\nconstraint!(model, :normalization, w[1] - 1, ZeroCone())\nconstraint!(model, :quartic_recurrence, w[1] - w[2] - w[3], ZeroCone())\nconstraint!(model, :moment_matrix, [w[1] w[2]; w[2] w[3]], PSDCone())\nobjective!(model, Maximize(), w[2])\n\nsettings = Settings(\n    model;\n    algorithm=:sdp,\n    limits=Limits(iterations=200, time=60.0, threads=1),\n    verbosity=0,\n)\noutputs = Outputs(:all, :all, :all; objectives=true, certificate=:summary)\nresult = optimize!(model; settings=settings, outputs=outputs)\n\nstatus(result)             # :optimal\nvalue(result, w)           # approximately [1, 0.618034, 0.381966]\nprimal_objective(result)   # approximately 0.618034\ncertificate(result).valid  # true when the independent check passes\n\nThe three runnable, self-checking examples are deliberately the only examples promoted by the v0.5 documentation:\n\nexamples/quartic_bootstrap_sdp.jl — certified moment-space bounds with Float64 and extended precision.\nexamples/quartic_discrete_lp.jl — a Chebyshev-rational atomic discretization of the same measure.\nexamples/quartic_2x2_socp.jl — the exact Lorentz form of the first two-by-two moment constraint.","title":"First solve"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Quick start — the typed Model builder and result accessors.\nSOCP and fixed trace — native Lorentz solves and the Q3 specialization.\nPrecision — choosing Float64, Float64x4, or BigFloat.\nJuMP and MOI — JuMP modeling through the SDPX optimizer.\nAutomatic pipeline — classification, presolve, scaling, and planning.\nPreprocessing — conservative structural reductions.\nSparse execution — frozen-CSC Schur and LP paths.\nParameters — the common and expert option surface.\nDiagnostics and certificates — interpreting a result.\nArchitecture — frontend, midend, planning, and KKT formulations.\nLinear-algebra providers — the replaceable LA seam.\nBenchmarks and evidence — measured results and policy.\nAPI reference — qualified implementation and metadata reference; the public quickstart is the typed Model route above.\nREADME — installation and a minimal semidefinite solve.\n\nThe repository's docs/ directory keeps the long-term operational and research notes: adaptive dense/sparse optimization, adaptive parameter policy, bridge schema, cluster workflow, and threading.","title":"Where to go next"}]
}
